# Raw Scan: {{project_name}}

> This file is auto-generated by `prospec knowledge init` (or `prospec knowledge init --raw-scan-only`) for analysis by the `prospec-knowledge-generate` Skill.
> Do not edit manually. It is regenerated on every run of `prospec knowledge init` (including `--raw-scan-only`).

## Tech Stack

| Item | Value |
|------|-------|
| Language | {{#if tech_stack.language}}{{tech_stack.language}}{{else}}unknown{{/if}} |
| Framework | {{#if tech_stack.framework}}{{tech_stack.framework}}{{else}}—{{/if}} |
| Package Manager | {{#if tech_stack.package_manager}}{{tech_stack.package_manager}}{{else}}—{{/if}} |
| Source | {{#if tech_stack.source}}{{tech_stack.source}}{{else}}auto-detected{{/if}} |

## Entry Points

{{#if entry_points}}
{{#each entry_point_displays}}
- {{this}}
{{/each}}
{{else}}
_No entry points detected_
{{/if}}

## Dependencies

{{#if dependencies}}
{{#each dependencies}}
- {{this.name_display}}{{#if this.version_display}} @ {{this.version_display}}{{/if}}
{{/each}}
{{else}}
_No dependencies detected_
{{/if}}

## Config Files

{{#if config_files}}
{{#each config_file_displays}}
- {{this}}
{{/each}}
{{else}}
_No config files detected_
{{/if}}

## Directory Tree

```
{{directory_tree}}
```

## Directories Without Source Files

> Directories that hold files but no source file. `module-detector.ts` counts a file as source
> only when it carries an extension AND that extension is not on its non-source denylist — so a
> directory whose only content is extensionless files (a `bin/` of scripts) qualifies too, its
> files reported as `(no extension)`. Root-level files belong to no directory and are never listed.
>
> This is a fact about the scan, not a detection verdict. A directory listed here can still be a
> module two ways: a curated `module-map.yaml`, which detection prefers over every heuristic, and
> the no-module fallback, which re-runs detection over the unfiltered file list when the narrowed
> pass finds nothing. Read the list as evidence — if one of these is this project's substance
> (Kubernetes manifests, LaTeX chapters, a Makefile-driven build) and `module-map.yaml` does not
> already cover it, put it there deliberately. Nested directories fold into their topmost
> non-source ancestor, and both lists are ordered by file volume — a cap keeps the biggest, not the
> alphabetically first.

{{#if non_source_directories}}
{{#each non_source_directories}}
- {{this.path_display}} — {{this.file_count}} {{#if (eq this.file_count 1)}}file{{else}}files{{/if}}: {{#each this.extension_displays}}{{#unless @first}}, {{/unless}}{{this}}{{/each}}{{#if this.extensions_omitted}} (+{{this.extensions_omitted}} more){{/if}}
{{/each}}
{{else}}
_Every directory holds at least one source file_
{{/if}}
{{#if non_source_directories_omitted}}
_… and {{non_source_directories_omitted}} more directories, omitted by the {{non_source_directories_cap}}-directory cap_
{{/if}}

## File Stats

| Metric | Value |
|--------|-------|
| Total files | {{file_stats.total_files}} |
| Scan depth | {{file_stats.scan_depth}} |
