# Review Findings: harden-contained-reads

| ID | Location | Severity | Lens | Status | Summary |
|---|---|---|---|---|---|
| F-1 | src/lib/knowledge-reader.ts:113 | major | correctness | fixed | 把讀取失敗一律轉成 null 會讓存在但讀不到的 module-map.yaml 退化為「沒有 map」，check.service 隨即靜默改用 constitutionFallbackRules() 並對著存在的檔案回報 not found——牴觸 loadModuleMap 自己「do not silently switch rule sets」的承諾。已把讀取結果改為 absent/escaped/unreadable 三態，governance 載入器（module-map、feature-map）對 unreadable 改為 loud，raw content 讀取維持 graceful。Mutation：移除 loud 分支 → 新測試轉紅（killed）。 |
| F-2 | src/lib/drift-sources.ts:287 | major | parallel-site | fixed | 同檔案六個姊妹 collector 仍以裸 readFileSync 讀 readdir 列出的 .md，一個名為 oops.md 的目錄就讓 collectReqDefinitions／collectTaskStates 拋 EISDIR 並帶走其餘 13 個 verdict——正是本變更宣稱修掉的類別，落地的 REQ-LIB-014 因此過度宣稱。已改走非拋錯的 readTextOrSkip（刻意不加 containment，掃描範圍不變、只改失效模式），並補三個回歸測試與 delta-spec 條文。Mutation：把 collectReqDefinitions 改回裸讀 → 轉紅（killed）。 |
| F-3 | tests/unit/lib/knowledge-reader.test.ts:197 | major | test-quality | fixed | 結構性 guard 只掃 readContainedFile 一個函式體：在 drift-sources 別處新增第二份讀取實作可完全穿過（實測 survived），且 toContain('catch') 因 realpath 的 catch 而空轉。已改為（一）以凍結計數把 readFileSync 在整個 drift-sources 的出現次數釘在 3、（二）斷言 read 必須位於 try 之內、（三）補 readContainedFile 自身路徑的行為測試（L1 convention 為目錄）。Mutation：新增第二份讀取實作 → 轉紅（killed，原本存活）。 |
| F-4 | src/lib/drift-sources.ts:781 | major | parallel-site | fixed | existsContained 仍是第三份 realpath containment 述詞（PB-006 早已在 ledger 點名），而落地的 REQ-MCP-006 已寫成「THE single implementation」。已抽出 isContainedPath 由兩處共用；existsContained 只保留 existsSync ＋ 該述詞——不整支委派，因為指向目錄的 markdown link 必須 exists=true 才不會冒出假 broken-link FAIL。Mutation：把述詞複製回本地 → 轉紅（killed）。 |
| F-5 | .prospec/changes/harden-contained-reads/proposal.md:26 | major | spec-architecture | fixed | F-1 把治理檔案的 unreadable 改為 loud 之後，本變更自己的 proposal US-1 AC4 與 tasks T6 仍寫著「視為缺席（graceful）」，與程式及 delta-spec 相反；兩處已改為 loud 並註明 raw 內容讀取維持 graceful。 |
| F-6 | tests/unit/lib/drift-sources.test.ts:248 | major | test-quality | fixed | 「collectMarkdownLinks／collectReqReferences 略過讀不到的檔案」原本以目錄當 fixture，但這兩處走 glob scanner 的 onlyFiles，目錄在讀取前就被濾掉——斷言空轉，Mutation：移除 readTextOrSkip 的 try/catch 後仍全綠（survived）。改用 chmod 0o000 的真實檔案（POSIX 閘控，Windows 無法建構該 fixture 並就地說明），同一 mutation 現在轉紅（killed），且額外斷言那個檔案的 broken link 從未被回報。 |
