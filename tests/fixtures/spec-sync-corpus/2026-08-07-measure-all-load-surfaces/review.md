# Review Findings: measure-all-load-surfaces

| ID | Location | Severity | Lens | Status | Summary |
|---|---|---|---|---|---|
| F-1 | src/templates/skills/_knowledge-loading-rules.hbs:7 | critical | spec-architecture | fixed | 部署進每一份 SKILL.md 的 Progressive Loading 表仍把 feature spec 與 demand conventions 併在 L2 列、宣告 `l2_per_module`，與本變更改判的 `spec_per_file`／`demand_knowledge_per_file` 相差 5 至 10 倍；已補 Spec／Demand／Skill 三列並把行數上限納入 L2 列（3 個 lens 各自獨立回報）。 |
| F-2 | src/lib/drift-sources.ts:733 | critical | correctness | fixed | `filterConventions` 漏傳 `additional_core_conventions`，使專案宣告為 Core Convention (L1) 的檔案被以 `demand_knowledge_per_file` (10000) 評分而非 `l1_per_file`（實測 `_playbook.md` 9552 tokens 零 finding）；已把 `additionalCore` 串進 `collectKnowledgeSize` 並讓 core／demand 兩半都出自同一次 split（4 個 lens 各自獨立回報，附 fixture）。 |
| F-3 | src/lib/drift-sources.ts:789 | critical | security | fixed | `collectAuthoredSkillItems` 經 `markdownFiles`→`scanDirSync` 走訪 skill references，該路徑遇 ENOTDIR／EACCES 會 throw `ScanError`；因 collector 是 `runChecks(...)` 的引數，整份 15 項 verdict 會一起消失（實測 parent commit 正常、本 commit `WHOLE RUN ABORTED`）。已改用永不 throw 的 `budgetedMarkdownFiles`。 |
| F-4 | tests/unit/lib/drift-checker.test.ts:186 | critical | test-quality | fixed | per-kind 測試以 `BASE_SIZE_BUDGET[rule.tokenKey]` 推導預期上限，fixture 隨缺陷移動，形成恆真式：把 `spec.tokenKey` 改指 `l1_per_file` 後整個 unit suite 2373 個測試仍全綠。已改為字面上限表＋逐 kind 的 tokenKey／label 字面斷言，並保留由 registry 驅動的完整性檢查。 |
| F-5 | src/lib/drift-sources.ts:722 | major | correctness | fixed | 重用 `scanDirSync` 會套用 `SENSITIVE_PATTERNS`，使檔名含 secret／credential／env／key 的 Feature Spec 被靜默排除（實測 10000 tokens、兩倍超標者零 finding）—— 預算 gate fail open。新走訪器不套用該樣式，並補上負向測試。 |
| F-6 | src/lib/drift-sources.ts:792 | major | test-quality | fixed | reference 去重只用 basename、未以 skill 命名空間，兩個不同 skill 各帶同名 reference 時較小的那份被靜默吞掉、永遠無法 WARN（未變異程式碼上直接實測）。去重 key 改為 `{skill}/{basename}`，只對部署真正產生的跨 agent path 副本去重。 |
| F-7 | src/lib/drift-sources.ts:778 | major | correctness | fixed | `!dir.isDirectory()` 會排除以 symlink 部署的 skill 目錄（readdir 對 symlink 回報 isDirectory()===false），與同檔 `moduleKnowledgeFiles` 明文的「symlink 是候選、不是 skip」政策相反且讓預算 fail open；守衛改為接受 symlink，並補 fixture。 |
| F-8 | tests/contract/skill-format.test.ts:46 | major | parallel-site | fixed | `TEMPLATE_CONTEXT` 與 sentinel ctx 手寫列舉三個預算欄位，而 agent-sync 已 spread 注入七個；Handlebars 對未知變數渲染成空字串，故樣板漏掉新預算時契約測試無法變紅 —— 這正是 F-1 沒被測出來的原因。改為由 `DEFAULT_KNOWLEDGE_TOKEN_BUDGET` 推導 sentinel，並新增「partial 必須渲染每一個預算欄位」的斷言（已變異驗證會紅）。 |
| F-9 | src/templates/knowledge/_index-auto-block.hbs:9 | major | spec-architecture | fixed | 生成的 index.md 自動區塊仍把 load-on-demand conventions 標為「(L2)」，與同檔新增的 Demand 列（10000 門檻）自相矛盾；已移除該層級標籤，讓預算表獨占分層命名。 |
| F-10 | src/templates/skills/prospec-knowledge-generate.hbs:42 | major | parallel-site | fixed | 產生下游 index.md 的 skill 指示只列舉三個預算，會使下游專案的 index.md 只宣告 7 個機器強制預算中的 3 個；已補齊七個並明寫「必須全部宣告」。 |
| F-11 | README.md:761 | major | docs-claims | fixed | 兩份根 README 的設定欄位清單仍寫 `knowledge.token_budget` 只管 L1／L2 知識檔，與同檔上方剛改寫的段落矛盾；已同步更新（雙語）。 |
| F-12 | README.md:717 | major | docs-claims | fixed | README 與兩份 config 樣板宣稱量的是「generated SKILL.md」，實作走訪的是每個已部署 skill 目錄（本 repo 手寫的 `.claude/skills/release` 即在內）；改以實作為準修正宣稱 —— 排除手寫 skill 才會讓預算 fail open。 |
| F-13 | prospec/ai-knowledge/modules/templates/skill-authoring.md:35 | major | parallel-site | fixed | 知識庫與 `agent-sync.service.ts` 的註解仍宣稱注入三個預算變數，實際已注入七個；兩處都改為「每一個 `KnowledgeSizeBudget` 欄位」並記下 partial 必須逐欄位有列的契約。 |
| F-14 | src/lib/drift-sources.ts:715 | major | security | fixed | spec 與 demand-knowledge 兩段同樣使用會 throw 的走訪，目前只因 `collectReqReferences` 會先在同一批目錄丟出相同錯誤而未成為新迴歸；一併改為非拋出走訪，不留第二、第三個全滅點。 |
| F-15 | .agents/skills/prospec-knowledge-generate/SKILL.md:1 | major | maintainability | proposed | 修正 F-1 必需的三列表格使 `prospec-knowledge-generate` 由 4923 升到 5092，越過自身 5000 門檻（已把本變更新增的散文壓到最短，仍差 92）。如實留為 WARN；收斂手段是把 phase 專屬散文抽成 on-demand reference，屬 issue #142 §E 的獨立工作。 |
| R2-1 | tests/e2e/cli.test.ts:138 | critical | correctness | fixed | round-1 把 `_index-auto-block.hbs` 的標題去掉 `(L2)`，而 e2e 仍以舊字串切片，工作樹當時已有一個紅測試 —— 我 round-1 跑的 `pnpm test` 全綠是假綠，因為改 `.hbs` 後沒有 `pnpm build`，e2e spawn 的是舊 `dist`。已同步四個站點的標題並修正切片字串。 |
| R2-2 | src/lib/drift-sources.ts:803 | critical | correctness | fixed | round-1 為修「symlink 部署的 skill 目錄」而在遞迴走訪加入 `entry.isSymbolicLink()`，但取代掉的 `scanDirSync` 是 `followSymbolicLinks: false`；實測一個 `features/loop -> ..` 讓單一 spec 膨脹成 29,524 筆項目。已改為只descend 真實目錄（symlink 的 `.md` 檔仍是候選），並補上 symlink 迴圈與 depth 守衛兩個測試（皆已變異驗證會紅）。 |
| R2-3 | src/lib/drift-sources.ts:1077 | critical | correctness | fixed | round-1 的宣稱過大：只有 skill references 那個形狀被硬化，`markdownRoots` 下的不可讀目錄仍會由 `collectReqReferences` 丟出 `ScanError` 而拖垮全部 15 個 verdict（parent commit 亦然）。已把註解、測試命名與 delta-spec 的 `**Spec:**` 收斂到真正交付的範圍，並把既有缺陷標為本 REQ 範圍外、另行追蹤。 |
| R2-4 | src/lib/index-template.ts:31 | critical | docs-claims | fixed | `knowledge/index.md.hbs` include 了 loading-rules partial，但 `buildIndexTemplateContext` 從未注入任何預算欄位，每個 CLI 產生的 index.md 都渲染成 `≤  tokens per file`（本變更把 3 個空格擴成 7 個）。已將 `tokenBudget` 設為必填選項讓編譯器抓出全部呼叫點，三個 emitter 各自以 `resolveKnowledgeTokenBudget` 解析，並新增以 sentinel 斷言 index.md 逐欄位渲染的契約測試；實跑 `dist` 的 `init` 確認七個數字皆為實值。 |
| R2-5 | src/templates/skills/prospec-knowledge-generate.hbs:148 | major | parallel-site | fixed | `(L2)` 只從 `_index-auto-block.hbs` 一處拿掉，另有 3 個平行站點仍寫舊標題（其中一個正是 R2-1 的成因）；四處已一致。 |
| R2-6 | src/lib/drift-sources.ts:762 | major | docs-claims | fixed | `conventionFileNames` 註解宣稱與 index writers 「同一組」，實際刻意不套用 `SENSITIVE_PATTERNS`（`_secret-rotation-rules.md` 這裡量、index.md 不列）；註解已改為明寫這唯一一處分歧與理由（依名稱豁免預算即 fail open），行為維持不變。 |
| R3-1 | src/lib/drift-sources.ts:713 | critical | correctness | fixed | `collectKnowledgeSize` 的 L2 段仍有未守衛的 `readdirSync(modulesDir)`（`existsSync` 對檔案也回 true），modules 是檔案或不可讀時整個 run 中止、零 verdict —— 正是我 round-2 註解宣稱已關掉的同一個形狀。已抽出 `readdirNamesOrEmpty`；實跑建置後的 CLI 對同一 fixture 由「零 verdict」變為 15 個判定。 |
| R3-2 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:94 | critical | spec-architecture | fixed | REQ-LIB-027 的 `**Spec:**`（archive 時逐字落進信任區）有四處被 round-1／round-2 的修復推翻卻未同步：4 參數簽章＋「signature unchanged」、l1 仍寫 `CORE_CONVENTIONS`、reference 去重仍寫 by basename、convention walk 誤植為走 `budgetedMarkdownFiles`。已整段重寫並補上五條新的 WHEN/THEN。 |
| R3-3 | prospec/specs/features/ai-knowledge.md:509 | critical | spec-architecture | fixed | 標題改名與 loading 表由 4 列擴成 7 列是可觀察的生成格式變更，卻沒有任何 MODIFIED REQ，三個信任區站點（REQ-KNOW-013／REQ-KNOW-035／REQ-AGNT-035）會在 archive 後留下矛盾。已補三條 MODIFIED 條目，各自帶重寫後的 `**Spec:**`。 |
| R3-4 | README.md:7 | critical | docs-claims | fixed | round-2 新增測試後未重跑 `pnpm counts`，五個檔案 19 個計數停在 3286（實際 3291），CI 的 `counts:check` 會直接紅燈。已重跑並驗證同步。 |
| R3-5 | tests/unit/lib/drift-sources.test.ts:421 | critical | test-quality | fixed | 測試名為「…and under a skill」但 fixture 沒寫 `src/templates/skills/`，authoring gate 短路使 skill 那一半完全不可達，該測試無法為它宣稱的一半而變紅。已補齊 fixture 並實際在 skill references 下放不可讀目錄；另補 modules-as-a-file 與 symlink walk root 兩個新案例。 |
| R3-6 | src/lib/drift-sources.ts:766 | major | parallel-site | fixed | `conventionFileNames` 與 index writers 存在第二處未記載的分歧：symlink 的 `_*.md` 這裡量、writers 的 `onlyFiles: true` 不列。註解已改為明列兩處分歧與其共同方向（皆為多量，因為依名稱豁免預算即 fail open）。 |
| R3-7 | src/lib/drift-sources.ts:802 | major | correctness | fixed | 「不進入 symlink 目錄」只擋 entry 不擋走訪起點 —— `readdirSync` 會解析自身最後一段，故 `references -> ..` 會把 skill 自己的 SKILL.md 以 `reference` kind（2500 而非 5000 門檻）重複量一次。已對起點加上同一條 no-follow 規則並補測試。 |
| R3-8 | src/lib/drift-sources.ts:853 | major | docs-claims | fixed | 去重註解寫成「one skill yields at most one finding」，與 symlink 別名產出兩筆項目的行為矛盾。行為維持不變（harness 依目錄名派送，兩個名稱就是兩個會被載入的 skill，合併反而低報），改為註解與 delta-spec 的 AC 明寫去重維度是 skill NAME 跨 agent path。 |
| R4-1 | src/lib/drift-sources.ts:818 | critical | correctness | fixed | round-3 加的「走訪起點是 symlink 就回傳 []」讓合法把 `specs/features` 或 `references/` 做成 symlink 的專案整批量測歸零 —— 預算在正常佈署上 fail open。已**退回**該守衛（用一個 major 換一個 critical 是錯的交換），改為明文記載被放回的病態情形有界（一層、非任何工具會產生的設定），並把測試改為釘住「symlink 樹仍被量測」。 |
| R4-2 | tests/unit/lib/drift-sources.test.ts:484 | critical | test-quality | fixed | round-2 的 symlink 迴圈測試被 round-3 的 root guard 完全遮蔽，只有兩道守衛同時移除才會紅 —— 無法因它宣稱的原因失敗。退回 root guard 後該測試重新由 entry 層守衛獨力守住。 |
| R4-3 | prospec/specs/features/drift-detection.md:266 | critical | spec-architecture | fixed | `REQ-SERVICES-065` 寫死 4 參數的 `collectKnowledgeSize(...)` 呼叫，而修復把它改成 5 參數，delta-spec 的 MODIFIED 清單卻沒有這條 —— archive 後信任區會永久與程式碼矛盾。已補 MODIFIED 條目。 |
| R4-4 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:102 | critical | docs-claims | fixed | AC 宣稱 EACCES 時仍產出 15 個 verdict，實測被推翻；且散文把中止點指名為 `collectReqReferences`（它其實已有 try/catch），真正未防護的是 `collectMarkdownLinks` 與 `collectReqDefinitions`。已把 AC 拆成成立的 ENOTDIR 半與不成立的 EACCES 半，並更正兩處散文的 collector 名稱。 |
| R4-5 | src/templates/skills/_knowledge-loading-rules.hbs:13 | critical | docs-claims | fixed | 註記改成「Every budget above comes from `token_budget`」，但同表 L0 列仍寫 `~500 tokens` —— L0 沒有這個欄位、也不受量測，且與同專案 `index.md` 的同一列相反。L0 列已改為 `Agent-injected — out of knowledge-size scope`，註記改為「below L0」。 |
| R4-6 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:97 | critical | spec-architecture | fixed | 重寫的 REQ-LIB-027 `**Spec:**` 丟掉兩條信任區現行已斷言且仍在生效的行為（`l1`／`l2` 超標 finding 與 `≤` 邊界規則、module 目錄下非 `.md`／子目錄／不安全名稱跳過不報錯）—— 丟掉的 bullet 會永久離開信任區且不報錯。兩條皆已補回。 |
| R5-1 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:96 | critical | spec-architecture | fixed | REQ-LIB-027 的 `**Spec:**` 與 AC 仍宣稱 walk 不跟隨 symlinked walk root，但該守衛已在 round 4 退回，且新測試明文釘住相反行為 —— 這段會逐字取代信任區的整個 REQ body。已改為記載「root 刻意跟隨」與退回的理由，並把 AC 拆成 entry 與 root 兩條相反的規則。 |
| R5-2 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:70 | critical | docs-claims | fixed | REQ-TYPES-061 的 `**Spec:**` 寫「declares six optional thresholds」卻隨即列出七個、段末又寫 all seven，實際就是七個 —— 這個自相矛盾的數字會逐字落進信任區。已更正為 seven。 |
| R5-3 | prospec/ai-knowledge/_glossary.md:10 | critical | docs-claims | fixed | round-4 的 L0 掃除漏了 `_glossary.md`：它仍寫 L0 `~500 tokens`，且仍把 Demand Conventions 與 Feature Specs 併在 L2 共用 ≤1,000/模組 —— 而它是 agent 啟動必讀的 Core Convention，等於信任區同時宣告兩張互斥的載入表。已改寫為與 `prospec/index.md` 一致的六個載入面並指向權威表格。 |
| R5-4 | src/services/check.service.ts:199 | major | test-quality | fixed | round-4 新增的第五個參數在 service 邊界毫無測試守護 —— 換成常數 `[]` 後全套 3,287 個測試依舊全綠。已補 `check.service` 層的回歸測試（以 8,000 token 的 promoted convention 讓 l1 與 demand 兩種判定相反），並變異驗證會紅。 |
| R5-5 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:70 | major | spec-architecture | fixed | 兩則 MODIFIED 的新 `**Spec:**` 又各刪掉一條仍成立且仍有測試在守的既有信任區行為（retired `l0_max`／`l1_per_module` 不再繫結、module 目錄只有 README 時輸出恰為一項）。因 `**Spec:**` 整段取代 body，這兩條會靜默永久消失；皆已補回，並補回「module 名由路徑推導、不需 module-map」一句。 |
| R6-1 | prospec/ai-knowledge/_glossary.md:9 | critical | docs-claims | fixed | R5-3 只改寫 `_glossary.md` 第 10 列，緊鄰的 **Knowledge Engine** 列仍宣告分層為「L0/L1/L2」—— 同一份 Core Convention (L1)、同一張表裡兩個互斥的分層定義。已改為指向同表下一列的權威列舉；`_glossary.md` 578 tokens 仍遠低於本 repo 的 2,500 門檻。 |
| R6-2 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:95 | critical | spec-architecture | fixed | 重寫的 REQ-LIB-027 `**Spec:**` 把 module 那一臂的「symlink entry 仍是 CANDIDATE、containment 交給 canonical reader 的 realpath 檢查」與「列舉已排序故 item 順序跨機器一致」整句刪掉 —— 而該句正是 F-7 修復所援引的政策錨點，且有三個測試在守。已補回完整句。 |
| R6-3 | README.md:761 | major | docs-claims | fixed | F-11 重寫的 `knowledge.token_budget` 欄位說明寫「generated skills and their references」，與同檔 44 行上方 F-12 才剛改成「every deployed SKILL.md ... hand-authored included」矛盾 —— 等於在同一份檔案裡把剛移除的錯誤宣稱又寫回去（實測 `.agents/skills/release/SKILL.md` 這個手寫 skill 確實在量測集合內）。兩份 README 皆已更正。 |
| R6-4 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:67 | major | docs-claims | fixed | 「改動前被量測的那一層合計 13,186 tokens」低報 37%，實測為 20,796（index.md 1,931 ＋ core conventions 5,333 ＋ 9 個 module 知識檔 13,532）—— 而這正是「被量的是最小一塊」整篇論證的基準數字。已在 proposal 與 delta-spec 兩處更正並附拆解；我以 `git show 3d07d6c:` ＋ 同一個估算器獨立覆核。 |
| R6-5 | .prospec/changes/measure-all-load-surfaces/proposal.md:65 | major | docs-claims | fixed | SC-005 依原措辭對任何變更都不可滿足（`knowledge-health` 必因「知識同步押在 archive」轉 WARN、`test-provenance` 必因「record-tests 押在 verify」為 FAIL），而 T26 [V] 卻已勾選宣稱它成立 —— 一個已勾選的假驗證。SC-005 與 T26 皆已改為明列這兩個流程必然差異。 |
| V2-1 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:180 | critical | spec-architecture | fixed | verify 2/5 獨立評分實測發現 REQ-TEMPLATES-149 的 bullet「初始化後的 `.prospec.yaml` 會顯示每個預算欄位」為假：`init/prospec.yaml.hbs` 沒有任何 production 消費者（`prospec init` 走 `writeConfig` 直接序列化，從不 render），實跑產出的 config 連 `token_budget` 區塊都沒有 —— 此 REQ 自 #64 起即不成立，六輪 review 全數漏掉。已把 REQ 收斂到真正出貨的 `prospec config example`，並明載死樣板現況。 |
| V2-2 | tests/unit/lib/config.test.ts:172 | major | test-quality | fixed | REQ-LIB-028 宣稱 resolver 的欄位清單對 schema 的鍵有斷言，實際只對 `DEFAULT_KNOWLEDGE_TOKEN_BUDGET` 斷言 —— schema 加了欄位而 default 沒加時，zod 會接受該覆寫、resolver 靜默忽略、無任何測試變紅。已補 schema↔default 的 key-set 相等斷言，並雙向變異驗證（兩個方向皆紅）。 |
| V2-3 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:97 | major | docs-claims | fixed | REQ-TESTS-048 與 proposal 仍寫「三份 load-on-demand 治理知識檔」—— 那是已放棄的硬編碼登記表設計的殘留，出貨的 collector 是推導的，且 fixture 刻意只用兩份。三處措辭皆已改為描述推導出的集合。 |
| R7-2 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:183 | critical | spec-architecture | fixed | 改寫 REQ-TEMPLATES-149 時把信任區現行 body 唯一的斷言（`init/prospec.yaml.hbs` 的種子欄位與 `DEFAULT_KNOWLEDGE_TOKEN_BUDGET` 一致）降級為括號附註，沒有任何 bullet 涵蓋它 —— `**Spec:**` 整段取代 body，這條會永久消失。已補回為明確 bullet 並註明目前無機械守衛。 |
| R7-3 | src/templates/references/config-example.yaml.hbs:75 | major | docs-claims | fixed | 新 bullet 宣稱「每個預算欄位都附其治理載入面的說明」，但 `reference_per_file` 與 `skill_per_file` 共用一則註解。選擇改樣板讓宣稱為真（兩份種子各補一行），而非放寬措辭。 |
| R7-4 | .prospec/changes/measure-all-load-surfaces/delta-spec.md:96 | major | docs-claims | fixed | REQ-LIB-027 寫「each deployed `references/*.md`」，但走訪是遞迴的、實際涵蓋 `references/**/*.md`，與同段稍後描述遞迴的句子自相矛盾。已更正。 |
| R7-1 | tests/unit/lib/config.test.ts:172 | minor | test-quality | rejected | 審查者宣稱新的 key-set 測試只擋單向（schema 多欄位），反方向（default 多欄位）不會紅。**實測推翻**：我把 `phantom_per_file` 只加進 `DEFAULT_KNOWLEDGE_TOKEN_BUDGET` 與 `KnowledgeSizeBudget` interface（grep 確認兩處皆落地），該測試即紅（1 failed / 39 passed）。集合相等本就雙向，該 agent 的反向變異應未真正落到檔案上。 |
