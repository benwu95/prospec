# Review Findings: restrict-identity-fallback

| ID | Location | Severity | Lens | Status | Summary |
|---|---|---|---|---|---|
| F-1 | src/lib/review-merge.ts:124 | critical | correctness & edge cases | fixed | 退回索引每鍵只存一列，重播同一輪的兩個無 id finding 會逐次長列（2→3→4）並讓第二列被第一列的 summary 覆蓋，牴觸 REQ-CLI-028 自身保留的 byte-identical bullet；改為每鍵一個依表序排隊的 pre-round 列、認領即出列後 idempotent，mutation MD（認領時整鍵驅逐）轉紅釘住 |
| F-2 | src/lib/review-merge.ts:126 | critical | security & data integrity | fixed | `target.location = finding.location` 不重建退回鍵，同輪稍後的無 id finding 會以舊 location 認領那列，把已被 id 命中搬走的列拉回並覆蓋 summary（實測：F-1 由 a.ts:9 被拉回 a.ts:1，兩個發現塌成一列）；改為任何被認領的列一律離開退回索引，mutation ME（僅 target===candidate 時出列）轉紅釘住 |
| F-3 | tests/unit/lib/review-merge.test.ts:68 | major | test-quality | fixed | 唯一的 idempotence 測試只餵帶 id 的 finding，無 id 路徑的重播沒有守衛，F-1 的長列回歸能在 2998 全綠下出貨；補上「無 id 重複輪重播 byte-identical」與「列被搬走後舊鍵不可再認領」兩個測試，mutation MD/ME 各自轉紅 |
| F-4 | src/templates/skills/prospec-review.hbs:70 | major | parallel-site completeness (PB-007) | fixed | PB-007 掃描漏掉 skill 本體：Persistence 段仍寫「a finding without an id keys by location+lens」這條前修正的無條件規則，而 reviewer 讀 SKILL.md 早於 on-demand 的 review-format reference；已改述三條路徑並重新 bundle + agent sync |
| F-5 | prospec/ai-knowledge/modules/lib/README.md:56 | major | docs-claims (PB-003) | partially-fixed | README 原本正好卡在 1800 token 上限，新不變式的空間來自刪既有敘述（PB-011 禁止以稀釋密度換空間）；已還原 line 21 的 `identity-keyed` 並改以刪除 L1 已載明的 `I/O-free station engines —` 前綴讓位。`markdown-table.ts` 的 SINGLE-source 主張刻意維持壓縮：該不變式仍由 Key Files 的「THE pipe-table engine」與 Modification Guide #6「reuse it, own only the header predicate」兩處陳述，`I/O-free` 則由 index.md L1 陳述 —— 屬 PB-011 允許的 restatement 去重，非密度損失 |
| F-6 | src/types/station.ts:23 | minor | parallel-site completeness (PB-007) | fixed | ReviewFindingSchema 的 JSDoc 仍是無條件的「A finding without an `id` is keyed by (location, lens)」；補上未知 id 開新列與 pre-round 範圍 |
| F-7 | tests/contract/skill-format.test.ts:2115 | minor | test-quality | wontfix | 負向斷言只擋字面 /dedup\w*\s+by\s+Location/i，其他等義改寫（如 collapse rows sharing a Location）可繞過；散文的「必不出現」規則無法以封閉集合斷言，維持字面守衛並在註解揭露此界線，不假裝涵蓋 |
| F-8 | tests/contract/skill-format.test.ts:2079 | minor | docs-claims (PB-003) | fixed | 「Each assertion was mutation-verified」未列出施加的 mutation，違反 review-format 的 naming 規則且對字面子字串斷言近乎恆真；改為列名 H0-H5、註明每次都重新 bundle（H1 首次即因 bundle 過期而紅），並明說它證明的是非空泛、不是等義改寫也會被抓 |
| F-9 | src/lib/review-merge.ts:135 | critical | correctness & edge cases | fixed | 第一輪修復把缺陷換了形狀而非消滅：claim() 只把列移出退回索引，該列仍可經 byId 被同輪稍後的 id 命中，於是「先無 id、後 id 點名同一列」的一輪會把兩個發現塌成一列（實測 merge once=1 列，id-less critical 消失只留 severity 汙染一列 status=fixed 的列），merge twice=2 列即非 idempotent；改為在任何 location 比對前先保留本輪以 id 點名的所有列（明示身分勝過推斷身分、與陣列順序無關），mutation MG（移除保留 pass）轉紅釘住 |
| F-10 | .prospec/changes/restrict-identity-fallback/delta-spec.md:21 | major | spec-architecture | fixed | REQ-CLI-028 的 After 敘事與 proposal FR-003 仍寫「每個 (location, lens) 鍵單輪至多被認領一次」，但改為佇列後同鍵三列可被認領三次，與同一 REQ 的 Spec bullet「a pre-round row is claimable once」互斥，會讓 verify 的 spec-compliance 面對兩條互相排斥的規則；兩處改為 per-row 措辭並補上保留 pass 的規則 |
| F-11 | .prospec/changes/restrict-identity-fallback/proposal.md:43 | minor | docs-claims (PB-003) | fixed | Edge Case 宣告「既有表兩列共用同鍵：只保留最後一列（既有行為，不在本次範圍）」，但佇列化實際上把它改成依表序取第一列 —— 自己畫的界線被自己跨過卻無工件揭露；改為明文記載此行為變更與其對既有含重複鍵 review.md 的影響 |
| F-12 | src/types/station.ts:23 | minor | parallel-site completeness (PB-007) | fixed | 第一輪修 F-6 時的改寫寫成無條件的「an id no row carries opens a new row」，反而抹掉 legacy 收養例外 —— schema 讀者拿到的那一句對「pre-ids 無 id 列」正好是錯的；補回例外與 pre-round 扣除保留列的範圍 |
| F-13 | README.md:7 | major | docs-claims (PB-003) | fixed | 第二輪加測試後未重跑 `pnpm counts`，README/README.zh-TW/index.md/module-map.yaml/tests README 五處測試計數落後實際（`pnpm counts:check` exit 1、14 處 stale），而 prospec check 不查計數正確性（PB-004）；重跑 counts 後 `counts:check` 回報 in sync |
| F-14 | .prospec/changes/restrict-identity-fallback/delta-spec.md:33 | minor | spec-architecture | fixed | REQ-CLI-028 兩條 bullet 絕對化地寫「id-less finding opens its own row」，但同鍵若還有未被認領的 pre-round 列（保留 pass 讓出隊首後），它其實會接手下一列 —— 與同一 REQ「first unclaimed one in table order」互斥，正是 F-10 的同型自我矛盾；改為「取下一個未認領的 pre-round 列，沒有才開新列」 |
| F-15 | tests/contract/skill-format.test.ts:2118 | minor | test-quality | fixed | F-4 修的是 reviewer 最先讀到的 skill 本體，但新契約測試只對 review-format.hbs 做正向斷言、對 prospec-review.hbs 僅有負向 regex —— 把前修正句子原樣放回 Persistence 段，全套仍綠；補上 section-scoped 正向＋負向斷言，並以還原原句的 mutation 實測轉紅 |
| F-16 | prospec/ai-knowledge/modules/lib/README.md:23 | minor | docs-claims (PB-003) | fixed | 為挪出預算空間而把「其餘 18 個 .ts」改成 19 是錯的：src/lib 仍是 35 檔、Key Files 表列名 17 檔（含 template.ts 列點名的 bundled-templates.ts），35-17=18；19 會把生成檔重複計入 single-purpose helpers，且該數字與本變更無關（不該動）；已還原為 18 |
