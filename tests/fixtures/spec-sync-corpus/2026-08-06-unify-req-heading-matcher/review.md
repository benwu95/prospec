# Review Findings: unify-req-heading-matcher

| ID | Location | Severity | Lens | Status | Summary |
|---|---|---|---|---|---|
| F-1 | src/cli/formatters/archive-output.ts:127 | critical | spec-architecture | fixed | 新增的 refusal 走 stdout 且被 `--quiet` 整段吞掉（測試還把「quiet 下沉默」釘住），把「靜默寫錯值」換成「靜默不寫」，與 REQ-CLI-024 的 worklist 慣例相違。已改為 stderr、`--quiet` 下仍可見、不動 exit code；mutation：把它改回 stdout+quiet guard → 2 條測試轉紅。 |
| F-2 | tests/contract/spec-heading-single-source.test.ts:30 | critical | test-quality | fixed | single-source contract 的 detector 抓不到本次真正刪掉的兩種形狀（`/^####\s+REQ-/` 這條 regex、以及 heading 字串先存變數再 `includes`），三位 reviewer 獨立實測皆全綠；且 HEADING_STRING_PROBE 無 positive control。已改寫為對照「被刪除形狀」的 detector（h4 regex 禁令＋heading 字面值 per-file 預算＋re-typed id class）並先證明每個 detector 會命中；mutation M-a/M-b/M-c 三種形狀放回皆轉紅。 |
| F-3 | tests/unit/services/archive-finalize.service.test.ts:228 | critical | test-quality | fixed | 歸零拒絕的 story_count 半邊未被釘住：把判準欄位表縮成 `['req_count']` 單欄仍全綠（fixture 兩計數同時歸零）。已加「只有 story_count 歸零」的 fixture 並斷言 reason 指名該欄位；mutation M-e 轉紅。 |
| F-4 | src/services/check.service.ts:207 | critical | test-quality | fixed | `collectSpecCounters` 的接線（REQ-SERVICES-077）無任何測試：把 features 目錄改成不存在的路徑，全套 143 檔／3162 條測試全綠，新 check 會在每個真實專案永久 skip。已加正向 warn 案例＋`.prospec.yaml` 路徑覆寫案例；mutation M-d 轉紅。 |
| F-5 | src/lib/spec-headings.ts:101 | critical | correctness | fixed | `readSpecCounters` 的 h2 分支在計數前 `continue`，h2 層級的 REQ heading 永遠算不到，與「任何層級都計入」的契約相違；實測一份全 h2 REQ 的 spec 得 `req_count: 0` 並因新拒絕永久無法校正。已把 REQ heading 判斷移到 h2 分支之前；mutation M-j 轉紅（5 條）。 |
| F-6 | src/services/archive.service.ts:1153 | critical | correctness | fixed | 邊界一般化成「層級 ≤ 找到層級」會把層級更深的兄弟 REQ 整段當 body 吞掉並從信任區刪除（而 ADDED 一律插 h4，正好製造這種版面），h1 REQ 更會吃掉 `## Edge Cases` 與整張 Change History；相對舊 `/^#{2,4}\s/` 是退步。邊界改為「任何 REQ heading、任何層級 ≤ max(level,2) 的 heading、或 `---`」；mutation M-f/M-g/M-h 三條各自轉紅。 |
| F-7 | src/services/archive.service.ts:1558 | critical | correctness | fixed | `createNewFeatureSpec` 宣稱 `story_count>0` 而 body 沒有任何 US heading，與新增的歸零拒絕互斥：工具自建的 spec 一出生就永久無法 reconcile 且永久 WARN。已改為把 REQ 分組在 `**Story:**` 標題下並由渲染後 body 推導計數（新增 REQ-SERVICES-078）；mutation M-n 轉紅。 |
| F-8 | src/lib/drift-sources.ts:348 | critical | correctness | fixed | 所有 spec 都解析不到時 `collectSpecCounters` 回 `available:true, specs:[]` 讓 check 空過（CRLF 檢出的專案即為此形），與「skips — never a vacuous pass」的自陳相違。已加「零樣本即 unavailable」守門，並讓 `readSpecCounters` 容忍 CRLF；mutation M-k/M-l 各自轉紅。 |
| F-9 | src/types/drift-report.ts:101 | major | docs-claims | fixed | 「WARN 級因為下一次 archive 會校正」在歸零拒絕情境下為假（那正是 finalize 唯一拒寫的情況），同一句被複製到 5 處。五處全部改為承認該例外：多數情況會校正，但被拒絕時 warn 會留到人工收斂。 |
| F-10 | src/services/archive.service.ts:1410 | major | docs-claims | fixed | `executeFinalize` 的 doc comment 仍把計數規則寫成 `### US-` 與 `#### REQ-`（本變更廢除的窄判準），且未提歸零拒絕。已改述為經 `readSpecCounters` 的層級無關推導＋拒絕行為。 |
| F-11 | src/templates/skills/prospec-archive.hbs:139 | major | docs-claims | fixed | archive skill 模板宣稱 finalize「reconciles every feature spec」且把任何 refusal 都解讀為「summary 未覆寫」，新增第二種 refusal 後會把 agent 導向錯誤修法。模板改為區分兩種 refusal，並把 REQ-TEMPLATES-159 列入 MODIFIED（PB-017）。 |
| F-12 | prospec/specs/features/sdd-workflow.md:272 | major | spec-architecture | fixed | 既有 REQ-TESTS-057 明文寫「skipped-never-PASS across 13 checks」，而本變更就是改那條斷言，卻只列了 REQ-TESTS-045（PB-017）。已把 REQ-TESTS-057 列入 MODIFIED，並把斷言改為由 `DRIFT_CHECK_IDS.length` 導出而非寫死數字。 |
| F-13 | src/services/archive.service.ts:1137 | major | parallel-site | fixed | 替換迴圈沒有 break，對「已被舊缺陷寫壞、同一 id 兩段」的 spec（下游現況）會把落地 body 複製兩份並把第二段層級改寫，且無任何回報。已改為只合併第一段、其餘原樣保留並回報 `pendingConvergence`；mutation M-i 轉紅。 |
| F-14 | tests/unit/types/drift-report.test.ts:184 | major | docs-claims | fixed | 測試名稱仍寫「exactly the fourteen frozen check ids」而同一個 `it` 已斷言 15 —— 在本變更親手改的那幾行旁邊留下新的過期數字。已改為 fifteen。 |
| F-15 | src/lib/spec-headings.ts:50 | major | correctness | fixed | 分隔符從三個唯讀判準的 `\s+` 收窄成 `[ \t]+`，用全形／不斷行空格分隔的 REQ heading 從此不算定義，會讓 FAIL 級的 `req-references` 在下游升級後由綠翻紅，且未記錄。已還原為 `\s+` 並把理由寫進註解與 REQ-LIB-041。 |
| F-16 | tests/unit/services/archive-spec-body.service.test.ts:721 | major | test-quality | fixed | 六條 boundary 測試（含既有 h4 三條）全用空 body 的 MODIFIED route，`skipping` 永遠 false → 邊界程式碼一行都沒執行；刪掉 `--- ` 邊界仍全綠。已全部改用帶 `**Spec:**` 的 fixture，並把「`---` 條數」由原本會被 frontmatter fence 滿足的 `toMatch(/^---$/m)` 改成精確計數。 |
| F-17 | tests/unit/cli/archive-output.test.ts:321 | major | test-quality | fixed | 沒有斷言釘住「有拒絕時必須抑制 already consistent」這個新加的 `else if` 守衛，退回 `else` 仍全綠 —— operator 會同時看到兩個互相矛盾的宣稱。已加雙向斷言（有拒絕時不出現、無拒絕時仍出現）。 |
| F-18 | src/services/archive.service.ts:1184 | critical | correctness | fixed | F-13 的修復自己開的洞：duplicate-id 分支提早 return 只回 `pending`，於是第一段 body 被 `**Spec:**` 取代時被摧毀的 authored bullets 完全不進 `droppedBehavior`（實測那條 bullet 從檔案消失、兩個 worklist 都不提），違反本變更 REQ-SERVICES-072 與既有 REQ-SERVICES-073。已改為兩份回報並行；mutation R2-a 轉紅。 |
| F-19 | src/lib/spec-headings.ts:101 | critical | correctness | fixed | F-8 的 CRLF 容忍讓 CRLF spec 第一次變成可被改寫，而 writer 的重組模板寫死 `\n`、counter 行的 `\s*$` 又吃掉 `\r` —— 實測 12 個 CRLF 變 9 個 CRLF ＋ 3 個裸 LF。已讓 `readSpecCounters` 回傳 `eol` 並由 `setCounter` 保留行尾；mutation R2-b 轉紅。 |
| F-20 | src/lib/spec-headings.ts:112 | critical | correctness | fixed | F-5 把 REQ 判斷移到 h2 分支之前，導致 h1/h2 的 REQ heading 不再重設 `inDeprecated`，它之後的 active REQ 全被當 Deprecated 漏數（實測正確 3 被算成 1，且非零故躲過歸零拒絕）。已改為「層級 ≤ 2 的 heading 一律關閉 Deprecated 區段」；mutation R2-c 轉紅。 |
| F-21 | src/services/archive.service.ts:1336 | critical | correctness | fixed | F-7 的 story 分組把 storyless REQ 接在群組之後，於是它落在最後一個 story 標題底下成為假歸屬（實測 REQ-DEMO-002 落在 `### US-1` 下），違反 REQ-SERVICES-078。已改為 storyless 先行、直接掛在區段標題下；mutation R2-d 轉紅。 |
| F-22 | src/services/archive.service.ts:1333 | major | security | fixed | F-7 新開的落地面：`**Story:**` 原文未經檢查就渲染成 `### {story}`，帶 REQ id 的標籤會在信任區憑空造出一個 REQ 定義且推導計數與它自洽（實測 `### REQ-DEMO-999` 落地、req_count 含 phantom），此後 `collectReqDefinitions` 會把它當已定義。已加 `storyHeadingText` 中性化為 code span；mutation R2-e 轉紅。 |
| F-23 | .prospec/changes/unify-req-heading-matcher/delta-spec.md:172 | major | docs-claims | fixed | REQ-SERVICES-072 的同一個 `**Spec:**` 區塊裡留下兩段互斥的邊界敘述（舊句「for an h4 REQ this is exactly the previous behavior」在加入 h1/h2 下限後為偽），而該區塊會原文畢業進信任區。已刪除過期那句。 |
| F-24 | prospec/specs/features/drift-detection.md:40 | major | spec-architecture | fixed | 既有 REQ-LIB-014 body 內「thirteen other verdicts」與 REQ-TYPES-052 同源，本變更只回填指名那處、未 grep 這個 invariant 的其他命中點（PB-017／PB-007）。已改為 fourteen 並把 REQ-LIB-014 列入 MODIFIED。 |
| F-25 | prospec/ai-knowledge/modules/services/README.md:46 | major | docs-claims | fixed | 為了讓新增的 invariant 塞進 token 預算，順手刪掉兩條與本變更無關、前人刻意記下的 pitfall（`never re-cast doc.toJS()`、raw-scan 的 `toInlineCodeSpan` 欄位列舉），屬 Surgical Changes 之外的知識淨損失。已還原兩條，改從自己的新增文字與 L2 重複處挪出空間，回到 1799/1800 token。 |
| F-26 | src/lib/spec-headings.ts:124 | critical | correctness | fixed | F-19 只讓 frontmatter 容忍 CRLF，body 掃描仍 `split('\n')`：`\r` 是 JS regex 的 line terminator，`.` 不吃它，故 `$` 錨定的 `/^##\s+(.+)$/` 在 CRLF 每一行都不命中 —— 實測本 repo 10 份真實 spec 有 5 份 story 計數不同（sdd-workflow 32→0、drift-detection 14→4），且錯值非零故躲過歸零拒絕、會被靜默寫入信任區。已改為 `split(/\r?\n/)`；mutation R3-a 轉紅（3 條）。 |
| F-27 | README.md:7 | critical | docs-claims | fixed | round-2 新增測試後未重跑 `pnpm counts`，5 個檔案共 14 處測試計數停在舊值，CI 的 `pnpm run counts:check` 當時為紅。已重跑 `pnpm counts` 並確認 `counts:check` 綠。 |
| F-28 | tests/unit/lib/spec-headings.test.ts:130 | major | test-quality | fixed | 唯一的 CRLF fixture 只用 `### US-1` —— 正好是 CRLF 下唯一還能命中的 story 形狀（`/^###\s+US-/` 無 `$` 錨點），所以那條斷言是假綠，F-26 就是靠它出貨的。已把每條 CRLF 斷言改為 LF／CRLF 雙跑（含 `## US-` 與 Deprecated 兩種形狀），並加一條以本 repo 真實 spec 比對兩種行尾的端到端測試。 |
| F-29 | prospec/ai-knowledge/modules/types/README.md:14 | major | docs-claims | fixed | `DRIFT_CHECK_IDS` 加到 15 後，擁有該登記表的 types 模組知識檔仍寫「14 frozen」兩處 —— 我更新了 lib／drift-engine／module-map／index／tests 的同源數字，唯獨漏掉登記表自己所在的模組，且 `pnpm counts` 不追這兩處。已更新為 15。 |
| F-30 | prospec/ai-knowledge/modules/cli/README.md:13 | major | parallel-site | fixed | 把 refusal 移到 stderr 等於新增第三條 WARNING-class worklist，但 cli 模組知識檔仍寫「TWO WARNING-class worklists」—— cli 是本輪唯一改了程式卻沒同步知識的模組（PB-005）。已改為 THREE 並指名 `refusedReconciliations`。 |
| F-31 | src/services/archive.service.ts:1196 | major | correctness | fixed | bodyless MODIFIED 又遇同 id 重複時，`duplicatePending ?? pendingFor(route, NO_SPEC_BLOCK_REASON)` 只回報重複、吞掉「body 被保留、需人工收斂」的理由 —— 兩行之上才寫下的「兩份回報都是獨立事實」原則沒套用到這個分支。已合併兩個理由；mutation R3-b 轉紅。 |
| F-32 | src/lib/markdown-fences.ts:62 | major | parallel-site | proposed | 與 F-26 同型但在隔壁未修的平行站點，且 main 上即存在（非本變更造成）：`withoutFencedBlocks` 的 fence 判準以 `$` 錨定卻吃 `split('\n')` 的行，`\r` 是 JS regex 的 line terminator，故 CRLF checkout 下 fence 完全偵測不到 —— 同一份內容 LF 下 `file-paths` PASS、CRLF 下 FAIL（fenced 範例裡的示意連結被當真連結，FAIL-class 會讓 `check --strict` exit 1），`artifact-language` 方向則 fail-open。兩向 mutation 皆全綠，零覆蓋。已開 issue #140，範圍是 markdown-fences ＋ 5 個 collector 呼叫端與其 LF／CRLF 差分測試，不併入本變更。 |
