# Review Findings: enforce-sub-module-budget

| ID | Location | Severity | Lens | Status | Summary |
|---|---|---|---|---|---|
| F-1 | src/lib/drift-sources.ts:865 | critical | security | fixed | moduleKnowledgeFiles 以 e.isFile() 過濾，使 symlink README 整檔退出量測，budget gate fail open 且違反 REQ-LIB-027「README-only 目錄零差異」；改以 !e.isDirectory() 收候選、containment 交還 canonical reader 的 realpath 檢查。Mutation：filter 改回 e.isFile() → symlink 回歸測試轉紅（killed）。 |
| F-2 | src/templates/skills/references/drift-report-format.hbs:91 | critical | parallel-site | fixed | 出貨的報告形狀正典漏列 last_sub_module_commit，使 REQ-TYPES-073 的可重現性在 agent 實際查的文件上不成立；已補鍵與說明並重新 bundle/agent sync，另加由 KnowledgeHealthModuleSchema.shape 推導的契約 guard。Mutation：自 jsonc 區塊刪鍵（保留說明文字）→ 強化後的 guard 轉紅（killed）。 |
| F-3 | src/lib/drift-checker.ts:169 | critical | correctness | fixed | 模組有 sub-module 但無 README 時報 stale:true，而報告欄位重算為 false，與 REQ-TYPES-073 的可重現性承諾矛盾；已把承諾收斂為「documented 模組可重現，無 README 由 coverage 規則裁決」並補測試釘住。Mutation：`: true` 改為無條件 isStale → 新測試轉紅（killed）。 |
| F-4 | prospec/specs/features/drift-detection.md:50 | critical | spec-architecture | worklist | US-2 acceptance scenario 仍寫「src 晚於 last README commit → stale」，與改後的 REQ-LIB-015 矛盾；`**Spec:**` 只能替換 REQ body，無載具可及 US 層，已登錄 delta-spec 的「Phase 3.5 手動收斂清單」逐行指定目標與應收斂內容。 |
| F-5 | prospec/specs/features/drift-detection.md:207 | major | spec-architecture | worklist | US-8 仍以「each module README」描述 L2 量測範圍，改後應為模組目錄下每個 .md；同屬 US 層，已列入 Phase 3.5 收斂清單。 |
| F-6 | prospec/specs/features/ai-knowledge.md:267 | major | spec-architecture | worklist | US-330 acceptance scenario 仍寫 ≤400 token，REQ-KNOW-016 的 Spec 只改 REQ body，archive 後同檔會同時宣告 ≤400 與 ≤1,000；已列入 Phase 3.5 收斂清單。 |
| F-7 | prospec/ai-knowledge/modules/templates/README.md:7 | major | spec-architecture | fixed | `## Sub-Modules` 被放在 `## Key Files` 之前，違反本變更自己編輯的正典所宣告的固定節次序（Sub-Modules 應在最末）；全 repo 首次抽取會成為錯誤先例，已移至末尾。 |
| F-8 | src/lib/drift-checker.ts:677 | major | test-quality | fixed | newerOf 只測到 sub-module 較新與 null 兩條路徑，README 較新的分支無測試；Mutation：`return b;`（放棄比較）原本全綠存活，補測試後該 mutation 轉紅（killed）。 |
| F-9 | src/lib/drift-sources.ts:869 | major | test-quality | fixed | 新引入的 .sort() 決定論無斷言；已補順序測試並在測試名稱與註解據實說明其涵蓋範圍——它抓得到主動重排（Mutation：`.sort()` → `.reverse()` 轉紅、killed），抓不到在 APFS/NTFS 上單純移除 .sort()。 |
| F-10 | prospec/ai-knowledge/modules/templates/README.md:47 | major | docs-claims | fixed | 拆分時把「Contract tests flag restatement.」誤掛到 dual-copy 條目，但 conventions 雙份副本並無任何機器比對，且該句語意與「edit both」相反；已改為據實陳述（僅 status-lifecycle 的 gate 節有比對）。 |
| F-11 | prospec/ai-knowledge/modules/templates/skill-authoring.md:19 | major | docs-claims | fixed | 新 sub-module 宣告 `Depends on: types/skill.ts, types/version.ts`，與 README 的「none」及 module-map.yaml 的 depends_on: [] 矛盾，等於憑空造出依賴邊；已改寫為「.hbs 無 import，值由 agent-sync 注入，真正的邊是 services/agent-sync → {templates, types}」。 |
| F-12 | .prospec/changes/enforce-sub-module-budget/delta-spec.md:27 | major | docs-claims | fixed | REQ-TYPES-073 的 `**Spec:**`（會逐字落地信任區）寫「ISO 8601 string or null」，但 schema 為 z.string().optional() 明確拒絕 null，與相鄰的「absent, never fabricated」自相矛盾；已改為「present 時為 ISO 字串，否則整個鍵缺席」。 |
| F-13 | tests/contract/skill-format.test.ts:321 | major | test-quality | fixed | round 1 新增的 knowledge_health 鍵集合 guard 少一層 indirection：只從 jsonc 刪鍵、保留說明文字仍會通過（正是它要抓的缺陷形狀）；已改為對 section 內的 ```jsonc fenced block 比對，並修掉 `-1` 邊界會靜默擴張切片的問題。Mutation：僅刪 jsonc 內的鍵 → 轉紅（killed）。 |
| F-14 | src/lib/knowledge-reader.ts:344 | major | correctness | proposed | 既存缺陷（HEAD 即存在，非本變更引入）：README 若是指向目錄的 symlink，readTextIfExists 的 readFileSync 未包 try/catch，EISDIR 會往上拋並中止整個 check run——正是 readContainedFile 註解所說要避免的失效模式；建議另開變更修，本變更不擴大範圍。 |
| F-15 | README.md:665 | major | docs-claims | fixed | 根 README（英／繁中）與 src/types/config.ts 的預算註解仍把 l2 預算描述為「per module README」，在量測範圍擴及 sub-module 後成為低報；已三處同步為 per module file，並修掉同句對 L1 的過度宣稱（L1 只有 token 預算、無行數預算）。由獨立 docs-claims 查核確認 claim ⊆ implementation、雙語語意一致、無其他遺漏站點。 |
| F-16 | tests/unit/lib/drift-sources.test.ts:165 | major | test-quality | fixed | 「略過 subdirectory」的 fixture 取名 diagrams（不帶 .md），因此 .md 過濾早已排除它，`!e.isDirectory()` 這道 guard 從未被鑑別——Mutation：刪除 `!e.isDirectory() &&` 後 230 個測試全綠（survived）。改用「名為 README.md 的目錄」作為鑑別性 fixture（該路徑會走到未包 try/catch 的 readTextIfExists 而拋 EISDIR 中止整個 check run）後，同一 mutation 轉紅（killed）。 |
| F-17 | .prospec/changes/enforce-sub-module-budget/delta-spec.md:118 | major | spec-architecture | fixed | Phase 3.5 手動收斂清單四行皆逐字屬實，但漏了 drift-detection.md:46（US-2 的 "I want" 敘述，畢業後會與其下的 REQ-LIB-015 body 矛盾）與 ai-knowledge.md:533（SC-5，非錯誤但已不完整）；兩列已補入清單。 |
| F-18 | prospec/ai-knowledge/modules/tests/README.md:3 | major | docs-claims | fixed | 本變更把自己新增的 14 個 unit test 記成 13，導致 `pnpm counts:check` 失敗、14 個事實計數在 5 個檔案中失同步（tests.total 2918→2919、tests.unit 2075→2076），違反提案 SC-005；已重跑 `pnpm counts` 重導並確認 in sync。 |
| F-19 | src/types/drift-report.ts:134 | major | test-quality | fixed | REQ-TYPES-073 的「rejects an explicit null」在程式上為真但無測試把關——Mutation：schema 改為 `z.string().nullable().optional()` 後 735 個測試全綠（survived）。已在 drift-report 契約測試補上 null-filled 條目必須 parse 失敗的斷言，同一 mutation 轉紅（killed）。 |
