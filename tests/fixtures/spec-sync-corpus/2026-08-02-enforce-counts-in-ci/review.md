# Review Findings: enforce-counts-in-ci

| ID | Location | Severity | Lens | Status | Summary |
|---|---|---|---|---|---|
| G-1 | tests/contract/ci-workflow.test.ts:40 | critical | correctness | fixed | 封閉集合並不封閉：抽取器只認 `pnpm run <script>`，以 `pnpm exec`／`npx` 新增的閘門完全隱形（審查實測插入 `pnpm exec depcheck` 三條斷言全綠），而 ci.yml 的 windows-smoke 早就用這個拼法 —— 這正是本變更要防的靜默逃逸，且會讓 delta-spec「新增閘門必須更新 baseline 才會綠」在畢業前就是假的；改為比對每個 step 的**完整指令字串**，mutation D3 轉紅釘住 |
| G-2 | tests/contract/ci-workflow.test.ts:8 | major | test-quality | fixed | baseline 只釘閘門「存在」，卻宣稱釘住「CI 強制什麼」：`\|\| true`、step 層 `continue-on-error`、`if: false`、job 層 `if: false` 四種中和手法都不會轉紅，而 continue-on-error 就活在同一個檔案的 windows-smoke；新增「job 與每個閘門 step 皆不得帶 if/continue-on-error」斷言，mutation D4/D5/D6 各自轉紅 |
| G-3 | tests/contract/ci-workflow.test.ts:51 | major | test-quality | fixed | 「以 SET 比對」實作成 `[...scripts].sort()` 對上一個必須自己先排好序的字面量，於是失敗訊息叫人做的事（把新閘門附加到 baseline）反而讓測試繼續紅；改為依**執行順序**比對完整指令清單，baseline 即是可讀的實際序列 |
| G-4 | tests/contract/ci-workflow.test.ts:73 | major | test-quality | fixed | windows-smoke 的負向斷言是新的斷言類別卻從未被 mutation 驗證（原 mutation 集三個都碰不到它），違反 PB-001 第 3 條；補上 D7（在 windows-smoke 加 counts 步驟 → 轉紅）與 D8（加 counts **註解** → 須維持綠）兩個方向的驗證 |
| G-5 | .github/workflows/ci.yml:34 | major | efficiency | fixed | `counts:check` 內部 spawn `vitest run --reporter=json`（`scripts/sync-counts.ts:27`），放進 test job 等於每個 PR 把 2,993 個測試跑兩遍（實測 +60s），而快速失敗只嘉惠「只有計數落後」的子集；使用者裁決走根因：`test:coverage` 額外寫出 JSON 報告，新增 `--from` 讓閘門吃既有跑分 —— 實測閘門步驟 1.26s，且刻意不做隱式尋找（殘留報告會把量測變常數） |
| G-6 | .prospec/changes/enforce-counts-in-ci/delta-spec.md:22 | major | spec-architecture | fixed | Spec 寫「compares the sequence … by equality」但實作是排序後比對，且測試註解自陳順序不是契約 —— 兩者互斥且會原樣畢業進信任區；改實作為真正的依序比對後，Spec 文字成立，並補上中和防護與路徑一致兩條 bullet |
| G-7 | CONTRIBUTING.md:59 | major | parallel-site (PB-007) | fixed | 只改了雙語 README 而漏掉 CONTRIBUTING.md 的 Development Workflow 指令清單，且 `prospec-verify` 模板宣稱「本 repo 的生成器在貢獻者文件中具名」—— 該檔從未提過 counts；補上 `pnpm counts` / `counts:check` 及其 CI 角色 |
| G-8 | tests/contract/ci-workflow.test.ts:28 | minor | correctness | fixed | 手寫文字切分會誤判：2-space 縮排的註解把 job body 截成 3/5 個閘門（正是註解宣稱守衛能防的那種假紅）、`run: \|` 區塊內的散文會被算成閘門；改用既有依賴 `yaml` 做真解析，三種形狀一次消滅 |
| G-9 | tests/contract/ci-workflow.test.ts:24 | minor | correctness | fixed | `lines.indexOf('  test:')` 是整行精確比對，CRLF checkout（repo 無 .gitattributes）下三條測試全數丟出 job not found，而 windows-smoke 的檔案清單很窄不會暴露它；改用 YAML 解析後不再依賴行尾字元 |
| G-10 | tests/contract/ci-workflow.test.ts:73 | minor | test-quality | fixed | `not.toMatch(/counts/)` 是對整個 job 原文的裸子字串比對，於是註解本身寫「這裡刻意不放 counts 步驟」會讓測試轉紅 —— 該註解正是斷言理由要求留下的；改為只對解析出的**指令**斷言，D8 實測註解不誤紅 |
| G-11 | .prospec/changes/enforce-counts-in-ci/tasks.md:34 | minor | test-quality | fixed | T10 把第三個 mutation 記成「刪掉非空守衛」，實際施加的是破壞 job 切分正則（打到的是另一個守衛），且該非空守衛對上非空 baseline 的相等比對本就不可能恆真 —— 記錄與證據不符；改寫任務敘述為實際出貨的設計與 mutation 集，並在 tasks.md 頂端註明是 review 修復輪的更新 |
| G-12 | tests/contract/ci-workflow.test.ts:57 | major | test-quality | fixed | 封閉集合仍漏一整類拼法：`commands()` 只取 `run` 步驟，`uses:` 動作完全不入清單（審查實測加入 `uses: some/depcheck@v1` ＋ `continue-on-error: true` 五條斷言全綠），而 test job 本來就有 4 個 `uses:` 步驟 —— 與 G-1 同族、只差一個拼法類別；改為比對**完整步驟清單**（動作記為去版號的 `uses:<name>`），mutation E1 轉紅、E2（版號升級）維持綠 |
| G-13 | .prospec/changes/enforce-counts-in-ci/delta-spec.md:22 | major | spec-architecture | fixed | Spec 宣稱「any command … added, removed, reordered, or rewritten 都會轉紅」，但多行 `run:` 只被收斂成 `\|`，在 coverage-comment 區塊內塞任何指令都不會被看見 —— 與 G-6 同型的 claim-exceeds-implementation，且即將畢業；新增「多行腳本不得內含套件管理器呼叫」斷言（mutation E3 轉紅），並把 Spec 文字改為陳述實際的比對粒度 |
| G-14 | tests/contract/ci-workflow.test.ts:112 | minor | correctness | fixed | windows-smoke 的負向斷言掃的是收斂後的清單，counts 步驟若寫成 block scalar 就通過 —— SC-001 只對單行拼法成立；改掃 raw `run` 內容（含 block body），mutation E8 轉紅 |
| G-15 | tests/contract/ci-workflow.test.ts:102 | minor | test-quality | fixed | `toBeUndefined()` 讓語意上的 no-op 誤紅：`continue-on-error: false` 與 `if: success()` 都留下完整閘門，卻會轉紅且訊息宣稱相反的事實；改為比對「值不得為 truthy／條件須等同預設」，mutation E4/E5 維持綠、E6（真中和）仍轉紅 |
| G-16 | tests/contract/ci-workflow.test.ts:80 | minor | correctness | fixed | 跨檔路徑斷言只認 `--outputFile.json=`，不管有沒有 `--reporter=json` —— 拿掉 reporter 後根本不會產出報告，斷言仍綠，閘門則以「找不到檔案」失敗（正是該斷言註解說要避免的情況）；補上 reporter 斷言，mutation E7 轉紅 |
| G-17 | .prospec/changes/enforce-counts-in-ci/proposal.md:59 | minor | documentation | fixed | SC-001 寫「八個 mutation」卻只列七個，tasks.md 又說「八個 mutation 各對應一條 FR」而 FR 只有五條 —— 在一個主題就是「計數誠實」的變更裡由 G-11 的改寫引入新的數錯；改為據實列出十二個 kill 與四個 false-red 防護，並移除「各對應一條 FR」的假對應 |
| G-18 | src/templates/skills/prospec-verify.hbs:353 | minor | parallel-site (PB-007) | fixed | G-7 只修了一半：出貨的 verify 模板仍無條件宣稱「This repo's generator is named in its contributor docs」，對本 repo 為真、對每個下游專案為假且會逐字 render 進它們的 SKILL.md；改為條件句「Where a project has one, its contributor docs name it.」並重新 bundle + agent sync，同時把「shipped template 不得斷言本 repo 的事實」記進 templates module README |
| G-19 | scripts/sync-counts.ts:34 | major | correctness | fixed | `--from` 沒有任何新鮮度概念，而文件把它寫成本地指令：以 repo 現有的報告跑 `counts:check --from` 對正確的文件回報 14 筆假 DRIFT（實測），寫入模式更會把過期數字蓋進五個檔案 —— plan 把此風險列為 High 並宣稱「無隱式尋找」是緩解，但指名一個檔案並不會讓它變新。修法：寫入模式直接拒絕 `--from`（`--from` 成為唯讀專用），雙語 README 與 CONTRIBUTING 改回以自行量測的 `pnpm counts:check` 為本地指令，`--from` 只描述為 CI 在 `test:coverage` 之後的用法 |
| G-20 | .prospec/changes/enforce-counts-in-ci/proposal.md:59 | minor | documentation | fixed | G-17 的修復在新數值上重演同一個缺陷：SC-001 寫「十二個」但括號只列十一項，delta-spec 與 tasks 同步帶著同樣的 12-vs-11 落差 —— 改為對「當時出貨的實作」重跑整組並以單一權威句記錄實測結果（11 kills / 5 guards / 1 control），四個工件共用同一句 |
| G-21 | .prospec/changes/enforce-counts-in-ci/delta-spec.md:19 | minor | documentation | fixed | AC3 寫三個 false-red 防護、proposal 與 tasks 寫四個 —— 即將畢業的驗收條件低報了自己的證據；統一為實測的五個（含新增的「block scalar 內提到 npm 的 shell 註解」） |
| G-22 | .prospec/changes/enforce-counts-in-ci/plan.md:77 | minor | documentation | fixed | plan step 4 仍停在修復前的 mutation 設計（八個、無 `uses:`／block scalar／`--reporter=json`），與其他三個工件已改寫的內容不一致；同步為權威句，並補上「每輪修復後都要對當時出貨的實作重跑整組」——前兩輪的 mutation 記錄對應的正是已被取代的實作 |
| G-23 | tests/contract/ci-workflow.test.ts:100 | minor | test-quality | fixed | 新增的 PACKAGE_MANAGER 掃描在 test job 內重演 G-4 的誤紅類別：coverage 區塊裡一行提到 npm 的 shell **註解**就會轉紅並宣稱該區塊「invokes a package manager」（實測 RED）；改為掃描前先剔除 shell 註解行，並把失敗訊息改成給出補救方向（閘門要自成一個 step），guard F5 實測維持綠 |
| G-24 | tests/contract/ci-workflow.test.ts:132 | minor | test-quality | fixed | `--reporter[= ]json` 釘的是 CLI 拼法而非它命名的行為：把 json reporter 移到 vitest.config.ts 仍可運作卻會轉紅，且訊息說了相反的事實；靜態檢查看不到 config 合併後的 reporters，故採 deliberate-exclusion：註解寫明此界線，失敗訊息改為指出「若 reporter 搬到 config，請把這條斷言指過去」 |
| G-25 | CONTRIBUTING.md:78 | minor | documentation | fixed | G-7 補的區塊把 CI 的行為掛在錯的指令上：註解寫「CI runs this against the report test:coverage writes」卻直接放在會自己跑一次全套的裸 `pnpm counts:check` 上方；改為明寫 CI 跑的是 `--from` 形式，本地這行則是自行量測 |
| N-1 | .prospec/changes/enforce-counts-in-ci/delta-spec.md:22 | major | spec-architecture | fixed | 第三輪出貨的「改寫模式拒絕 --from」是一條使用者可見、雙語 README 有記載、且有整個測試檔釘住的 CLI 契約，卻不在 REQ-TESTS-070 的 Spec 或任何 AC 裡，也不在 proposal FR-003／plan step 2／tasks T5 —— 畢業後信任區將不存在此契約（G-6 的 claim/implementation 不對稱，方向相反）；四個工件與 Spec bullet 一併補上 |
| N-2 | tests/unit/scripts/counts-from-report.test.ts:44 | major | test-quality | fixed | 新測試耦合了它最不該耦合的東西：斷言 `cannot verify` 只在「沒有其他 drift」時才會印出，任一 .hbs 計數過期就會走 drift 分支而讓 `pnpm test` 因無關狀態轉紅（審查在 repo 副本實測）；改為只斷言 skip 訊息與非零退出，兩者與 repo 狀態無關 |
| N-3 | tests/unit/scripts/counts-from-report.test.ts:1 | major | correctness | fixed | 新測試檔是 untracked 而其姊妹檔已 intent-to-add，`git commit -a` 會把含這兩個測試的計數提交進去卻漏掉檔案本身 —— CI 的新閘門會以「計數漂移」轉紅而非指出真因，且唯一防止 stale-write 回歸的機制永遠到不了 main；已 `git add` 兩個新檔 |
| N-4 | tests/contract/ci-workflow.test.ts:104 | minor | test-quality | fixed | 註解剔除只修了一半 G-4/G-23 的誤紅家族：block scalar 內引號字串提到套件管理器（如 echo '_regenerate with pnpm counts_'）仍會轉紅並宣稱相反的事實；改為只比對**指令位置**（行首或 shell 分隔符之後）的套件管理器，kill（行首 `pnpm exec`）維持紅、引號字串維持綠，兩者均實測 |
| N-5 | tests/unit/scripts/counts-from-report.test.ts:31 | minor | test-quality | fixed | `mkdtempSync` 沒有清理，是 repo 內 14 個用 mkdtemp 的測試檔中唯一沒有 rmSync 的，每跑一次套件就在 tmpdir 留一份 counts-*/r.json；改為 try/finally 清理 |
| N-6 | .prospec/changes/enforce-counts-in-ci/review.md:5 | minor | documentation | fixed | 本變更自己的 review 帳本是第四次數錯：22 列但 G-10／G-13／G-20 憑空消失、round-3 warning 宣稱 7 項對上 6 列、逐輪 majors 加總 9 大於表中 8 列 —— 根因是**這條分支從 main 切出、帶的是未修復的 review-merge 引擎**，三筆發現各自被同 (location, lens) 的前一列吞掉（issue #116 本 session 第二次現場重演）；已 rebase 到 fix/restrict-identity-fallback 並以真正的 CLI 重建 review.md，25 列 ID 全在，逐輪計數與表格自洽 |
| N-7 | tests/contract/ci-workflow.test.ts:57 | critical | correctness | fixed | verify 2/5 判 FAIL：N-4 的修復把錨點寫成 `^`（第 0 欄），但它守的那個 block 是 `{ … }` 群組、每一行指令都有縮排 —— 以周圍相同縮排插入 `pnpm run depcheck` 完全不會轉紅（實測綠），同一行放在第 0 欄才紅。閘門正好在它宣稱要封的地方、以該區塊唯一的書寫方式被繞過，且該 bullet 即將原樣畢業成假宣稱；改為 `^\s*`（任意縮排的行首）後 K12 轉紅，兩個誤紅防護（shell 註解、引號字串）實測仍綠 |
| N-8 | .prospec/changes/enforce-counts-in-ci/delta-spec.md:28 | major | spec-architecture | fixed | 即將畢業的 bullet 用了「gate step」卻從未定義，而實作把它窄化為 `pnpm run` 開頭的步驟 —— 於是在 `pnpm install --frozen-lockfile`（其 `--frozen-lockfile` 本身就是閘門）掛 `continue-on-error: true` 仍然全綠，讀規格的人推導不出這個範圍；把 install 納入閘門集合（K13 轉紅），並在 Spec 明文定義「command gate＝依賴安裝與 baseline 中的 `pnpm run` 腳本」，同時說明兩個 reporting 步驟因合法帶 `if: always()` 而不在範圍內 |
| N-9 | .prospec/changes/enforce-counts-in-ci/delta-spec.md:27 | minor | spec-architecture | fixed | 「added, removed, reordered, or rewritten … 都會轉紅」對多行腳本的 body 過度宣稱（它被收斂成單一 token）；補上「多行腳本以一個 token 比對，其內容由下一條 bullet 管轄」讓兩條 bullet 合起來覆蓋 block body |
| N-10 | .prospec/changes/enforce-counts-in-ci/proposal.md:59 | minor | documentation | fixed | 權威句第五度低報證據：N-4 實測的第六個誤紅防護（block scalar 內引號字串提到套件管理器）未列入、`控制組全綠` 只出現在 proposal —— 對當前出貨的實作重跑整組後改為據實的 13 kills／6 guards／1 control，四個工件逐字共用同一句 |
