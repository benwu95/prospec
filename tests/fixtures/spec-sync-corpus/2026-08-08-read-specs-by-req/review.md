# Review Findings: read-specs-by-req

| ID | Location | Severity | Lens | Status | Summary |
|---|---|---|---|---|---|
| F-1 | .prospec/changes/read-specs-by-req/delta-spec.md:216 | critical | spec-architecture | fixed | MODIFIED REQ-LIB-041 的 `**Spec:**` 假句已改為與程式碼檔頭一致的「Its only import is the other leaf that owns CommonMark fences」，`markdown-fences` 自身零 import 故「無 lib→lib cycle」那半句保留為真。 |
| F-2 | .prospec/changes/read-specs-by-req/delta-spec.md:190 | critical | spec-architecture | fixed | REQ-TESTS-080 的假斷言宣稱已改為描述實際出貨的守衛（fixture 釘定義判準五種形狀 ＋ FAIL-class `req-references` 在本 repo 語料維持 PASS）；一次性等值量測留在 tasks.md 作為變更紀錄，不再被當成測試宣稱。 |
| F-3 | src/templates/skills/prospec-archive.hbs:103 | critical | docs-claims | fixed | Phase 3.5 step 1 改為「讀每一條本變更畢業的 REQ」，REQ 集合由 Graduation key by scale 指定，並明寫五份 worklist 皆為 EXCEPTION report（乾淨落地的 REQ 不入列、quick 無 routes 故結構性全空）、其角色是指出哪些還需額外處理；契約斷言同步改寫並經三個變異驗證（`every requirement…`、`do **not** define that set`、`EXCEPTION report`、`names that set` 各自 KILLED）。 |
| F-4 | prospec/ai-knowledge/modules/services/read-only-queries.md:25 | critical | docs-claims | fixed | 改成讓宣稱為真而非只改文字：services/README.md:18 的路徑補成 `src/services/mcp.service.ts`，`collectMcpReadmeCounts` 現回報 claimed=8/actual=8、tools 3/3（原為 `claims: []` 空過）；sub-module 文字改為說明該 check 只在 claim 保持「反引號 `src/` 路徑 ＋ registers」形狀時才稽核。 |
| F-5 | src/lib/spec-headings.ts:123 | major | security | fixed | 首次修復（排除 CR 出字元類 ＋ 剝行尾 CR）**實測無效**：重跑仍為 cubic（8 KB 82 秒），因為觸發用的 CR 在行**中**（`B\rZ`）。真因是「可重疊 quantifier ＋ 永遠失敗的錨」，已改為結構解——`STORY_HEADING` 只比對到 id 為止、無尾隨 capture 無 `$`，`H2_HEADING` 同步去錨。實測 N=500→16000 全部 0.01～0.42ms（原 24／161／1267／10120／81863ms），且行中 CR 的 story 現在還能正確解析（原本比對不到）。變異驗證：把錨接回去後測試 run 直接掛住到 10 分鐘逾時（單一同步 regex 無法被 vitest timeout 中斷），已從備份還原並重新量測確認線性。 |
| F-6 | src/services/mcp.service.ts:316 | major | correctness | fixed | 無選擇器改為 toolError 並指向 `spec://feature/{name}` resource（該 result 無 `text` 欄位，回空集合等同宣稱「這個 feature 沒有任何規格」）；契約測試新增此案例。 |
| F-7 | src/services/spec-show.service.ts:71 | major | correctness | fixed | 區分「完全沒給旗標」（→ 整檔，REQ-CLI-035 的既有降級）與「給了旗標但展開後無可用 id」（→ `PrerequisiteError` ＋ 非零退出）；unit 覆蓋 `''`／`,`／`'  '`／`,,,`，e2e 覆蓋三種並斷言 stdout 不含 spec 內容。 |
| F-8 | src/lib/spec-headings.ts:301 | major | test-quality | fixed | 補上 h1 級 REQ 的邊界斷言（body 不得吞掉 `## Edge Cases`）——所有既有 fixture 都寫在 h4，`Math.max` 在該層級是 no-op；變異 `Math.max(heading.level, 2)` → `heading.level` 現已 KILLED。 |
| F-9 | src/cli/formatters/spec-show-output.ts:24 | major | test-quality | fixed | 新增 formatter 單元測試（`tests/unit/cli/output-sanitization.test.ts`）：以帶 BEL 的 spec 文字、miss selector 與 path 斷言三處 `sanitizeTerminal` 生效，並斷言 payload 結尾必有換行、miss 驅動 exit 1。 |
| F-10 | src/templates/skills/prospec-verify.hbs:27 | major | docs-claims | fixed | item 7 明寫「ADDED REQ 在此缺席是設計內狀態」（archive Phase 3.5 才畢業），並指示 MODIFIED／REMOVED 從 spec 讀、ADDED 從 delta-spec 讀、`--story` 用來看落點；契約測試釘住這兩句。 |
| F-11 | prospec/ai-knowledge/modules/cli/README.md:13 | major | parallel-site | fixed | `formatters/` 列 26 → 27，與同檔 Public API、module-map.yaml、index.md 一致。 |
| F-12 | README.md:610 | major | parallel-site | fixed | 兩份 root README 的 MCP `**Tools**:` 枚舉補上 `get_spec_requirements`（含 tool-not-resource 的理由與無選擇器拒絕），維持雙語 parity。 |
| F-13 | src/lib/spec-headings.ts:225 | major | spec-architecture | fixed | 依裁決走 (a)+(c)：REQ-LIB-041 的等價宣稱改為直述邊界規則，並加註兩個 owner 的一致性是「被監看的事實」而非保證；新增契約測試對十份 active spec 的全部 487 條 REQ 比對 indexSpec 與 archive merge 的邊界（含 `compared > 400` 的空集合守衛）。(b) 收斂 archive 寫入者留作後續 issue。 |
| F-14 | src/services/mcp.service.ts:293 | major | maintainability | proposed | MCP tool 未走 `spec-show.service`，自行重做 feature 解析、not-found 訊息與逗號展開（對應 service 私有的 `expand()`），兩條路徑已實質分岔（F-6／F-21）；依 PB-006／PB-007 應抽成單一共用入口。 |
| F-15 | src/lib/spec-headings.ts:124 | major | correctness | fixed | `STORY_HASHES` 改用與 matcher 同一個分隔符字元類（`[^\S\r\n]+`），ideographic space 的 heading 不再保留自身井號；新增測試斷言 `##\u3000US-1: Wide` 的 record heading 為 `US-1: Wide [P0]` 且 storyLevel 為 2，變異（改回 `[ \t]+`）KILLED。 |
| F-16 | src/lib/spec-slices.ts:57 | major | correctness | fixed | `covered` 由 id 改為位置區間去重；新增測試：US-1 內與 Edge Cases 下各有一個 `REQ-DUP-001` 時，`--story US-1 --req REQ-DUP-001` 同時回 story slice 與第二份 body、misses 為空。變異（改回 id 比對）KILLED。 |
| F-17 | .prospec/changes/read-specs-by-req/delta-spec.md:243 | major | spec-architecture | fixed | Phase 3.5 收斂清單更正為「原 7 條全在、新增 6 條、共 13 條」，並補 3a 項涵蓋 mcp-server.md:115 的 US-4 `so that` 與 :117-118 的 Acceptance Scenarios。 |
| F-18 | src/lib/spec-slices.ts:103 | major | maintainability | fixed | `storyLevel` 上移到 `SpecRequirementRecord`（由 `indexSpec` 建 record 當下寫入），刪除 `storyLevelOf` 線性查表與 `?? 2` 的隱性 h2 fallback；heading 形狀只剩 parser 一個擁有者。 |
| F-19 | src/lib/spec-headings.ts:213 | major | maintainability | fixed | 三個欄位分別處置：`level` 判定為誤報（REQ-LIB-041 的 `**Spec:**` 明載 record 攜帶 heading level，且 rewrite 必須保留原層級）；`struck` 透出到 `SpecSlice` 並補測試——Deprecated 區外的 struck REQ 原本以 `deprecated:false` 被當成 active；`SpecStoryRecord.title` 刪除，改為 `heading`（原文，兼作重複 US 的判別鍵）。 |
| F-20 | src/lib/spec-slices.ts:3 | minor | docs-claims | fixed | 檔頭更正為「兩個 surface 共用 `selectSpecSlices`；CLI 另外用 `renderSpecSlices`，tool 回結構化 slices」。 |
| F-21 | src/services/spec-show.service.ts:60 | minor | security | fixed | 服務端拒絕訊息的可用清單補上 `.filter(isSafeResourceName)`，與 MCP 的 list 半邊一致，不再列出讀取路徑一律拒絕的名字。 |
| F-22 | src/services/mcp.service.ts:311 | minor | security | fixed | toolError 不再回吐呼叫端提供的 feature name（service 無法匯入 cli 的 sanitizer，依賴方向禁止），改為只列出實際存在的 spec；契約測試以帶真實 ESC 的 OSC 52 名字斷言不回吐。 |
| F-23 | src/lib/spec-slices.ts:122 | minor | correctness | fixed | `renderSpecSlices` 改以 slice 自身攜帶的行尾串接，CRLF 輸入不再產出混合行尾；變異（固定 `\n`）KILLED。 |
| F-24 | src/cli/formatters/spec-show-output.ts:32 | minor | correctness | fixed | `misses` 改為 `{selector, kind}`，kind 來自旗標而非 `startsWith('US-')` 猜測；測試涵蓋 `--req us-1` 與 `--story 34` 兩個原本被貼錯標籤的方向。 |
| F-25 | tests/contract/skill-format.test.ts:4960 | major | test-quality | fixed | 我為 F-3 新寫的斷言原本把兩個子句寫成 disjunction（`A\|B`），實測刪掉 `EXCEPTION report` 仍全綠——任一邊滿足即通過，兩邊都沒被單獨釘住；拆成兩條獨立斷言後兩個變異皆 KILLED。 |
| F-26 | .prospec/changes/read-specs-by-req/delta-spec.md:165 | critical | spec-architecture | fixed | F-3 的修復自身引入的矛盾：REQ-TEMPLATES-177 的 `**Spec:**` 仍編碼被修掉的錯誤語義（「the worklists decide WHICH requirements to judge」），與改後模板直接相反且會逐字落進信任區；已改寫為「REQ 集合由 graduation key 指定、worklist 為例外報告」，並把四句改為各自獨立斷言。 |
| F-27 | src/lib/spec-headings.ts:207 | minor | maintainability | fixed | 註解與測試 fixture 內的字面全形空白觸發 `no-irregular-whitespace`（lint 紅），改為 `\u3000` 轉義與文字描述。 |
