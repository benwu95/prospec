# Lessons Ledger

> Version-controlled, accumulating tier of the feedback-promotion pipeline. Keyed + incremental —
> `frequency` is a cross-change counter (never re-derived by re-scanning). `status`: personal |
> suggest-promote | promoted | declined | retired (the closed set the format reference declares).
> Auto-fed by `/prospec-archive` Phase 4.5 (Harvest), refreshed by `/prospec-learn` Collect, and
> audited by `/prospec-learn` Sweep before every Collect — an expired row turns `status: retired`
> with a `｜ **Retired**:` suffix naming the mechanism that eliminated the root cause; no row is
> ever deleted and no counter ever edited. A `promoted` / `retired` row keeps its one-line failure
> mode plus that suffix: the canonical rule lives in `_playbook.md`, and the per-occurrence
> narrative is `git log -p` on this file. A `personal` row is never compressed — its description IS
> the promotion evidence.
> Format + harvest + sweep rules: `.claude/skills/prospec-learn/references/promotion-format.md`.
> Durable across worktrees/clones (replaces the retired gitignored `.prospec/lessons.md`).
> Evidence pointer: each `source_changes` name archived since that convention existed resolves to its
> committed record at `prospec/specs/_archived-history/{date}-{name}.md` — cite that file's
> `## Review & Verify` section (grade, criticals/majors, quality_log digest) as the durable evidence,
> not the gitignored `.prospec/archive/` bundle. Names predating it (e.g. `add-review-fix-loop`,
> `add-drift-checker`, `harden-lib-correctness`, `src-review-remediation`,
> `src-review-round2-remediation`) have no such file — their evidence is this file's own git history,
> and a missing record is never evidence that nothing happened.
>
> **Description language**: the `description` column — including its `｜ **Promotion**:` / `｜ **Retired**:`
> provenance suffix — is written in the original correction's language (often Traditional Chinese), a named
> exception the Constitution's Language Policy rule declares for this file, kept so the raw correction
> context is not lost in translation at this working-notes tier. Keys, `kind`, `status` and the structural
> fields stay English (`status` is a bare enum token); `_playbook.md` is English apart from the correction
> evidence the same rule exempts.

| key | description | frequency | impact_modules | kind | source_changes | status |
|---|---|---|---|---|---|---|
| test/structural-false-green | 斷言只驗內容存在性、不驗結構完整性與負向路徑 → 缺陷在場仍全綠；須 section-scoped + 結構斷言（集合/順序/contiguity）+ 負向斷言 + mutation-verify ｜ **Promotion**: PB-001 強化版，Approved-by: benwu95, 2026-06-11（正典規則與 21 次再證的逐次明細見 `_playbook.md` PB-001 與 git 歷史）；最近再證：pilot-mutation-testing 第 19 度——涵蓋面等於作者當下想得到的清單（只點名 `prospec-check.yml` 而真正的閘門是 `ci.yml`；負向 grep 只擋一種措辭），改為列舉／集合基準比對並以具名守衛取代恰好釘在當日數量的 `>= 3` | 29 | 6 (tests,templates,cli,lib,services,types) | convention | add-output-contract, add-review-fix-loop, add-token-measurement-harness, reorder-stable-prefix-loading, add-drift-checker, add-mcp-server, src-review-round2-remediation, add-reverse-spec-extraction, quick-scale-and-ceremony-cleanup, slim-skill-trigger-context, converge-skill-triggers, document-drift-report-contract, align-language-policy-scope, split-verify-adjudication, archive-cli-entry, fix-cli-first-regressions, add-harness-capability-flags, report-dropped-req-bullets, pilot-mutation-testing, delegate-module-adjudication, mechanize-light-scale-gates, unify-req-heading-matcher, stop-clobbering-product-spec, configurable-generated-artifacts, add-reuse-single-source-gate, strip-governance-ids-from-templates, fix-disclosed-cleanups, retire-legacy-index-migration, refresh-v2-documentation | promoted |
| docs/measurement-attribution-overclaim | 文件/註解宣稱的行為必須與實作實際可觀測者一致；量不到／沒做到的用 deliberate-exclusion 措辭明示 ｜ **Promotion**: PB-003，Approved-by: benwu95, 2026-06-12（正典規則與 17 次再證的逐次明細見 `_playbook.md` PB-003 與 git 歷史）；最近再證：archive-cli-entry 第 12 度——skill 模板宣稱「CLI rolls back…and reports the error」，實則外層 catch 吞掉、終端只見泛用 skipped 行（review major F4），以 `ArchiveResult.skippedReasons` 攜帶真實原因使宣稱回真 | 20 | 6 (cli,templates,lib,services,types,tests) | playbook | add-token-measurement-harness, reorder-stable-prefix-loading, add-drift-checker, add-mcp-server, harden-lib-correctness, src-review-round2-remediation, backfill-promotion-path, fix-init-clobber-add-upgrade, converge-constitution-audit, quick-scale-and-ceremony-cleanup, add-metadata-format-reference, archive-cli-entry, delegate-module-adjudication, agent-sync-hygiene, preserve-curated-index-columns, slim-knowledge-l1-l2, translate-feature-specs-to-english, add-reuse-single-source-gate, refresh-v2-documentation, normalize-executor-labels-and-stats | promoted |
| measure/spend-accounting-failure-paths | 金流/預算控制必須逐呼叫入帳、驗證輸入（NaN/空值）、失敗路徑不漏計——4 個 critical 同源於「成功路徑才入帳」假設 | 1 | 2 (lib,scripts) | playbook | add-token-measurement-harness | personal |
| docs/runbook-executable-as-written | 操作 runbook 留檔前須逐指令實際執行驗證（pnpm `--` 轉發、commit 前置缺漏皆為照抄必死） | 1 | 1 (docs) | playbook | reorder-stable-prefix-loading | personal |
| git/commit-message-english | commit 訊息（標題+正文）一律英文  ｜ **Promotion**: 已由使用者直接入憲：原則 2 + 0c1a4a2，無需再晉升 | 1 | 0 | constitution | session-correction (1d236fc amend) | declined |
| spec/fr-uniformity-needs-shared-type | 宣告「每個 X 都要 Y」的 FR，實作須以共用型別強制（如 `Source<T> = {available, reason?, items}`），逐項手寫必有遺漏——五檢項三有二無 availability 包裝成 critical（review C4） | 1 | 3 (lib,services,types) | convention | add-drift-checker | personal |
| ci/pipeline-semantics-not-unit-testable | CI workflow 管線語意層缺陷（pipefail、shell 預設）不被 CLI 層單元/e2e 覆蓋——workflow 模板需要自己的內容斷言（shell: bash、SHA pin、permissions、compose 形式）；`\| tee` 吃掉 exit code 由三個獨立 lens 同報（review C1） | 2 | 2 (templates,tests) | playbook | add-drift-checker, add-windows-smoke-ci | personal |
| scan/false-positive-kills-trust | 掃描器/檢查器類功能：誤報比漏報致命（摧毀對工具的信任）——排除規則（fenced block 含 CommonMark 關閉語意、block comment、placeholder/glob、repo 外路徑）須首版設計並以 dogfood 實證零誤報（首跑即中 fence 誤報 + 6 個誤報類 major） | 1 | 1 (lib) | playbook | add-drift-checker | personal |
| refactor/relocation-reference-sweep-completeness | 遷移／改名一個 symbol／型別／artifact 須掃全部引用面（src 與**測試**的 import、wording／描述、知識檔），不只明顯的 file-path 字串；path-grep 契約斷言對散文殘留是盲的 ｜ **Promotion**: PB-008 · Approved-by: benwu95 · 2026-07-06（正典規則與 4 次再證的逐次明細見 `_playbook.md` PB-008 與 git 歷史）；最近再證：inject-resolved-knowledge-budgets 第 3 度——`KnowledgeSizeBudget` 移至 `types/config` 後測試仍從舊模組 import（TS2459 latent），當時 typecheck／test／lint 三閘皆漏（該一類已於 2026-07-06 由 include-tests-in-typecheck 機械化，其餘引用面仍靠人工掃描） | 4 | 6 (templates,types,tests,lib,services,cli) | playbook | add-knowledge-flywheel, quick-scale-and-ceremony-cleanup, inject-resolved-knowledge-budgets, implement-hierarchical-index | promoted |
| security/invariant-misses-parallel-consumers | 安全／一致性不變式只施加在最初發現的面，同一資料源的平行消費路徑漏掉；修一個面時必須 grep 同一資料源的全部消費者，把不變式套到每一面並各自測試凍結 ｜ **Promotion**: PB-007，Approved-by: benwu95, 2026-07-02（正典規則與 11 次再證的逐次明細見 `_playbook.md` PB-007 與 git 歷史）；最近再證：archive-cli-entry 第 9 度 ×2——dry-run 的 feature-map 預測以結果導向 guard 重推 real run 的觸發條件（confirmed critical F1，修法抽 `readFeatureRoutes` 單一來源、預測鏡射觸發而非結果）；同輪 formatter 自述「failure-class 走 stderr」卻只套 refused/notFound、漏同類的 skipped（F3 major） | 13 | 6 (lib,services,cli,types,tests,templates) | playbook | add-mcp-server, src-review-round2-remediation, fix-upgrade-doc-coverage, quick-scale-and-ceremony-cleanup, align-language-policy-scope, enforce-metadata-schema, split-verify-adjudication, harden-verify-adjudication, archive-cli-entry, delegate-module-adjudication, agent-sync-hygiene, refresh-v2-documentation, normalize-executor-labels-and-stats | promoted |
| git/commit-body-bulleted | commit message 正文用條列式（- 開頭逐項），不用散文段落——使用者於 add-mcp-server commit 建議時糾正 | 1 | 0 | convention | session-correction (add-mcp-server) | personal |
| docs/duplicated-count-drift | 重複的事實型數字（測試 per-layer 細項、模板／檔案 inventory）跨 README ×2 + index.md + module README 漂移——根因已由 `pnpm counts` 消滅，殘留缺口已由 Constitution `[MUST] Factual Count Integrity` 三層模型收束 ｜ **Retired**: 2026-07-04（#66）——根因由 #65 消滅，2026-08-02 起 `pnpm counts:check` 於 CI 執行；2026-08-04 全部計數規則晉升至 Constitution | 23 | 6 (lib,types,tests,templates,docs,services) | playbook | readme-onboarding-restructure, enhance-skill-instructions, fix-archive-sibling-reference, vendor-engineering-heuristics, src-review-round2-remediation, complete-capability-to-feature-migration, add-reverse-spec-extraction, extract-backfill-spec-skill, feature-first-backfill, add-feature-map, converge-archive-summaries, backfill-promotion-path, fix-init-clobber-add-upgrade, preserve-agent-config-edits, remove-deprecated-steering-command, fix-upgrade-doc-coverage, add-plan-flow-diagram, migrate-skill-contract-to-vitest, carry-review-verify-evidence, skip-unspawnable-test-command, restore-cli-first, restrict-identity-fallback, extend-provenance-audit-scope | retired |
| archive/knowledge-sync-touched-module-readme | archive 知識同步只更新「內容有變」的 module README，漏掉「source 被改但 README 內容剛好不需改」的 module → drift knowledge-health 以 git commit 時間戳判該 module stale (WARN)，並被下個變更 verify V4 繼承 ｜ **Retired**: 2026-07-04（#66）——根因由 #65 消滅：verify S/A commit-prompt 將每個 source-touched module README 折入 feature commit（預防），archive Entry Gate 為 backstop；source-only commit 不再抵達 archive stale。17 次再證的逐次明細見 git 歷史與 `source_changes` → `specs/_archived-history/`（生成檔造成的同族 stale 另見 knowledge/generated-file-trips-module-stale，亦已 retired） ｜ **Un-retired**: 2026-08-03 sweep（Approved-by: benwu95）——退役理由的 prevention 半邊已被實證推翻：add-learn-staleness-sweep 的 feature commit 只同步 lib、漏了 templates，`knowledge-health` 當場報 templates stale（pre-amend commit `71db32c` 改 `src/templates/**` ×2 而無 `modules/templates/**`），由 archive Entry Gate 這個 backstop 攔下、amend 修正。verify S/A commit prompt 的「折入 feature commit」是給 agent 的**指令**而非機制，失效模式因此仍可能發生，故依「只有失效模式不可能再發生才退役」的判準恢復為 live 並計入本次復發 ｜ **Declined**: 2026-08-08（Approved-by: benwu95）——晉升問題已於 2026-08-03 由 `PB-005` 的 Sweep correction 裁決：該 entry「stays retired by explicit adjudication and is deliberately NOT returned to `## Entries`」，理由是 archive Entry Gate 這個 backstop 使結論成立。本列先前仍掛 `suggest-promote`，於是每次 learn 都重新建議一件已裁決的事。改記 `declined` 只登記那次裁決，**不退役**——失效模式仍活著，stop-silent-spec-body-loss 第 21 次重現（feature commit 改了 `src/templates/**` 未動 `modules/templates/**`，`knowledge-health` 當場報 templates stale，amend 修正）。計數器與 `source_changes` 全數保留為證據。真正的解是把 prevention 機械化（比對 commit 中 source 有變的模組 vs knowledge 有變的模組，與 `agents:check` 同型）——issue 已開；該閘門落地且無後續重現後，才具備退役的證據 | 20 | 6 (types,lib,services,cli,tests,templates) | playbook | centralize-index-column-schema, fix-archive-sibling-reference, vendor-engineering-heuristics, src-review-remediation, src-review-round2-remediation, complete-capability-to-feature-migration, add-reverse-spec-extraction, extract-backfill-spec-skill, feature-first-backfill, add-feature-map, converge-archive-summaries, backfill-promotion-path, fix-init-clobber-add-upgrade, preserve-agent-config-edits, remove-deprecated-steering-command, add-plan-flow-diagram, carry-review-verify-evidence, add-learn-staleness-sweep, unify-req-heading-matcher, stop-clobbering-product-spec | declined |
| fix/rework-misses-parallel-site | rework 一條行為規則套到 N 個明顯站點時，漏掉邏輯相同的平行站點（含非 skill 的 entry config／lifecycle／scale 變體／模組 README） ｜ **Promotion**: 併入 PB-007（觸發條件擴及「套用一個修正」），Approved-by: benwu95, 2026-07-28（正典規則與 5 次再證的逐次明細見 `_playbook.md` PB-007 與 git 歷史）；最近再證：split-verify-adjudication 第 4 度——修 flaky timeout 只改自己新增測試的 3 個檔案，漏掉同屬 git-bound 負載類、但未被本變更編輯的第 4 個檔案。教訓補強：判定「同一族」要按**負載／失效機制**分類，不是按「我改過哪些檔案」 | 12 | 5 (lib,services,templates,tests,cli) | playbook | enhance-skill-instructions, sync-knowledge-at-verify-commit, enforce-metadata-schema, split-verify-adjudication, mechanize-light-scale-gates, stop-clobbering-product-spec, read-specs-by-req, configurable-generated-artifacts, fix-project-agnostic-template-leaks, fix-disclosed-cleanups, add-req-id-uniqueness-check, support-native-language-trust-zone | promoted |
| refactor/duplicated-helper-parallel-sites | 同一邏輯 helper 在平行模組各複製一份（masking heuristic、read-or-empty、containment 讀取、emphasis 剝除）→ 覆蓋面分歧且改動易漏一處；應抽單一 helper 至中性 leaf module 共用（勿造 lib→lib cycle） ｜ **Promotion**: PB-006，Approved-by: benwu95, 2026-06-22（正典規則與 8 次再證的逐次明細見 `_playbook.md` PB-006 與 git 歷史）；最近再證：enforce-metadata-schema 第 7 度——新寫的 `stripMarkdownEmphasis` 與 `lib/knowledge-reader` 既有的 `**` 剝除重複且字元集更寬（services 手抄 lib），對 `` `lib` ``/`*lib*` 會給出不同模組身分；抽為 `knowledge-reader.stripCellEmphasis` 單一來源並補跨消費端一致性測試 | 10 | 4 (lib,services,types,tests) | convention | src-review-round2-remediation, harden-feature-prefixed-req-sync, preserve-agent-config-edits, fix-upgrade-doc-coverage, dedupe-init-doc-registry, mechanize-review-gate, enforce-metadata-schema, restore-cli-first, refresh-v2-documentation, normalize-executor-labels-and-stats | promoted |
| knowledge/req-citation-precedes-graduation | 在 knowledge-update 階段於 module README 引用本變更「未畢業」的 REQ id → feat commit 後 `prospec check` 即報 req-references dangling FAIL，須待 archive Phase 3.5 graduation 才消解（feat-commit→archive 之間存在 transient dangling 視窗） ｜ **Retired**: 2026-08-03 · 根因已於 `0682ca5`（2026-07-04）消除——`/prospec-verify` 的 S/A commit prompt 現明文要求「Update descriptions only; do not cite this change's not-yet-graduated REQ ids」，規則已落在撰寫決策點。三個 occurrence（2026-06-15 ×2、2026-07-03）全部早於該 commit，此後約 30 個封存變更未再發生。Retired-by benwu95 | 3 | 3 (templates,tests,types) | playbook | add-dependency-knowledge, add-plan-flow-diagram, add-quickstart-command | retired |
| spec/reverse-extraction-fabricates-and-undercovers | AI 反向萃取/生成 spec 兩失效模式（dogfood 於真實 Python brownfield module 證）：(a) 捏造可數事實——草稿宣稱的 enum/mapping 數量與來源實際不符（多處精確數字錯誤）；(b) 靜默部分覆蓋——僅約半數行為入 US、其餘靜默漏失。守則：可數事實對來源核實否則 `~N`/`[NEEDS CLARIFICATION]`；先枚舉行為再聚類全部、明列 deferred（coverage 須可見）。已折入 prospec-design Phase 2b-code + REQ-TEMPLATES-104 AC5 + mutation-verified contract 斷言 | 1 | 2 (templates,tests) | playbook | add-reverse-spec-extraction | personal |
| verify/coverage-not-machine-measured | verify P4「coverage ≥80%」無法機器量測——repo 未裝 coverage provider，質性覆蓋充足但數值未證 → 每次 verify 記 WARN（grade A 不受阻） ｜ **Retired**: 2026-06-16（當時未記理由）；2026-08-03 sweep 補實：根因已消除——`@vitest/coverage-v8` 已列 devDependency，`pnpm test:coverage` 於 `ci.yml` 的 test job 執行並寫出 vitest JSON 報告，coverage 自此為機器量測值而非質性判斷（計數規則見 Constitution `[MUST] Factual Count Integrity`） | 3 | 1 (tests) | playbook | add-knowledge-refresh-command, raw-scan-multi-language, raw-scan-c-cpp-swift | retired |
| raw-scan/techstack-deps-language-ordering | detectTechStack 與 collectDependencies 對每個語言的偵測順序/條件須對齊，否則同一 raw-scan 的 Tech Stack 與 Dependencies 兩段互相矛盾——review 兩度揪 major：(1) Ruby+PHP polyglot（Tech Stack=ruby 但 deps fall-through 到 composer）以 Gemfile→[] 短路修；(2) C-family 語言/deps 閘不對稱（detectCFamily 需 build 檔+源副檔名、collectDependencies 只看 manifest）以共用 hasCFamilySource gate 修。新增語言時兩處務必同序、同條件並各自測試。score：freq=2 ∧ modules=2 ⇒ 未達 freq≥3，維持 personal | 2 | 2 (lib,services) | convention | raw-scan-multi-language, raw-scan-c-cpp-swift | personal |
| design/hollow-artifact-to-pass-gate | 為了過一個為別種流程設計的 gate 而產生空殼 artifact（backfill promote 一度產 plan.md/tasks.md，內含預先打勾、實際無事可做的 task）是 make-work——正解是讓 gate **scale-aware**（verify/review Entry Gate 對 backfill 例外、verify 1/5 not-applicable、archive Phase 2 tasks-skip），不是用假檔蓋過。對稱於 quick 捨 plan+delta-spec、backfill 捨 plan+tasks。add-scale-adapter playbook 的本意是逐站顯式處理 false-block，非 fabricate artifact。使用者 review 揪出 | 1 | 2 (templates,tests) | playbook | backfill-promotion-path | personal |
| security/self-attested-marker-needs-provenance | 可手改的純文字 metadata marker（如 `scale: backfill`）若用來放寬品質 gate，等於無 gate——任何人可手設以 bypass（backfill 降級 [MUST] 測試 gate，新程式碼亦可冒充）。須把 marker 綁定到可檢查的 provenance（backfill 品質降級僅在 `backfill-draft.md` 存在時套用，否則退回 standard 契約 + WARN），把「promote 是唯一入口」從 social convention 變成硬條件。verify review C2 揪出（confirmed critical）；skip-unspawnable-test-command 第 2 度：同族的反向案例：新增的誠實 skip 在 Windows 上可由 `test_command: anything.cmd` 觸發，等於用設定關掉一個 FAIL-class 閘門。結論是**不加閘門而是寫明限制** —— 誠實 skip 家族本質上可由設定觸及，該 check 的用途是防無聲假 PASS 而非不可繞過；揭露靠報告的 `skipped` + reason 與 verify 的 `not-adjudicated`（絕不 PASS）保留，並在 proposal 的 Edge Cases 明載 | 2 | 2 (lib,templates) | playbook | backfill-promotion-path, skip-unspawnable-test-command | personal |
| workflow/user-confirm-timeout-proceed-with-record | 需要使用者即時確認的規劃決策（如 scale 確認）逾時無回應 → 以推薦預設續行是正確的自主行為，但必須三處留痕：quality_log WARN、proposal Open Questions、最終摘要明示可覆寫點——讓事後批准（使用者以 y 續行）有據可查。fix-upgrade-doc-coverage：AskUserQuestion 60s 逾時，scale: standard 依準則表+前例採用並全程留痕，verify 前使用者實質批准；add-plan-flow-diagram 第 2 度：new-story AskUserQuestion 逾時，四項設計選項（觸發/位置/內容/scale）依 issue 意圖採推薦預設並三處留痕（quality_log WARN、proposal Open Questions、摘要明示可覆寫），使用者事後以 y 續行並確認 any-of 判準 | 2 | 0 | playbook | fix-upgrade-doc-coverage, add-plan-flow-diagram | personal |
| types/type-name-contradicts-role-semantics | 型別名承載的語意與使用場景矛盾（CanonicalDoc 註解宣稱 never user-customized，卻被 user-managed 清單重用）是 load-bearing 誤導——共用「形狀」時型別名應角色中性（如 ConventionDocSource），ownership 語意放在各 const 註解；proposal 載明的結構名（UserManagedDoc）被實作靜默改用他型也屬同型偏差。dedupe-init-doc-registry review major，實作者採納改名修復 | 1 | 1 (types) | convention | dedupe-init-doc-registry | personal |
| test/consistency-guard-over-extraction | 同一份指引為 skill 自包含而刻意重述於兩範本（plan-format Section 5 與 prospec-plan Phase 4 的 any-of 門檻，比照 Context7 前例）時，抽共用 partial 會因兩處需要不同「高度」的敘述而變形、且與既有 Context7 不一致、降低範本可讀性——正解是加 section-scoped 跨檔一致性契約斷言釘住同一組 token（mutation-verified），保留兩形態又擋漂移。與 refactor/duplicated-helper-parallel-sites（PB-006，抽 helper）互補：程式邏輯重複抽 helper、指令散文重述加一致性守衛 | 1 | 2 (templates,tests) | convention | add-plan-flow-diagram | personal |
| test/e2e-help-timeout-flaky-under-load | git／spawn-bound 測試在 full-suite 併發滿載下爆掉 vitest 5s 預設（cold CLI spawn、gitFixture 密集），單獨跑全綠——環境性 flake，但 `--record-tests` 會把它的 exit code 蓋章進 `metadata.yaml`，使機械測試裁決非決定性 ｜ **Promotion**: PB-010, Approved-by: benwu95, 2026-07-29（正典規則與 6 次再證的逐次明細見 `_playbook.md` PB-010 與 git 歷史）；最近再證：split-verify-adjudication 第 6 度——新增約 19 個 git-bound 測試使套件 +51s，把**未被本變更編輯**的 `tests/contract/mcp-server.test.ts` 推爆（鄰居有 `}, 20000)` 覆寫故通過、它沒有故死）。修法一般化為每個檔案在檔頭宣告 `vi.setConfig({ testTimeout: 30_000 })`，而非逐測試覆寫（後者會反被檔案預設倒掛） | 8 | 2 (services,tests) | playbook | generate-factual-counts, sync-knowledge-at-verify-commit, mechanize-review-gate, converge-constitution-audit, support-file-module-paths, split-verify-adjudication, guard-canonical-doc-drift, autonomous-drift-draft | promoted |
| docs/module-readme-manual-counts-uncovered | 機器計數未涵蓋的手維護計數（模組 README 檔數、feature spec frontmatter 計數器）仍隨 source 改動漂移 ｜ **Promotion**: PB-004 窄化重啟 → 2026-08-04 晉升至 Constitution `[MUST] Factual Count Integrity` tier 3（hand-maintained），Approved-by: benwu95 | 4 | 4 (tests,lib,cli,services) | convention | mechanize-review-gate, emit-trigger-scaffold, enforce-metadata-schema, mechanize-knowledge-sync-gate | promoted |
| spec/behavior-change-omits-modified-req | 變更改動了既有 named REQ 所治理的行為，但 delta-spec 只 ADDED 新 REQ、未把該既有 REQ 列 MODIFIED → archive Phase 3.5 只寫新 REQ、既有矛盾條款留存 → 自我矛盾的 Feature Spec。plan/delta-spec 階段須 grep 受影響行為的措辭與識別字，把每條治理它的既有 REQ（含 User Story 驗收場景）列 MODIFIED ｜ **Promotion**: PB-017 · freq=3, modules=3 · approved-by benwu95 · 2026-08-03（正典規則與逐次再證見 `_playbook.md` PB-017 與 git 歷史） | 5 | 4 (templates,tests,types,services) | playbook | slim-skill-trigger-context, align-language-policy-scope, extend-provenance-audit-scope, add-learn-staleness-sweep, configurable-generated-artifacts | promoted |
| docs/readme-check-enumeration-manual | 每新增一個 drift check，root README 的 `prospec check` 散文枚舉就漂移——無單一來源、無機器守門 ｜ **Promotion**: PB-009 → 2026-08-04 晉升至 Constitution `[MUST] Factual Count Integrity` tier 3（hand-maintained），Approved-by: benwu95 | 5 | 3 (lib,cli,docs) | playbook | mechanize-review-gate, quick-scale-and-ceremony-cleanup, enforce-knowledge-size-budget, split-verify-adjudication, unlock-measurement | promoted |
| test/typecheck-excludes-tests-hides-type-errors | `tsconfig.json` `exclude: ["tests"]` 使 `pnpm typecheck` 不檢查測試檔——測試檔的 type-only import 破損（遷移型別後仍從舊模組 import → TS2459）不被 typecheck 攔、esbuild 於 runtime strip 故 vitest 亦綠、eslint 亦不報，三閘全漏，僅對抗式 review 揪出 ｜ **Retired**: 2026-08-03 —— 根因已由 include-tests-in-typecheck（2026-07-06）消除：`pnpm typecheck` 改跑 `tsconfig.typecheck.json`（`include: src/**, tests/**, scripts/**`），測試檔與 scripts 的型別破損現在是機器可攔的 TS 錯誤，該變更當場修掉 54 個浮現的既存型別錯誤。兩個 occurrence 皆早於該日期；殘留的引用面（wording／文件／知識檔）由 refactor/relocation-reference-sweep-completeness（PB-008）承接。Retired-by benwu95 | 2 | 1 (tests) | playbook | inject-resolved-knowledge-budgets, include-tests-in-typecheck | retired |
| review/proposed-fix-or-scenario-can-be-wrong | 對抗式 review 的 finding 情境或建議修法未必正確，套用前須先重現 finding、再驗證修法不引入回歸——support-file-module-paths M1：reviewer 稱非源碼檔的 fenced ```ts import 會成偽依賴邊，但實測該 fence 已被既有 template-literal 去除（backtick 觸發）抹掉、不成邊（真正向量是非源碼檔的**裸** import 行）；且 reviewer 建議修法（非源碼檔回退 `${prefix}/${EXT}`）會 scandir 一個檔案 → 重引本變更剛修掉的 ENOTDIR。改以「非源碼檔 file 條目回傳 null、caller skip」正確修復並 mutation-verify。守則：review→fix loop 中，尤其對 reviewer 附帶的具體修法，先重現再驗回歸 | 2 | 3 (lib,types,templates) | playbook | support-file-module-paths, add-issue-link-field | personal |
| knowledge/generated-file-trips-module-stale | 提交 module 內的 generated 檔（`src/lib/bundled-templates.ts`，每次 template 變更由 `pnpm bundle` 重生）會使 drift knowledge-health 以 git 時間戳判該 module stale——即使邏輯與 README 內容皆未變、且該 module 不在 `related_modules`，於是作為 pre-existing WARN 被下個變更 verify V4 繼承 ｜ **Retired**: 2026-08-02 —— 根因已由 exclude-generated-from-staleness 機械解消除（issue #121）：`GENERATED_SOURCE_ARTIFACTS` 註冊表把生成檔排除在 `last_src_commit` 的路徑集合外，digest 涵蓋不變，兩個方向各有 mutation 驗證過的測試釘住。裁決為不寫入 `_playbook.md`——理由同 docs/duplicated-count-drift：反覆出現的規則之所以反覆，是因為沒有任何機制執行它 | 3 | 2 (lib,templates) | playbook | emit-trigger-scaffold, converge-skill-triggers, enforce-counts-in-ci | retired |
| knowledge/l2-readme-at-budget-cap | L1/L2 知識檔壓在 token 預算上緣時，任何新增的真實不變式都會觸發 knowledge-size WARN，而壓縮自己新增的文字只救回個位數 token——根因是檔案已滿。解法依序：壓縮既有內容、抽 sub-module、或由專案擁有者以明文 REQ 調高預算並自陳「PASS 來自預算放寬而非知識變小」；絕不降低知識密度、絕不靜默調預算 ｜ **Promotion**: PB-011, Approved-by: benwu95, 2026-07-29（正典規則與 5 次再證的逐次明細見 `_playbook.md` PB-011 與 git 歷史）；最近再證：split-verify-adjudication 第 3 度且擴及 L1——`_status-lifecycle.md` 變更前已 1791/1800，改為由擁有者上調預算（l1 2000／l2 1500）並以 REQ-TYPES-069 登記 | 6 | 6 (lib,types,docs,tests,templates,services) | convention | align-language-policy-scope, enforce-metadata-schema, split-verify-adjudication, restrict-identity-fallback, extend-provenance-audit-scope, read-specs-by-req | promoted |
| review/fix-introduces-new-defect | 修正本身需要與原始程式碼同等的審查強度：enforce-metadata-schema 的 1 critical + 9 major 中 5 個由修正自身引入（偏離算術不成立、helper 插進 JSDoc 與宣告之間、`.loose()` 反而關掉 excess-property 檢查、`.loose()` 只作用頂層使 AC 過度宣稱、`satisfies` 未涵蓋 spread 成員）——若 review 在 round 2 收手，5 個全會流進 verify。對策：每輪修正後重跑完整 review lens，勿只複查被修的那一點 ｜ **Promotion**: PB-007 (strengthened, not a new entry), freq=3, modules=5, Approved-by: benwu95, 2026-07-31（正典規則見 `_playbook.md` PB-007 的 2026-07-31 強化段） | 7 | 6 (types,lib,services,templates,tests,cli) | playbook | enforce-metadata-schema, fix-cli-first-regressions, pin-windows-kill-semantics, delegate-module-adjudication, unify-req-heading-matcher, refuse-near-miss-feature-map, add-issue-link-field | promoted |
| workflow/review-provenance-stale-after-verify-sync | `computeChangeDigest` 雜湊 HEAD ＋ working-tree ＋ untracked（排除 `.prospec`）：改變更工件不 stale、改 source／tests／知識檔會 stale、**commit 本身必然 stale**。順序：內容定案 → commit → `--record-review` ＋ `--record-tests` → archive ｜ **Promotion**: PB-016 · Approved-by: benwu95, 2026-08-01（正典規則與 3 次再證見 `_playbook.md` PB-016 與 git 歷史） | 7 | 4 (lib,services,templates,cli) | playbook | align-language-policy-scope, split-verify-adjudication, filter-nonsource-modules, refuse-near-miss-feature-map, converge-req-body-boundary, guard-canonical-doc-drift, autonomous-drift-draft | promoted |
| gate/exemption-order-suppresses-known-failure | 機械閘門的分支順序決定豁免的實際範圍：`evaluateTestProvenance` 把 stale 分支排在 non-zero-exit 之前，於是「既 stale 又失敗」的 proven backfill 走進豁免分支，把**已記錄的失敗**吞掉——直接違反該 REQ 的粗體條款與模板的絕對句「never suppress a recorded non-zero exit」，而該維度現在逐字採用此 check 的狀態，agent 再也救不回來。規則：豁免必須逐分支而非迴圈頂端 `continue`；「已知的失敗」永遠排在「狀態未知」之前判定；並用一條在舊順序下會轉紅的測試釘住優先序；harden-verify-adjudication 第 2 度（高一層再犯）：collector 對不可解析 command 的 source-level early-return 在枚舉之前就吞掉已記錄的非零 exit——同一規則推上 collector/evaluator 邊界（不可用性降格為事實欄位、recorded-failure 判序最優先），review 另指出 digest-null 軸仍存同型（設計保留、待 follow-up） | 2 | 2 (lib,templates) | convention | split-verify-adjudication, harden-verify-adjudication | personal |
| security/swallowed-error-degrades-to-constant-fact | 被 `catch` 吞掉的錯誤再經 `?? ''` 塌成預設值時，會把「事實」變成常數而非誠實失敗（`gitCapture` maxBuffer 超限 → digest 退化成僅 HEAD sha，staleness 靜默關閉，`test-provenance` 在已改壞的程式上回報 PASS 且記為 machine）。規則：餵給閘門指紋的擷取失敗必須 fail-closed 回 null ｜ **Promotion**: PB-013 · Approved-by: benwu95, 2026-07-31（正典規則與 3 次再證見 `_playbook.md` PB-013 與 git 歷史） | 3 | 2 (lib,services) | convention | split-verify-adjudication, harden-verify-adjudication, restore-cli-first | promoted |
| perf/expensive-fact-recomputed-per-consumer | 同一輪內不可能不同的昂貴事實，被每個消費者各算一次：`collectReviewProvenance` 與新增的 `collectTestProvenance` 各自呼叫 `computeChangeDigest`，使每次 `prospec check` 跑 10 次 git（6–10 為 1–5 的逐位元重複，約佔 40% 執行時間；實測 whole-tree digest 408ms vs 純檢查 1080ms）。規則：新增第二個消費者時，把該事實提到編排層算一次再注入，而不是複製呼叫 | 1 | 2 (lib,services) | convention | split-verify-adjudication | personal |
| impl/model-of-external-tool-unverified | 對外部工具行為的「模型」未經一手來源查證就寫進實作／測試／文件 → 通常不是功能失效而是**閘門反向失效**（libuv 不讀 PATHEXT，使可正常工作的機器把 FAIL-class 閘門降為 skip）。一手來源**包含工具自己印出的 WARN 與 dry-run 行**，不只原始碼；註解寫對不代表實作跟上 ｜ **Promotion**: PB-012 · Approved-by: benwu95, 2026-07-31（正典規則與 6 次再證見 `_playbook.md` PB-012 與 git 歷史） | 6 | 5 (lib,tests,cli,templates,services) | convention | skip-unspawnable-test-command, restore-cli-first, add-windows-smoke-ci, pin-windows-kill-semantics, pilot-mutation-testing, delegate-module-adjudication | promoted |
| test/e2e-stale-dist-false-green | `pnpm test` 不自動 build（無 pretest hook）：改動 src 後 e2e 對舊 dist spawn 出假綠——collector guard 重排改變了可觀察的 skip reason，e2e 斷言本應轉紅，卻以 stale dist 宣稱全套件綠（SC-007 假陽性），review spec-architecture lens 重建 dist 實測 1 failed 才揭穿。規則：改動 src 後、宣稱全套件綠或跑 e2e 前先 `pnpm build`（tests README pitfall 已載但無 gate 防護——考慮 pretest hook 或 CI 順序即為 gate） ｜ **Promotion**: PB-018, Approved-by: ben.hy.wu, 2026-08-10 | 5 | 3 (cli,tests,templates) | playbook | harden-verify-adjudication, restore-cli-first, measure-all-load-surfaces, stop-silent-spec-body-loss, guard-canonical-doc-drift | promoted |
| test/assertion-encodes-implementation-not-rule | 斷言把「實作怎麼做」而非「規則是什麼」編碼進去，於是實作錯了斷言也跟著錯，缺陷被鎖進測試。本輪兩條：「較前目錄的 `.cmd` 勝出」與「自訂 PATHEXT 順序」—— 兩者都只是複述我那個錯模型的搜尋迴圈，mutation 交換迴圈嵌套只有一條轉紅，也就是整個排序故事都建立在錯的模型上。與 test/structural-false-green 的差別：那是斷言太弱，這是斷言指向錯的東西。規則：斷言的措辭要能對照外部規則來源，並補一條「該因素不影響結果」的負向斷言把模型釘死 | 1 | 2 (lib,tests) | convention | skip-unspawnable-test-command | personal |
| story/companion-doc-us-depends-on-capability-us | new-story advisory WARN：同一變更同時交付「能力」US（`prospec status` 命令）與「指向該能力的常駐文件」US（Session Start 指向）時，INVEST Independence 局部妥協——文件 US 須在能力 US 之後交付；以變更內分層實作順序（能力先落地、模板最後改指向）化解，advisory 不擋站（verify 3/5 判非違規）。evidence: `_archived-history/2026-07-29-add-status-router.md` ## Review & Verify | 1 | 2 (templates,cli) | convention | add-status-router | personal |
| test/fixture-shape-narrower-than-real-artifact | restore-cli-first：兩個 confirmed critical 同源——引擎只對照自造 fixture 而非真實產物。recountFeatureSpecCounters 只數 `### US-`（真實 spec 多為 `## US-`，finalize 會把 5 份 spec 的正確計數改錯）；findLedgerTable 在第一個空行截斷（真實 ledger 表中有空行，41 列只見 19，upsert 會重複建 key）。守則：解析真實 repo 產物的引擎，測試必須以那份真檔為 fixture（或加一條對真檔的對帳斷言），自造 fixture 只是補充 | 1 | 2 (lib,services) | convention | restore-cli-first | personal |
| refactor/wiring-orphan-exposes-latent-destruction | restore-cli-first：把長期無入口的 knowledge-update.service 接上 CLI，等於讓其未被檢驗的破壞性路徑立刻可觸及——updateModuleReadme 會用 skeleton 蓋掉 auto block 內的人工知識（正是 2026-07-05 解耦它的根因）、updateModuleMap 用 stringifyYaml 全量重寫吃掉 module-map 的手寫註解（dogfood 當場抓到）。守則：公開一個孤兒 service 前，先逐一稽核它的寫入點並縮限，不要假設「本來就在那裡」等於「安全」 | 1 | 2 (services,lib) | playbook | restore-cli-first | personal |
| docs/cli-generated-artifact-skips-language-policy | 把 artifact 產出委派給 CLI 後，工具語言中立、只寫輸入 JSON 帶的語言 → Summary 列全落英文而違反 `.prospec/changes/**` 的 artifact 語言（verify 3/5 判 FAIL）。責任回到提供輸入的一方，唯一耐久的落點是該 artifact 自己的 **format reference** ｜ **Promotion**: PB-014, freq=3, modules=4, Approved-by: benwu95, 2026-07-31（正典規則與 3 次再證的明細見 `_playbook.md` PB-014 與 git 歷史）；**已機械化**：`artifact-language` drift check（第 14 個 id）於每次 `prospec check` 回報缺少 artifact language 字跡的變更工件（WARN 級），上線首跑即抓到本專案 9 個真實遺留違反——本列自此有機器防線，保留為 provenance | 3 | 4 (templates,cli,lib,types) | convention | restore-cli-first, fix-cli-first-regressions, add-harness-capability-flags, add-artifact-language-check | promoted |
| archive/mechanical-merge-drops-req-body | archive 的機械 Feature Spec sync 以 `**Spec:**` 區塊**整段取代** MODIFIED REQ 的 body（無區塊則保留舊 body 而程式已前進），故畢業前必逐個 REQ 對照「合併後的檔案」而非 CLI 回報的檔案清單 ｜ **Promotion**: PB-015, freq=3, modules=3, Approved-by: benwu95, 2026-07-31（正典規則與 3 次再證的明細見 `_playbook.md` PB-015 與 git 歷史）；**部分機械化**：report-dropped-req-bullets 起，spec-sync 以集合差集回報 `**Spec:**` 未重述的既有 bullet（`droppedBehavior`）、Phase 3.5 逐條 gate——覆蓋 delete-by-replacement，但不覆蓋「ADDED 重用既有 REQ id」與「保留的 body 已陳舊」兩種形狀 | 3 | 3 (services,templates,tests) | playbook | restore-cli-first, fix-cli-first-regressions, add-harness-capability-flags, report-dropped-req-bullets | promoted |
| docs/generated-file-maintained-but-not-its-source | fix-cli-first-regressions：只維護生成檔（`index.md`／模組 README）不碰來源（`module-map.yaml`）——`prospec knowledge update` 一次重生就把計數回退。修法＝registry 為每個生成檔 occurrence 補上 field-scoped 來源 twin，並以「生成檔 == render(來源)」guard test 雙向釘住。守則：維護派生事實時，先問「這個檔是不是別人的輸出」；只修輸出＝下一次重生就回退（計數規則見 Constitution `[MUST] Factual Count Integrity`） | 2 | 4 (tests,services,lib,templates) | convention | fix-cli-first-regressions, converge-req-body-boundary | personal |
| test/platform-gated-assertion-never-executed | 以平台為條件跳過的測試（`describe.runIf(process.platform === 'win32')`）在該平台沒有 CI 時等於不存在：既無法證實作，又會讓相鄰的無條件斷言把另一平台的語意寫死而長期無人發現。本輪兩例：runIf 區塊從未執行過（新增 windows-smoke 後才首次跑）、`unspawnableReason('pnpm test') === null` 把 POSIX 前提寫成跨平台（Windows 上 pnpm 就是 .cmd shim，必紅）、`process.kill(pid,'SIGTERM')` 的訊號前提在 Windows 映射為 TerminateProcess | 1 | 2 (tests,lib) | playbook | add-windows-smoke-ci | personal |
| verify/ac-evidence-requires-external-run | 驗收條件的證據只存在於外部執行（CI job 的 log）時，verify 無法在評分時取得：該維度須誠實記 WARN 而非給分，且觀測任務必須標為 `[M]`（out-of-repo、無 diff），否則 code task 未勾 → task-completion FAIL → 不能 verify，而 CI 又只在 commit/PR 後才觸發，兩者互為前提造成死鎖 | 1 | 1 (tests) | playbook | add-windows-smoke-ci | personal |
| spec/landing-block-states-delta-not-result | `**Spec:**` 落地區塊會整段取代既有 REQ body，所以必須陳述「變更後的完整需求」而非本次差異；且它是唯一會畢業的欄位——把訂正打在 `**Description**` 等敘事欄等於沒改，不實陳述仍會進信任區且工件自相矛盾 ｜ **Promotion**: absorbed into PB-015 (Strengthened 2026-08-03) · freq=3, modules=4 · approved-by benwu95 · 2026-08-03（正典規則見 `_playbook.md` PB-015 的 2026-08-03 強化段） | 3 | 4 (services,templates,lib,types) | convention | add-harness-capability-flags, filter-nonsource-modules, mechanize-light-scale-gates | promoted |
| template/conditional-branch-contradicts-unconditional-prose | 把能力判斷收斂進 `{{#if}}` 分支時，同一文件其他位置的**無條件**祈使句不會跟著變——false 分支渲染出「本 harness 沒有 X」與「請使用 X」並存的自相矛盾文件。本輪三處（The Loop 的 spawn 步驟、Reviewer Mode A、verify 2/5 的 spawn 指示），section-scoped 斷言全部看不到。修法：站點散文改為不指名機制、機制只由 partial 敘述，並加「兩個分支下 partial 之外皆不得出現該祈使句」的守衛 | 1 | 2 (templates,tests) | playbook | add-harness-capability-flags | personal |
| review/fix-introduces-same-class-defect | 修正 review findings 的過程本身會引入**同類**缺陷，且修正者的自信最高、最不會回頭驗。規則升級：修正要針對缺陷的**類別**而非被回報的那個實例（先問「同族還有誰」與「我是否只是把失敗換了個形狀」），並把修正本身當成新 diff 送回對抗式重驗 ｜ **Promotion**: PB-007 (strengthened, not a new entry), freq=3, modules=3, Approved-by: benwu95, 2026-07-31（正典規則見 `_playbook.md` PB-007 的 2026-07-31 強化段；6 次再證的明細見 git 歷史）；最近再證：pilot-mutation-testing 第 3 度——同一句因果宣稱連錯三版，且 r1 的修正只改了 finding 點名的檔案，`tasks.md` 兩處舊數字與五個規劃站點的舊設計全數存活 | 11 | 6 (services,cli,tests,lib,templates,types) | playbook | report-dropped-req-bullets, add-artifact-language-check, pilot-mutation-testing, restrict-identity-fallback, enforce-counts-in-ci, extend-provenance-audit-scope, add-learn-staleness-sweep, land-memory-only-knowledge, refuse-near-miss-feature-map, separate-review-evidence, autonomous-drift-draft | promoted |
| spec/req-scope-not-listed-modified | 改動落在某條既有 REQ 的宣告範圍內時，必須把它列為 MODIFIED，否則畢業後信任區會繼續描述舊行為而與出貨產物矛盾。report-dropped-req-bullets 改了 `delta-spec-format.hbs` 段落與 `prospec-archive.hbs` step 0——兩者都是 REQ-TEMPLATES-166 的宣告範圍——卻未列 MODIFIED，2/5 fresh-context 評分者揪出。判斷方式：對每個被改的產物問「哪條既有 REQ 宣稱它的行為」，而不是只看自己新增了什麼 | 5 | 5 (templates,services,lib,types,tests) | convention | report-dropped-req-bullets, mechanize-light-scale-gates, mechanize-knowledge-sync-gate, detect-inlined-gate-desync, capture-session-corrections | promoted |
| check/vacuous-pass-worse-than-crash | 掃描類 check 遇到不可讀來源時，靜默略過比直接崩潰更糟：崩潰會被看見，空洞 PASS 會被當成「已驗證」。add-artifact-language-check 三輪都踩在同一點上，最後一版才改為整個來源降為 `{available:false, reason}` 並指名該 root。規則：collector 對任何讀不到的輸入一律降級為帶原因的 skip，絕不縮小樣本後照常回報——報告必須能區分「掃過且乾淨」與「根本沒看」。**第五輪的收斂點是改寫宣稱的形狀，不是再補一條路徑**：「不會空洞通過」被連續四輪推翻（fs throw → ScanError throw → catch{continue} → containment／不可讀檔 → symlink 條目／DEFAULT_IGNORE 目錄名），因為 canonical scanner 有一組列舉不完也不該複製的過濾器。改成枚舉四個**記錄在案**的條件、並明說其餘與真正缺席無法區分後即穩定。規則：當一個保證連續被推翻，先檢查它是不是寫成了實作結構上無法兌現的**全稱**形式——修不完的往往不是缺陷，是宣稱 | 1 | 2 (lib,services) | convention | add-artifact-language-check | personal |
| scan/name-based-detection-leaks-by-orthography | 以「語言名稱字串」決定書寫系統，必然在正寫法變體上漏：`Serbian (Latin)`／`Hindi (Romanized)`／`日本語（ローマ字）` 都會被判成基礎語言的文字，對以拉丁字母書寫該語言的專案 100% 誤報。逐名特例只擋一個成員，需規則式（宣告拉丁正寫法即視為不可判定）並涵蓋非 ASCII 的宣告方式。根本解是 Unicode 屬性比對而非名稱比對 | 1 | 1 (lib) | playbook | add-artifact-language-check | personal |
| docs/broadcast-edit-needs-per-site-verification | 把同一個措辭修正推廣到多個檔案時，必須逐檔驗證結果，不能假設「同一個改寫」自動正確。add-artifact-language-check 第五輪把定義式改寫推到七份文件，其中兩份 README 反而被寫進了全稱宣稱與「never a vacuous pass／絕不空過」——正是那五輪花力氣撤回的句子，也正是 ledger 記載一再被證偽的措辭。推廣式編輯的失敗是靜默的：每個檔案單獨看都通順；pilot-mutation-testing 第 2 度 ×2：撤回的成本數字散落七處，兩輪修正各漏一批（r1 漏 tasks.md、r2 漏 proposal／plan／tasks 的五個設計站點）。**推廣式撤回比推廣式改寫更容易漏**，因為被撤回的措辭在每個站點的形態都不同 | 2 | 2 (docs,lib) | convention | add-artifact-language-check, pilot-mutation-testing | personal |
| test/assertion-pins-wording-not-invariant | 守衛釘的是某一種措辭而非不變量，於是同義改寫即可繞過而無任何失敗。pilot-mutation-testing：AC 要求「該規則不得以帶嚴重度的列存在於準則表」，我用負向 grep 釘關鍵字；reviewer 僅把 `name the mutations` 改成 `list the mutations` 就讓缺陷完整復原且全套綠。與 test/structural-false-green 的差別：那是斷言太弱，這是斷言把**規則的一種表述**當成規則本身。規則：凡「某物不得出現」的約束，釘集合或結構（item-set vs 版控基準、closed enum、目錄列舉），不要釘關鍵字——關鍵字的涵蓋面等於作者當下的詞彙量 | 3 | 5 (tests,templates,lib,services,cli) | convention | pilot-mutation-testing, converge-req-body-boundary, autonomous-drift-draft | promoted |
| scan/grep-silently-misses-non-ascii | 本 shell 的 `grep -rn` 會靜默漏掉含 CJK 的行——回報「沒找到」而非「讀不了」，於是空結果被當成「已清乾淨」的證據。pilot-mutation-testing：r1 修正後用 `grep -rn` 掃描殘留舊數字並宣稱完成，r2 以 locale-safe 掃描找到 tasks.md 兩處與五個規劃站點全數存活。規則：對非 ASCII 工件做完整性掃描一律用 Python／ripgrep 逐行 UTF-8 讀取，`grep -rn` 的空結果不得作為完整性證據——與 check/vacuous-pass-worse-than-crash 同型：空結果與真正缺席無法區分時就不能當保證 | 1 | 0 | playbook | pilot-mutation-testing | personal |
| design/rule-placement-follows-its-subject | 規則放錯層級會得到一條沒有施力點的建議。pilot-mutation-testing：「宣稱做過 mutation 驗證者須列出變異」被放進 review 的 test-quality 準則表並標 major，但 critical/major 依 `review-format` 定義是對**變更**提出的 finding 的權重——而該規則的主體是 **reviewer 自己的產出**，那個 major 無處可填。移入 finding 內容規則後才有落點。同輪衍生：跨檔路標必須**具名**指涉，位置式的「the row above」會隨增列無聲漂移到錯的對象。規則：寫一條規則前先問「它的主體是誰」——管產出格式的歸格式契約，管變更的才歸帶嚴重度的準則表 | 1 | 2 (templates,tests) | convention | pilot-mutation-testing | personal |
| test/mutation-verify-must-hit-the-deployed-copy | 契約測試經 renderTemplate 讀 BUNDLED_TEMPLATES（先於檔案系統），所以只改磁碟上的 `.hbs` 再跑測試＝假綠：mutation 沒有到達受測物。本輪第一次 mutation-verify 因此誤判 guard 存活，補 `pnpm bundle` 後同一 mutation 立刻轉紅。凡受測物有「來源→部署／打包」兩層者（templates→bundle、src→dist、.hbs→已部署 SKILL.md），mutation 必須套在測試真正讀取的那一層，或在套用後先重建 | 2 | 3 (tests,templates,lib) | playbook | enforce-sub-module-budget, skill-template-partials | personal |
| test/skip-fixture-does-not-discriminate-its-guard | 一個「略過 X」的測試若 fixture 同時被更前面的條件排除，被測的那道 guard 從未被鑑別——本輪 subdirectory 案例取名 `diagrams`（不帶 `.md`），`.md` 過濾早已排除它，刪掉 `!e.isDirectory()` 全綠存活。多條件 filter 的每個 conjunct 都要有只違反它一個的 fixture（此處＝名為 `README.md` 的目錄），並逐一 mutation 確認各有專屬 killer | 2 | 2 (tests,lib) | convention | enforce-sub-module-budget, add-learn-staleness-sweep | personal |
| fix/tightened-guard-drops-a-measurement-fail-open | 為了安全而收緊的過濾條件可能靜默移除一項量測，讓 gate 由 fail closed 變 fail open：`e.isFile()` 排除 symlink，使 symlink README 整檔退出 knowledge-size，而舊路徑（canonical reader 的 realpath containment）本來會量到並可能發 warn。containment 屬於讀取端（realpath 拒絕逃出樹外），走訪端不該重複判斷——在走訪層擋 symlink 買不到安全，只買到少一筆量測 | 1 | 1 (lib) | convention | enforce-sub-module-budget | personal |
| archive/us-level-spec-text-has-no-graduation-carrier | `**Spec:**` 區塊只替換 `#### REQ-` 的 body，因此變更若推翻了 User Story 層的敘述（`### US-` 的 I want、Acceptance Scenario、SC-N），archive 後 feature spec 會與它自己底下的 REQ 互相矛盾，且沒有任何自動載具或檢查會攔下。做法：在 delta-spec 建「Phase 3.5 手動收斂清單」逐行列出 `file:line` 與應收斂內容（本輪 8 列），並在 graduation 時逐條核對——獨立審查者兩度在此清單上找到漏列，說明清單本身也需要對抗式檢查 ｜ **Promotion**: 吸收進 PB-017 作為強化條款（非新增條目），Approved-by: benwu95, 2026-08-03；score detail: frequency=3 · impact_modules=2 · kind=playbook · rule=freq≥3 ∧ modules≥2 ⇒ suggest | 4 | 4 (templates,services,lib,types) | playbook | enforce-sub-module-budget, extend-provenance-audit-scope, add-learn-staleness-sweep, mechanize-knowledge-sync-gate | promoted |
| docs/frozen-contract-reference-omits-the-new-field | 凍結契約新增 additive 欄位時，除了 schema 與評估器，還要更新那份「agent 被明文要求據以重現、而非逆推 JSON」的出貨 reference（`drift-report-format`）與其部署副本；漏更新會讓可重現性承諾在使用者實際查的文件上不成立。durable fix：契約測試從 Zod `shape` 推導期望鍵集合並 scope 到 fenced jsonc block——比對散文提及不足以攔下「從 shape 刪鍵、留說明文字」這種形狀 | 1 | 3 (types,templates,tests) | convention | enforce-sub-module-budget | personal |
| fix/graceful-degradation-is-not-uniform | 把「讀不到＝視為缺席」套成一條全域規則會踩到一類例外：某些檔案的缺席本身就是一個決策開關。module-map.yaml 讀不到若退化成 null，check.service 會靜默改用 constitutionFallbackRules() 並對著存在的檔案回報 not found——dependency-direction 就以錯誤 ruleset 出裁決。做法：讓讀取結果帶 reason（absent／escaped／unreadable），content 讀取 graceful、治理檔案 unreadable 則 loud，並讓 loud 走既有的 typed error（帶 code 與可行動建議），而不是讓原始 EISDIR 逃出去 | 1 | 2 (lib,services) | convention | harden-contained-reads | personal |
| test/fixture-must-reach-the-code-under-test | 「略過讀不到的檔案」用目錄當 fixture 兩度空轉：一次是 .md 過濾早已排除它（enforce-sub-module-budget），一次是 glob scanner 的 onlyFiles 在讀取前就濾掉（本輪 collectMarkdownLinks／collectReqReferences）——兩次都是 mutation 存活才發現。寫略過類測試前先問「這個 fixture 會不會在抵達被測分支前就被前面的條件吃掉」，並用能真正觸發該分支的形狀（權限撤銷的真實檔案，POSIX 閘控） | 3 | 3 (tests,lib,services) | convention | harden-contained-reads, separate-review-evidence, unify-line-splitting | promoted |
| fix/sweep-the-whole-failure-class-not-the-reported-line | 修一個「讀取失敗會中止整場」的缺陷時，被回報的那一行只是該類別的一個實例：本輪 knowledge-reader 修好後，同檔案還有六個 collector 以裸 readFileSync 讀 readdir 列出的檔案，一個名為 oops.md 的目錄同樣讓 prospec check 整場拋 EISDIR（collector 是 runChecks(...) 的參數，一throw 帶走其餘 13 個 verdict）。修完點名處要 grep 同型呼叫並逐一判定，且以凍結計數（readFileSync 在該檔出現次數）把新增的第二份實作擋在門外——per-function 的結構性 guard 擋不住 | 1 | 2 (lib,tests) | playbook | harden-contained-reads | personal |
| archive/change-history-row-labels-the-change-by-hand | archive.service 的 appendToChangeHistory 把 Change 欄寫死成字串 archive-sync（src/services/archive.service.ts:1206，該函式未收到變更名），而歷史上每一列都是真實變更名——那是 graduation 時人工收斂的結果。Phase 3.5 要把自己那幾列改成變更名＋一句影響摘要，否則 feature spec 的 Change History 會退化成一串 archive-sync 而失去追溯價值。根治是讓 CLI 收變更名 | 1 | 1 (services) | playbook | harden-contained-reads | personal |
| fix/thread-the-value-through-every-sibling-branch | 把缺失的參數接上去時，只接「被回報的那一條」路徑等於把同一個缺陷留在姊妹分支：本輪 syncToFeatureSpecs 的 if 有兩條寫入路徑，appendToChangeHistory 修好了，else 的 createNewFeatureSpec 仍硬寫另一個佔位字串（initial-sync），且該行零測試覆蓋——改成任意字面 566 個測試全綠。做法：修完一條路徑，grep 同一個輸出形狀（此處＝Change History 列）的所有產生點，逐一接上並各補一條測試 | 1 | 2 (services,tests) | convention | name-change-history-rows | personal |
| test/pin-the-branch-production-actually-takes | 唯一的正負向斷言長在 fallback 分支上，而所有真實資料都走另一條：本輪 appendToChangeHistory 的斷言掛在「Change History 沒有表頭分隔列」的 EOF fallback 測試上，而 7 份 feature spec 全部滿足 in-table 插入條件——只改 in-table 那條 push 的突變存活。寫斷言前先確認 fixture 走的是哪一條分支，並讓產線路徑本身有一條 section-scoped 測試（本輪同一個坑在轉義測試上又踩一次：fixture 沒有既存 spec，於是只走建檔路徑） | 1 | 2 (tests,services) | convention | name-change-history-rows | personal |
| data/attribute-history-by-two-independent-keys | 回填歷史紀錄的歸屬不能只按單一相似度排序：本輪 15 列 Change History 若只按 REQ 重疊數排名，project-setup.md:664 會被歸給五天前引入那些 REQ 的變更（它的摘要也提到同一批 REQ）。可靠規則是兩個獨立鍵的合取——日期硬過濾候選，再要求該摘要的 Requirements 表涵蓋該列全部 REQ；非唯一解就保留原值並說明，絕不猜。改前產生對應表供人確認、改後以同規則重新推導比對，兩次執行分離才不是用寫入結果驗證寫入 | 1 | 1 (services) | playbook | name-change-history-rows | personal |
| test/verify-the-mutation-actually-applied | mutation-verify 若沒確認突變真的落到檔案上，得到的綠燈毫無意義：本輪用 perl 套一個含 ${} 與反引號的替換沒有命中，測試對著未變異的程式跑出 1 passed，我一度把它記成 survived。做法：每個 mutation 套用後先 grep 目標字串或斷言 replace 前後不同（python 的 assert new!=s），再跑測試；工具鏈已有前例——契約測試讀 bundled templates，突變也必須先 pnpm bundle 才會抵達受測物 ｜ **Promotion**: PB-019, Approved-by: ben.hy.wu, 2026-08-10 | 4 | 2 (tests,lib) | convention | name-change-history-rows, delegate-module-adjudication, measure-all-load-surfaces, stop-silent-spec-body-loss | promoted |
| heuristic/allowlist-erases-the-unlisted-member | 分類啟發法用「白名單＋未列出者排除」時，遇到清單沒預料到的成員會抹除真實內容，而非只是少認一個：本輪 module 偵測以原始碼副檔名白名單窄化，加上「子集為空才退回」的全專案判準，使語言不在清單上的專案丟失全部程式碼目錄——Fortran 專案（docs/ 內剛好有兩支 .sh）偵測結果只剩 `['docs']`，比不過濾更糟。極性應反轉為拒絕清單（列出明確不算的），讓未知落在「保留」一側，失誤方向變成多一個而非全滅。另一實測教訓：放寬分類是**非單調**的，因為它可能讓專案跨過門檻而關掉救援（dotfiles repo 由 `[bin, zsh]` 掉成 `[bin]`）——追求分類精確度的報酬遞減甚至為負 | 1 | 1 (lib) | convention | filter-nonsource-modules | personal |
| fix/rescue-condition-keys-on-input-not-outcome | fallback／救援的觸發判準要對準**結果**而非**輸入**：本輪「子集為空才退回完整清單」被「子集非空但太薄」繞過——k8s manifest 專案只有一支 CI script，沒有任何目錄達到門檻，偵測出零個 module，而 `knowledge init` 只在檔案不存在時寫 module-map.yaml，`modules: []` 於是永久黏著（已以端到端實測確認）。判準改為 `dirModules.length === 0` 後同時涵蓋兩種情形。通則：救援條件寫在輸入側時，任何「輸入非空但不足」的形狀都會穿過；且下游只寫一次的檔案會把一次性錯誤變成永久狀態 | 1 | 2 (lib,services) | convention | filter-nonsource-modules | personal |
| test/fallback-restores-the-result-making-the-assertion-tautological | 被測邏輯帶 fallback 時，針對「某類輸入被排除」的測試若 fixture 全屬該類，排除整類會讓 fallback 還原原本結果，斷言恆真：本輪 template／樣式測試的 fixture 只有 `.hbs`/`.vue`/`.scss`/`.css`，把整組加入拒絕清單後子集清空、fallback 還原完整清單，三個目錄照樣被偵測，測試全綠——恰好放過 proposal 指名要防的那個回歸（移除單一副檔名才會轉紅，故有部分敏感度而更難察覺）。做法：fixture 混入不屬該類的資料讓 fallback 不觸發，並以負向斷言承接（本輪加兩個純文件檔後，變異令 `docs` 出現而轉紅）。與 test/skip-fixture-does-not-discriminate-its-guard 機制不同：那是前置條件先排除 fixture，這是後置 fallback 還原結果 | 1 | 1 (tests) | convention | filter-nonsource-modules | personal |
| verify/checked-v-task-certifies-a-wrong-figure | 已勾選的 `[V]` 驗證任務可以攜帶錯誤數字通過所有機器閘門：drift 的 task-completion 與 verify 1/5 只讀 checkbox 狀態，不讀斷言內容。本輪 T13 勾選並認證「olfparser 16 → 10」，實測是 16 → 9（移除 7 個）——proposal 的 SC-002 已據實訂正，plan 與 tasks 卻殘留舊數字，由獨立審查者揪出。做法：訂正任何實測數字後 grep 全部工件（proposal／plan／tasks／summary）逐處收斂，且 `[V]` 任務的描述本身要視為需驗證的斷言而非已完成的證據 | 1 | 2 (templates,services) | playbook | filter-nonsource-modules | personal |
| design/spec-already-delegates-the-decision | 使用者一句提問（「檔名和 module 資料夾的判定沒辦法用 LLM 即時決定嗎」）推翻了我整個問題框架。issue #114 列出四張硬編碼表與四個方向，我照著挑了「哪一種啟發法調整最好」——但規格早已把裁決權指派給另一層（US-301 標題就是 AI-Driven Module Detection、REQ-KNOW-003 寫 AI 自行決定 module 邊界）。查證後真正的缺口不是啟發法不夠好，而是那一層**看不見裁決、也沒被授權推翻裁決**：raw-scan.md 的 Directory Tree 本來就顯示所有目錄，缺的是「這個目錄是被人工排除還是被拒絕清單默默丟掉」的可分辨性，以及 skill 從未被授權增刪 module-map 條目。**可操作的推論**：面對一個啟發法的缺陷，先查規格有沒有把這個決定指派給別的層；若有，正確的軸線是「那一層有沒有證據、有沒有權限」，而不是「啟發法怎麼調更準」——後者會把應該上移的判斷永久固化在錯的層 | 1 | 3 (lib,templates,services) | playbook | delegate-module-adjudication | personal |
| learn/invented-source-name-inflates-frequency | 為 archive 後、merge 前的一筆修復 harvest 教訓時，我用了一個自己發明的 source_change 名（`guard-manifest-derived-values`）——那不是一個 change，是同一個 change 同一個 PR 的後續 commit。`learn upsert` 的 `frequency` 數的是 **distinct source changes** 且只做聯集，於是兩個既有 key 各被灌水 1（7 應為 6、11 應為 10）。兩者本已 promoted 所以未改變任何晉升決定，但這破壞了 promotion-format 明文保證的「同一 ledger ⇒ 同一決策」——計數是那個保證的唯一輸入。而 CLI 沒有撤回路徑（只聯集不移除），修正只能手改 ledger。**可操作的推論**：`source_change` 必須是真實存在的 change 名（`.prospec/archive/` 下有對應目錄），同一個 change 內的第 N 次復發寫進 description 而非新增一個來源；harvest 前先確認該名字存在 | 1 | 2 (lib,services) | playbook | delegate-module-adjudication | personal |
| tooling/installed-binary-lags-source-corrupts-artifact | 以安裝版執行檔跑自己正在修的那條程式路徑，會讓缺陷污染本次變更的工件本身：本輪用安裝版 `prospec review merge`（1.0.0，早於本修復）併入第二輪發現，`F-12` 被併進 `F-6` 那一列 —— issue #116 的缺陷在自己的修復流程中當場重演，稽核軌跡少一筆且無任何錯誤訊息。規則：當變更修的是 CLI 自身行為時，該變更流程內所有相關 `prospec` 指令一律改跑 source（`pnpm cli …`）；受污染的工件要以 source CLI 從輸入重建，而非手動補列。 | 4 | 2 (cli,lib) | convention | restrict-identity-fallback, enforce-counts-in-ci, mechanize-knowledge-sync-gate, prompt-trust-zone-language-at-init | promoted |
| spec/absolute-consequent-contradicts-sibling-bullet | 同一個 REQ 內把驗收 bullet 的後件寫成絕對句，會與同 REQ 的另一條 bullet 互斥並一起畢業進信任區：本輪 REQ-CLI-028 的「id-less finding opens its own row」在同鍵還有未認領 pre-round 列時不成立（該由 bullet「取表序第一個未認領者」接手），REQ-TEMPLATES-066 的「never deduplicated by Location」則與 REQ-CLI-028 的 location+lens 退回比對正面衝突 —— 兩者都由 review／verify 的獨立評分者抓出。規則：寫 Spec bullet 時，後件要標明它的前提範圍；改寫既有 bullet 後必須把同一 REQ 的所有 bullet 兩兩掃過一次矛盾。 | 1 | 2 (templates,lib) | convention | restrict-identity-fallback | personal |
| verify/judgment-dimension-catches-what-review-missed | verify 2/5 判 FAIL，抓到四輪對抗式 review 都沒抓到的 critical。兩者問的問題不同：review 問「這個斷言擋得住什麼」，於是沿著斷言的設計意圖去試；2/5 問「即將原樣畢業進信任區的那句話是不是真的」，於是照**受測對象唯一的實際書寫方式**去試（該 block 每行指令都有縮排，而斷言錨在第 0 欄）。結論：judgment 維度不是 review 的複查，它的提問角度無法被更多輪 review 取代 —— 別因為 review 已跑很多輪就把 2/5 當形式。 | 3 | 4 (templates,tests,services,lib) | playbook | enforce-counts-in-ci, add-learn-staleness-sweep, converge-req-body-boundary | promoted |
| gate/mechanism-exists-but-nothing-runs-it | 完整的檢查器存在但不在任何 workflow 裡執行，而出貨 REQ 的驗收情境已宣稱它通過。「機制存在」被當成「機制在跑」。規則：宣稱某條性質成立時，要指出**誰在什麼時候執行它**；一個沒有執行者的檢查器等於沒有這道閘門（具體案例與修復見 Constitution `[MUST] Factual Count Integrity`） ｜ **Promotion**: 吸收進 PB-003 作為執行面的強化條款，Approved-by: benwu95, 2026-08-03；score detail: frequency=3 · impact_modules=6 · kind=convention · rule=freq≥3 ∧ modules≥2 ⇒ suggest | 4 | 6 (tests,cli,types,lib,services,templates) | convention | enforce-counts-in-ci, mechanize-light-scale-gates, add-learn-staleness-sweep, read-specs-by-req | promoted |
| harness/fresh-context-delegation-blocked-by-default | Claude Code 2.1.219+ 對 Opus 5 硬編碼注入「未經要求不呼叫 Agent tool」（`iMy(model)`，無 env override），與 `/prospec-review` 的 fresh-context reviewer 前提正面對撞：本變更的 review 與 verify 2/5 皆改走降級的 in-context 路徑，審查者＝實作者、獨立性喪失，且 verify 因該揭露 WARN 使 grade S 不可達（實得 A）。降級本身有明文揭露機制、不是靜默跳過，但代價每次都付。作法：使用者輸入 `/prospec-review` 本身即視為委派授權，不確定時在該回合明說一次；要永久解除則在 CLAUDE.md 或 `--append-system-prompt` 寫入常態授權——沒有設定開關可找 | 1 | 1 (templates) | playbook | exclude-generated-from-staleness | personal |
| docs/copies-pinned-to-each-other-not-to-code | 雙副本文件（_status-lifecycle.md 與其 init 模板）原本只有副本↔副本的契約斷言，那只能證明兩份文件一致，證明不了任一份與程式碼一致——REQ-TYPES-070 因此宣稱 SDD_STATIONS 與 lifecycle 文件相符，而該文件從未載有站點順序，信任區副本漏掉 design 多年無人察覺。規則：文件裡每一條會被程式碼實現的結構化事實（順序、集合、矩陣），要以可解析形式承載並與程式碼常數雙向集合比對；副本間同步是額外的一層，不是替代品 | 2 | 3 (tests,templates,types) | convention | mechanize-light-scale-gates, refresh-v2-documentation | personal |
| gate/check-scope-excludes-the-state-it-guards | review-provenance／test-provenance 兩個 evaluator 都以「status 不是 implemented 就 continue」開頭，所以變更一旦是 verified，兩道閘門就對它視而不見——verify 之後改程式碼（本輪為新增 promote 站）不會讓任何 baseline 轉紅，實測記錄 digest 與現行 digest 已不同而 check 仍全 PASS。與 gate/mechanism-exists-but-nothing-runs-it 的差別：機制有在跑，但它的稽核範圍剛好排除了最需要它的狀態。規則：閘門的狀態過濾條件要對照「它要防的事會在哪些狀態發生」，而不是「它平常在哪個狀態被呼叫」 | 2 | 3 (lib,services,tests) | playbook | mechanize-light-scale-gates, read-specs-by-req | personal |
| knowledge/budget-headroom-measured-in-wrong-unit | 向使用者揭露 knowledge-size 超標時，我用 `wc -c`（位元組）除以 4 去推基線餘裕，得到 48／5；但估算器是 `Math.ceil(charLength/4)`（字元），實際餘裕是 80／2。這兩份文件含大量 em dash 等三位元組字元，誤差因此達 30 token 以上，導致揭露列裡三個數字全錯，且把 385 token 的增量全歸給表格而漏記 135 token 來自 review 輪次補上的散文。規則：引用任何閘門的量測值時，用該閘門自己的估算器重算（`src/lib/token-accounting.ts`），不要用 shell 的位元組計數代替——單位不同的近似在含非 ASCII 的檔案上會直接錯到結論之外。 | 2 | 2 (lib,tests) | convention | extend-provenance-audit-scope, add-issue-link-field | personal |
| test/mock-state-leak-mimics-mutation-kill | 共用 mock 未在觸發受測動作前隔離（缺 `mockClear()`），跨測試累積的呼叫紀錄會產生偶發紅燈，而這個紅燈與真正的 mutation kill 完全同形——於是 PB-001 的 mutation-verify 訊號失去鑑別力：假紅會被當成 guard 生效，真紅也可能被當 flake 忽略。本輪 REQ-AGNT-023 的 entry-config 排除測試即為此類 one-time false-red，review 判 major 並以 `mockClear()` 修正。規則：mutation-verified guard 的斷言必須先重設共用 mock／模組狀態，讓「紅」只能由受測行為造成。 | 2 | 2 (tests,services) | convention | add-quickstart-command, prompt-trust-zone-language-at-init | personal |
| data/programmatic-yaml-write-back-drops-comments | skill 模板指示 agent 改寫使用者擁有的 `.prospec.yaml` `skill_triggers` 時把它當可重生產物，兩個 major 同根：失敗復原寫「restore prior skill_triggers」但檔案一旦被寫成不可解析就沒有來源可讀回，該指示不可執行；自由重新序列化整份 YAML 又會掉使用者註解與鍵序。修法是先取 pre-write snapshot、復原時逐字回寫，並以最小 in-place 編輯保留其餘鍵與註解。 | 2 | 3 (templates,services,lib) | convention | add-quickstart-command, preserve-curated-index-columns | personal |
| refactor/relocation-needs-migration-for-existing-installs | 搬動一個已出貨 artifact 的路徑時只改「產生端」不算完成：已 init 過的下游專案手上仍是舊路徑的檔案，新程式讀不到、舊檔成孤兒。本輪把 `_index.md` 提升為 `prospec/index.md`，必須在 `prospec-upgrade.hbs` 補一段 Index Migration（偵測舊檔 → 搬到新位置 → 保留 `prospec:user-start` 區塊 → 以 `prospec knowledge update` 重生 auto 區塊 → 刪除舊檔）。規則：relocation 的定義包含既有安裝的遷移與孤兒清除，且該步驟要落在 upgrade 流程裡可執行，而非只存在於本 repo 的 diff。 | 1 | 2 (templates,services) | playbook | implement-hierarchical-index | personal |
| archive/bundle-omits-the-finding-substance | 封存工件把「有發現」記錄成欄位而非內容，事後即無法審計：本變更的 metadata.yaml 只有 `status: archived` 與 `scale: full` 兩行（完全沒有 quality_log），review.md 唯一一列 critical 只填 Location／Severity／Lens／`resolved` 而無任何敘述，summary.md 也沒有 `## Review & Verify` 節——一個月後只能靠逆推 diff 猜那個 confirmed critical 是什麼。規則：review 列必須寫下發現本身與修法（不只 severity／status），archive summary 必須帶 Review & Verify 證據節；審計軌跡的價值在內容，欄位齊全不等於留證。 | 1 | 2 (services,templates) | playbook | implement-hierarchical-index | personal |
| test/equivalence-assertion-bypasses-the-render-path | 驗證「單一來源會傳播到生成檔」的等價性測試，若直接拿來源字串比對模板文字，就跳過了真實的序列化路徑：逸出缺陷（`:`、backtick、`'`、`→`、`5+1 (S/A/B/C/D)` 這類內容）會讓實際 SKILL.md 的 frontmatter 變成不合法 YAML，而測試照樣全綠。修法是等價性測試改為經 `escapeYamlScalar` render、再 parse 回來與原始 `skill.description` 做 round-trip 比對。規則：single-source 等價性斷言必須走真實 render＋escape＋parse 的往返，不可比對未經序列化的字串。 | 1 | 3 (tests,templates,types) | convention | agent-sync-hygiene | personal |
| docs/hand-written-field-path-drifts-from-frozen-schema | frozen schema（`src/types/drift-report.ts` 描述的 `prospec-report.json`）沒有任何單一記載處，於是每個 skill 作者各自手寫欄位路徑並逐步偏離真實結構：`prospec-learn` 與 `promotion-format` 引用了 schema 裡不存在的 `knowledge_health.stale[]`，讀取只得到 undefined，knowledge-freshness 優先序功能**靜默失效**。錯誤的欄位路徑永遠不噴錯，只讓功能無聲消失。規則：機器產物的結構須有一份出貨 reference 作單一來源，並以會變紅的契約測試把 reference 釘回 schema 常數。 | 1 | 3 (templates,types,tests) | playbook | document-drift-report-contract | personal |
| test/helpers-tested-but-not-the-orchestration-order | 新 lib helper（`index-table.ts` 的 buildIndexRow/buildIndexTable/backfillCuratedFromIndex，8 支單元測試）全綠，但真正承載正確性的不變式住在呼叫端：`knowledge-update.service` 必須先 backfill 寫回 module-map、再 collectAllModules 重讀、最後 regen；順序顛倒會讓回填被覆蓋，而 helper 層測試仍全綠。修法是補一支 execute() 層測試把 backfill→persist→regen 順序、no-clobber、幂等一起釘住。規則：順序與幂等這類不變式的保管者是 orchestrator，必須在其入口層加斷言，helper 覆蓋率不能代替。 | 1 | 3 (services,lib,tests) | convention | preserve-curated-index-columns | personal |
| tooling/nul-byte-makes-git-treat-file-as-binary | `tests/unit/cli/check-output.test.ts` 在 offset 708 帶有一個 pre-existing NUL byte，git 因此把它當 binary：`git diff` 只印 Binary files differ，於是所有以 diff 為輸入的稽核（對抗式 review、verify 的 diff 掃描）對該檔的變更完全看不見，本輪 reviewer 必須改用 `git diff --text` 才能確認那一行編輯真的存在。規則：稽核 diff 時若有檔案被 git 標成 Binary，不得當成沒有變更跳過——以 `git diff --text` 取回內容並查明二進位成因，否則該檔的 hunk 會永久逃過審查。 | 1 | 1 (tests) | convention | include-tests-in-typecheck | personal |
| verify/bulk-fidelity-needs-mechanical-invariants | 大批量同型工件轉換（10 份 feature spec 繁中→英，由並行 subagent 執行）的語意保真無法全量深讀，於是「只改語言、需求一字不改」這句宣稱本身沒有證據：本輪只深讀 4/10 檔 prose，其餘 6 檔靠機械訊號覆蓋，verify 因此誠實記 WARN 並取 grade A 而非 S。規則：這類變更的主要證據應是覆蓋 100% 檔案的機械不變式（書寫系統 grep 命中=0、逐檔 REQ-ID 集合 diff 為空、行數＝HEAD+1 的逐行 parity、數字 token 多重集相同、frontmatter 逐值不變），深讀退為抽樣；抽樣比例與殘餘風險須寫進 quality_log 並反映在等級上限。 | 1 | 0 | playbook | translate-feature-specs-to-english | personal |
| tooling/revert-by-checkout-destroys-uncommitted-work | mutation-verify 的還原方式用 `git checkout -- <file>`，在「整個變更尚未 commit」的狀態下等於刪掉自己剛寫的實作——本輪連毀兩次（src/lib/lessons-ledger.ts 的 guard、prospec-learn.hbs 的六處編輯），第二次要重打七段編輯才回到原狀。prospec 的 commit 邊界在 verify S/A，也就是 implement→review→verify 全程工作樹都是未提交的，因此 checkout／stash／restore 在這三站一律是資料遺失動作。做法：mutation 前先 `cp` 到 scratchpad，之後 `cp` 回來；並在委派審查時明文禁止這三個指令（本輪三個 subagent 都因此指示而未破壞工作樹） | 1 | 2 (tests,lib) | convention | add-learn-staleness-sweep | personal |
| verify:constitution | Verify constitution FAIL | 1 | 2 (templates,tests) | playbook | conditional-ripple-effects | personal |
| verify:tests | Verify tests FAIL | 1 | 2 (templates,tests) | playbook | conditional-ripple-effects | personal |
| docs/volatile-convention-documented-as-universal | 把沒有任何機制在執行的慣例寫成全稱句＝埋一顆必然過期的宣稱（CONTRIBUTING 的 archive commit 形狀連續過期兩代，唯一終結該鏈的處置是整句刪除）。規則：文件化一個形狀前先問「誰在強制它」；無執行者就描述該工件**攜帶什麼內容**（穩定），形狀交給 `git log`。附帶：查證此類宣稱時 grep pattern 須寬於被查事實的變異空間 ｜ **Promotion**: 吸收進 PB-003 第三面強化條款，Approved-by: benwu95, 2026-08-06 | 1 | 1 (cli) | convention | land-memory-only-knowledge | promoted |
| knowledge/no-honest-edit-concluded-too-early | 發版前 knowledge-health 對某模組 README 報 stale 時，「這是時間戳假象、沒有真實內容可寫」這個結論下得太早：實際一挖常有真事實（該測試檔除 SKILL.md 外還釘住 references/*.hbs 的欄位契約＝真實描述缺口；當輪親手跑指令量到的 pitfall）。規則：宣告「無誠實編輯可做」之前，先 (a) grep 該模組原始碼／測試的實際涵蓋面對照 README 敘述、(b) 把本輪親手量到的行為寫進 Pitfalls；窮盡之後才談誠實揭露 WARN。註：本鍵只涵蓋這個判斷紀律——生成檔觸發 stale 已由 issue #121 機制化（見 retired 的 knowledge/generated-file-trips-module-stale），補綠頂爆預算已由 PB-011 涵蓋，data-only registry 仍觸發 stale 屬產品缺口（issue #133）。證據強度揭露：多輪發版的歷史來自 Claude session memory，repo 內僅驗到 d4ba81d／36d8f23 兩筆同型 commit 佐證模式存在，故 frequency 誠實記為 1 而非 3 | 1 | 1 (lib) | playbook | land-memory-only-knowledge | personal |
| bug/dollar-anchored-line-regex-vs-crlf | `$` 錨定的 per-line regex 配 `split('\n')`：`\r` 在 JS regex 裡是 line terminator，`.` 不吃它、無 `m` 旗標的 `$` 只錨字串結尾，所以 CRLF checkout 下每一行都不命中。unify-req-heading-matcher 的 `readSpecCounters` 因此讓本 repo 10 份真實 spec 有 5 份 story 計數錯誤（sdd-workflow 32→0）且錯值非零、恰好躲過同一變更新加的歸零守門；隔壁 `withoutFencedBlocks` 同型未修（issue #140，main 上即存在，兩向皆零覆蓋）。正解是在 split 處統一 `split(/\r?\n/)`，而非在每個 pattern 尾巴補 `\r?`；任何 LF/CRLF 差分行為都要有雙跑斷言，且 fixture 不可只挑無 `$` 錨點那種恰好會通過的形狀 | 1 | 2 (lib,services) | convention | unify-req-heading-matcher | personal |
| spec/parenthesised-label-is-not-a-block-boundary | delta-spec 的 `**Spec:**` block 邊界由 `DELTA_BLOCK_LABEL` = `/^\*\*[A-Za-z][\w \-/]*:\*\*/` 判定，字元類不含括號 —— 所以 `**Deviation (recorded at implement time):**` 看起來是 label 卻不是邊界，整段變更敘事被吸進 Spec block 並原文落進信任區（archive Phase 3.5 手動移除）。寫 `**Label:**` 時只用字母、空白、`-`、`/`；任何寫在 `**Spec:**` 之後的區塊，落地前都要用 `archive --dry-run` 看實際會寫入什麼 | 1 | 2 (services,templates) | convention | unify-req-heading-matcher | personal |
| docs/one-invariant-many-prose-copies-in-one-spec | 凍結 check id 的總數在同一份 drift-detection.md 裡有四個散文副本（REQ-LIB-014 為總數減一、REQ-TYPES-034、REQ-TYPES-052、REQ-TESTS-045），另有 sdd-workflow 一處，全部無機械守門。unify-req-heading-matcher 加第 15 個 id 時分三輪才補齊：round 2 補一處、round 3 補一處、verify 的 fresh-context 2/5 才抓到第三處；更遞迴的是我為此新增的 bullet 自己把副本數寫成「三個」而實際四個 —— 修法不是改那個數字，而是改成逐一列舉 REQ id（清單即事實，沒有可漂移的數字）。加 id 時要 grep 整棵 specs 樹的同源數字，別只改被指名的那一處 | 2 | 3 (types,lib,tests) | playbook | unify-req-heading-matcher, unify-line-splitting | personal |
| knowledge/index-row-is-generated-order-included | `prospec/index.md` 的 Modules 表是由 `module-map.yaml` 生成的，contract test `own-knowledge-sync` 逐欄比對且**含順序** —— 我在 module-map 的 keywords 中間插入、卻在 index.md 末端追加，於是 commit 後 `--record-tests` 把紅色測試蓋章成 test_provenance FAIL。改 module-map 的任何欄位時，index.md 對應欄位要以同一順序同步（或直接由生成器重出），不要「兩邊都有就好」 | 1 | 2 (lib,tests) | convention | unify-req-heading-matcher | personal |
| contract/generator-violates-its-own-shipped-format | 出貨的格式規範規定 7 節、生成器只產出 1 節，兩者從無任何測試比對 → skill 的檢查項成為不可滿足的勾選項，agent 只能謊勾或手補後被下次執行清掉；判準：一份 reference 規定的節若沒有測試比對生成器產出，那份 reference 就只是願望 | 1 | 3 (services,templates,tests) | playbook | stop-clobbering-product-spec | personal |
| cli/installed-binary-lags-source | 已安裝的 prospec 執行檔落後 source，dogfood 當下若用它跑 archive 會以舊行為毀掉剛修好的 product.md；判別依據是 dry-run detail 的措辭差異，故涉及本次修復行為的 CLI 步驟一律用 npx tsx src/cli/index.ts 跑 | 1 | 2 (cli,services) | convention | stop-clobbering-product-spec | personal |
| gate/bundle-without-build-greens-stale-e2e | 改 `.hbs` 後只跑 `pnpm bundle` 不跑 `pnpm build`，e2e spawn 的是舊 `dist`，整套測試全綠是假的 —— round 1 因此漏掉一個當時已存在的紅測試 ｜ **Declined**: 2026-08-07 sweep（Approved-by: benwu95）——與 `test/e2e-stale-dist-false-green` 同一失效模式，本鍵是 archive Phase 4.5 收割時的 keying 錯誤（Collect 唯一的 LLM 步驟）。該次發生已計入本尊（freq→3，因而達 suggest-promote）；本列保留為 keying 錯誤本身的紀錄，不再單獨累計 | 1 | 2 (templates,tests) | playbook | measure-all-load-surfaces | declined |
| test/mutation-harness-must-assert-the-mutation-landed | 變異 harness 用了 vitest 4 已移除的 `--reporter=basic`，整個 run 在載入 reporter 時失敗，7 個變異全報「0 killed」的假綠；後續也用同一點推翻了一個 subagent 宣稱「測試只擋單向」的 finding ｜ **Declined**: 2026-08-07 sweep（Approved-by: benwu95）——與 `test/verify-the-mutation-actually-applied` 同一失效模式，本鍵是 archive Phase 4.5 收割時的 keying 錯誤（Collect 唯一的 LLM 步驟）。該次發生已計入本尊（freq→3，因而達 suggest-promote）；本列保留為 keying 錯誤本身的紀錄，不再單獨累計 | 1 | 1 (tests) | playbook | measure-all-load-surfaces | declined |
| test/expected-value-derived-from-subject-is-tautology | `describe.each` 從被測的 `rule.tokenKey` 推導預期上限，fixture 隨缺陷移動；把 kind 綁到錯的預算欄位，2,373 個測試無一變紅 —— 預期值須用字面表，registry 只驅動案例清單 | 1 | 2 (tests,types) | playbook | measure-all-load-surfaces | personal |
| spec/spec-block-replaces-whole-body-omission-is-silent | `**Spec:**` 整段取代信任區 REQ body，改寫時漏抄的 `WHEN/THEN` 會永久消失且不報錯 —— 本輪在 review round 4、5、6 各發生一次，須逐條 diff 現行 body | 3 | 6 (services,templates,cli,lib,types,tests) | playbook | measure-all-load-surfaces, stop-silent-spec-body-loss, converge-req-body-boundary | promoted |
| review/parent-rebuild-contaminates-delegated-agent | 主 agent 在 subagent 執行期間跑 `pnpm bundle`/`pnpm build`，污染其建置並害它逾時，還讓我誤把 82 個測試失敗歸因為「負載下 flaky」—— 委派審查期間主 agent 不得改動工作樹或重建 | 1 | 2 (tests,templates) | playbook | measure-all-load-surfaces | personal |
| verify/contract-check-finds-what-defect-search-cannot | 六輪 review 派 13 個審查者、24 個 critical，全數漏掉 `init/prospec.yaml.hbs` 是死樣板（內容正確、但沒人 render 它）；verify 2/5 對照契約問「這條 REQ 宣稱的行為實跑起來是否為真」才發現 —— REQ-TEMPLATES-149 自 #64 起即不成立 | 1 | 2 (templates,services) | playbook | measure-all-load-surfaces | personal |
| docs/gate-item-conditioned-in-isolation | Gate 是必須全數成立的合取式：只把自己新增的那一格加上例外條件，會讓相鄰的既有項在同一狀態下恆為 false，整個 gate 無法誠實勾完並卡住其後不可跳過的站（F-3 首發、F-6 以另一觸發條件復發） | 1 | 2 (templates,services) | playbook | refuse-near-miss-feature-map | personal |
| fix/predicate-narrower-than-guarded-operation | 守衛用的判準必須與它所守護的操作等寬：splice 取代整個區段，判準卻只數 ### 條目，於是 bullet list／table／prose 寫成的內容被判為空而遭抹除。先問「這個操作到底會動到什麼」，再寫判準 | 3 | 4 (services,tests,lib,types) | convention | refuse-near-miss-feature-map, read-specs-by-req, separate-review-evidence | promoted |
| docs/claim-overreaches-its-scope | 為讓句子完整而補上的範圍／前提／歸屬／序位，全是可被證偽的宣稱：一次 README 改寫同時錯了 --quiet 在 dry-run 的範圍、缺檔仍 bootstrap 的前提、worklist 的歸屬主詞、以及「第三行 stderr」的序位（F-11~F-14）。文件裡不需要的精確度就是不需要的錯誤面 | 8 | 6 (templates,cli,tests,lib,services,types) | playbook | refuse-near-miss-feature-map, add-issue-link-field, unify-line-splitting, situationally-aware-station-transitions, strip-governance-ids-from-templates, align-knowledge-check-attribution, support-native-language-trust-zone, prompt-trust-zone-language-at-init | promoted |
| process/git-checkout-destroys-uncommitted-work | mutation 探測還原時用 `git checkout -- <file>`，毀掉該檔案本變更全部未 commit 的編輯（本輪為 prospec-archive.hbs 的五處），並連同 bundle 與部署品一起回捲。**本 repo 第三次**。救回靠的是稍早剛好留下的 /tmp 備份，是運氣不是流程。規則：mutation 還原一律 `cp` 備份，未 commit 的分支上永不使用 `git checkout --` | 1 | 2 (templates,services) | convention | stop-silent-spec-body-loss | personal |
| guard/new-guard-inherits-the-blind-spot-it-closes | 為修補「模板改了沒部署測不出來」而寫的 `agents:check`，第一版只跑 `agent sync`——而 sync 是從 `BUNDLED_TEMPLATES` 渲染、不讀 `src/templates/**`，所以「改 .hbs 沒 bundle」照樣綠燈，正是它要修的兩種失敗的其中一種。且它自身無測試，移除 bundle 步驟的 mutation 存活。規則：新守衛落地前先問「它涵蓋整條鏈嗎」與「誰守護這個守衛」，並把重生步驟做成可對照版控字面值的資料 | 1 | 2 (tests,templates) | playbook | stop-silent-spec-body-loss | personal |
| spec/bullet-scoped-guard-blind-to-prose-numbers | 流失守衛是 bullet 粒度的，住在 REQ **散文段落**裡的凍結數字對它結構性隱形。四輪獨立 review 都沒抓到 `drift-detection.md:119` 的「five checks」，不是不夠仔細，是機制看不見那個位置。同一份 spec 中要求「追加 check id 時每處 prose 副本同步更新」的那條 bullet，自己三度少算（說三實有四、列四實有六、六實有七）。規則：凡依賴人工列舉的 prose 副本清單，都要加「任何散文數字在證明是序數之前都當作副本」的 catch-all，並優先尋求機器化 | 1 | 2 (services,types) | playbook | stop-silent-spec-body-loss | personal |
| review/fix-round-invalidates-the-independent-grade | judgment 維度（verify 2/5）由 fresh context 裁決，但每次依其發現修完工件，該評級就不再描述現況——若每次都要重評，會形成無限回歸。本輪跑了五輪才收斂（發現數 1→3→2→1→0）。做法：修完後跑一次**範圍收窄**的複核（只查修了什麼＋有無迴歸），並在提示中明說「若三項都修好且掃描無實質發現，PASS 就是正確且預期的答案」；絕不自行把 WARN 升成 PASS | 1 | 2 (templates,services) | convention | stop-silent-spec-body-loss | personal |
| test/disjunction-assertion-pins-neither-clause | 契約斷言寫成 `toMatch(/A\|B/)` 時，刪掉任一邊仍全綠——兩個子句都沒被單獨釘住。變異驗證抓到後拆成兩條獨立斷言，兩個變異才各自 KILLED。凡一條斷言涵蓋兩個獨立事實，就要拆成兩條。 | 1 | 1 (tests) | convention | read-specs-by-req | personal |
| templates/worklist-mistaken-for-the-work-set | 把 archive Phase 3.5 的閱讀面從「每份已同步的 spec」改成「worklist 具名的 REQ」，靜默漏掉最常見的一批——五份 worklist 全是例外報告（body 為空／漏抄 bullet／截斷），乾淨落地的 REQ 一律不入列，quick 更是結構性全空。工作集合要由 graduation key 指定，例外報告只說「其中哪些還需額外處理」。 | 1 | 2 (templates,services) | playbook | read-specs-by-req | personal |
| templates/instruction-reads-designed-absence-as-failure | verify item 7 改成用 `spec show` 讀 delta-spec 具名的 REQ，卻沒說 ADDED REQ 要到 archive 才畢業；跑本變更自己的 REQ 會印六行 `✗` 並 exit 1，agent 會把設計內的缺席讀成阻斷性失敗。新增的站點指令若會觸發工具的非零退出，必須在同一句說明那是預期狀態。 | 1 | 1 (templates) | playbook | read-specs-by-req | personal |
| test/assertion-pins-data-the-pipeline-rewrites | 斷言把「流程自己會改寫的資料」當成 oracle：REQ 計數等值表釘的是本 repo 十份 feature spec 的活計數，而同一個變更的 archive 畢業就改了它們（sdd-workflow 33/173→34/179、mcp-server 11→13），CI 因此為一次合法的內容變更轉紅——第一個踩到的就是本變更自己。等值要釘在「規則會分歧的形狀」上（合成 fixture，預期值仍取自改動前實作），而非今天的資料值；活語料上只留相對比較（LF vs CRLF、兩個 reader 互比），那種斷言畢業打不破。與 test/assertion-encodes-implementation-not-rule 的差別：那是把實作編碼進斷言，這是把可變資料當基準。 | 1 | 2 (tests,lib) | convention | read-specs-by-req | personal |
| spec/delivered-behavior-has-no-req-carrier | 交付了 delta-spec 沒有涵蓋的行為 → archive 時無畢業載體，信任區永遠不會記錄它。add-issue-link-field 同一輪犯兩次：(a) 為修安全性缺陷加的 flag refusal + whitespace collapse 無 REQ 且與既有 REQ 的 **Spec:** 矛盾（round 2 critical，審查者實跑 archive 證明畢業結果）；(b) review 補的 ff／new-story 訪談行為無 REQ（verify 2/5 fresh-context 評分者揪出）。守則：review→fix loop 每landing 一個新行為就問「哪條 REQ 承載它」，答案是『沒有』時新增 ADDED REQ，別等 verify | 1 | 4 (templates,cli,lib,services) | convention | add-issue-link-field | personal |
| async-file-ops | Do not use synchronous fs operations (like fs.readFileSync) inside async functions. Use fs.promises APIs to prevent blocking the event loop. | 1 | 1 (services) | convention | add-per-change-context-report-to-measure | personal |
| verify-graded-in-session | Graded verify dimensions in-session without using an independent fresh-context subagent. | 1 | 0 | playbook | feature-spec-sub-modules | personal |
| test/mutation-red-does-not-prove-the-semantic-is-pinned | 變異落地且測試轉紅，仍不等於斷言釘住了語意：把區段定位改比對標題後測試變紅，但它死於邊界位移而非 locator 身分 —— 一個 byte-equivalent 的 heading-keyed locator（找標題再回退一行）讓全套 2,890 個測試通過。變異要打在語意上，不是打在任何會讓測試轉紅的地方 | 1 | 2 (tests,lib) | convention | separate-review-evidence | personal |
| docs/measurement-not-retaken-after-the-artifact-changed | 量測在被量的工件停止變動之前就記進工件：SC-006 的三個 token 數字取自 review 之前，四輪修復把 Trust boundary 節與兩列上限加進 reference 之後全部過期（reference 實際 2,136 而非 1,369），由 verify 2/5 的 grader 抓出 —— 一份即將歸檔的工件裡的錯誤事實宣稱 | 1 | 1 (templates) | convention | separate-review-evidence | personal |
| spec/landing-block-transcription-drops-bullets | 把既有 REQ 內文抄進 delta-spec 的 `**Spec:**` 落地區塊時漏抄一條 bullet（肇因是先前的 `sed` 讀取視窗截斷了原文），而該區塊會逐字覆蓋信任區；CLI 的 droppedBehavior 只比對它自己執行的替換，抄寫階段的遺失不在其視野內。修法：抄寫後以機械式 bullet 集合比對原文，不靠閱讀 | 2 | 4 (services,templates,cli,types) | playbook | configurable-generated-artifacts, autonomous-drift-draft | personal |
| docs/shipped-prose-asserts-engine-behavior | 出貨模板與 `_status-lifecycle.md` 兩份副本的散文陳述某個引擎檢查「會讀什麼」，但只有 `## What each gate checks` 有整段字串相等守衛；判準被反轉後，該區段外的陳述在兩份副本與兩份已部署 SKILL.md 中同時變偽而無任何機制察覺，只有對抗式審查抓到 | 1 | 2 (templates,lib) | playbook | configurable-generated-artifacts | personal |
| docs/user-facing-config-key-undocumented | 新增使用者面向的 `.prospec.yaml` 設定鍵後，兩份根 README 皆未記載，而同層的 `additional_core_conventions` 在兩份 README 都有完整散文與 YAML 範例；Constitution 已有 [SHOULD] 規則但無任何機器守衛，只在 verify 3/5 以判斷發現 | 1 | 2 (templates,types) | convention | configurable-generated-artifacts | personal |
| refactor/convergence-adopts-one-owners-edge-semantics | 把重複實作收斂到單一擁有者時會連同該擁有者的邊界語意一起繼承。converge-req-body-boundary 把 REQ body 邊界收斂到 `indexSpec`，修好 fenced block 方向，卻在「比 REQ 更深的刪除線標題」上比舊碼**更早**截斷，而 delta-spec 的 WHEN 條款寫的是相反結果；短切片讓 droppedBehavior 看不到失去的 bullet，loss guard 一併失效（HEAD 擋下寫入、新碼放行）。修法：收斂前窮舉兩份實作的每一處分歧並逐一裁決採哪一方——「單一來源」不等於「正確來源」 | 1 | 3 (lib,services,tests) | convention | converge-req-body-boundary | personal |
| spec/added-req-id-collides-with-existing | delta-spec 的 ADDED REQ 編號是手挑的，而 archive 的 ADDED 路徑不比對既有 id、15 個 DRIFT_CHECK_IDS 也沒有一個稽核 REQ id 唯一性。converge-req-body-boundary 的 `REQ-TESTS-001` 與信任區既有同號 REQ 撞號，實測畢業後並存兩個同 id 標題，且 pendingConvergence／droppedBehavior／refusedRequirements 三清單全空、完全無訊號。修法：寫 ADDED 編號前先 grep `prospec/specs/` 取該前綴最大號＋1 | 1 | 2 (services,tests) | playbook | converge-req-body-boundary | personal |
| spec/implement-time-decision-reversal-not-back-propagated | implement 期間推翻了規劃期的設計決策，卻只把新決策記在 tasks.md 的一行註記裡，proposal.md 的 acceptance scenario、plan.md 的實作步驟與 delta-spec 的 `**Spec:**` 區塊全數留在舊決策上。本輪：`knowledge update` 重寫 README 時 bump `last_verified` 被改成「`knowledge verify` 是唯一 stamper」，delta-spec 因此宣稱了一個從未被實作的行為（review critical A-1 才抓到），而 proposal.md:35／plan.md:83 一路活到 verify 才被列為 WARN——proposal 的 scenario 會在 archive 畢業進信任區，等於差一步就把不存在的行為寫進永久紀錄。**可操作的推論**：決策在 implement 反轉時，反轉的當下就要回填三個規劃工件（proposal 的驗收情境、plan 的步驟、delta-spec 的落地區塊），把 tasks.md 的註記當成待辦而非歸檔；判準是「若這條敘述明天被逐字複製進 feature spec，它還為真嗎」 | 1 | 2 (services,types) | playbook | mechanize-knowledge-sync-gate | personal |
| review/delegated-subagent-git-checkout-wipes-worktree | A delegated review/verify subagent that runs `git checkout -- <file>` in its repro/mutation step reverts that file to HEAD and DESTROYS the orchestrator's uncommitted work in it (verify 2/5 grader wiped src/types/skill.ts). The subagent brief MUST forbid `git checkout` for revert (use a cp backup or Edit-revert), and the orchestrator MUST re-verify the working tree byte-identical after any tree-touching subagent (grep residue, git diff --stat, re-run provenance). | 1 | 2 (services,types) | playbook | enforce-group-render-merge | personal |
| archive/delta-spec-routing-headers-must-use-trust-zone-numbering | delta-spec MODIFIED/ADDED 的 **Feature:**/**Story:** 標頭必須用信任區(feature-spec)的實際 feature slug 與 story 編號,不能沿用 proposal 內部編號:archive 機械 sync 依標頭路由,錯的 **Feature:** 會在無關 feature 重複 append 該 REQ、且 landing-fidelity 因解析失敗靜默跳過比對;錯的 **Story:** 會把 ADDED REQ 落進無關 slice。verify delta-spec-compliance 維度應逐一核對每個 REQ 標頭的落點。(系統性修復追蹤於 issue #211) | 2 | 2 (services,templates) | playbook | record-judgment-gate-executor, retire-legacy-index-migration | personal |
| templates/rule-definition-single-home | 同一規則跨多份 shipped reference 出現時（plan rubric／plan-format／review-format／lens），定義句只准有一個家（本例 review-format critical #4），其餘站點以名稱引用；以負向契約測試禁止定義片語出現在其他渲染面、以共享標題字串的渲染比對釘住命名一致——lens 列一度復述兩條件、rubric 一度把 review 的窄門檻倒灌成 plan 站觸發（Architecture Verifier 與 review R2-2 各抓一次） | 1 | 2 (templates,tests) | convention | add-reuse-single-source-gate | personal |
| templates/shipped-template-governance-id-leak | 出貨 skill／reference 模板逐字渲染進下游專案，不得引用本 repo 治理識別字（PB-nnn、BL-nnn、issue #n、模組前綴 REQ id／通配）——17 處分布 14 檔，含三個 lens 段落標題與一處跨行 issue ref；規則須自足以文字說話，泛用範例（AUTH 之類的樣本前綴＋佔位編號）與 `PB-{NNN}` 格式記號除外；以逐模板渲染的負向契約守衛釘住（集合大小由 SKILL_DEFINITIONS＋getSkillReferences 推導） | 1 | 2 (templates,tests) | convention | strip-governance-ids-from-templates | personal |
| review/delegated-reviewer-prose-not-in-artifact-language | 委派 fresh-subagent 寫 review.md／verify.md 時 prompt 未指定工件語言，8 列 Summary／Evidence 全英文 → verify 3/5 Language Policy [MUST] FAIL；機械 artifact-language 只是「有中文存在」啟發式擋不住。規則：所有寫進 change artifact 的子代理 prompt 固定加「Summary／evidence 以繁體中文（台灣）撰寫；路徑、識別字、regex、Repro、enum 保英文」；修補以同 id 重送 merge 覆寫 | 1 | 2 (templates,tests) | convention | strip-governance-ids-from-templates | personal |
| audit/scope-decision-from-truncated-grep | 稽核時以 `grep … \| head -14` 判「信任區無 REQ 釘住這些字串」，命中被截斷（us-23.md REQ-TEMPLATES-132／REQ-TESTS-043），導致 quick scale 的錯誤前提（review S-2 critical）。規則：做 scope／scale 判斷的 grep 先看 `wc -l`／`-c` 總數，再讀全部命中；行過長用 `-o` 取片段而非 `cut` | 1 | 2 (templates,tests) | convention | strip-governance-ids-from-templates | personal |
| git/batched-log-omits-merge-diff-without-c | 批次 `git log --name-only`（無 pathspec）預設省略 merge commit 的變更檔案；要與逐路徑 `git log -1 -- <path>` 的 pathspec 簡化等價（含把檔案改成與所有 parent 皆不同的 combining/evil merge），必須加 `-c`（combined diff）——否則某 module 原始碼最新一次是被 merge 觸碰時，last_src_commit 會 fail-open 退回較舊 commit，使 knowledge-health staleness 假綠。以 fixture repo（combining `--no-ff` merge）＋fail-then-pass 迴歸測試釘住。 | 1 | 1 (lib) | playbook | dedupe-drift-git-subprocesses | personal |
| perf/lazy-import-defeated-by-service-reexport-of-leaf-helper | 做 startup lazy-import 精簡時：formatter 若透過 service 的 re-export 匯入一個純 helper，會把該 service 的 heavy 相依鏈（@inquirer／template→handlebars）整條拉進 registration graph，抵銷 lazy 化；而 cli formatter 直接 import lib 又觸犯 eslint cli→lib 禁令。正解是把純 helper 放到 canonical leaf（types），formatter 從 types 匯入，lib/service 各自 re-export 供既有呼叫者（PB-006/008 掃描所有 reference site）。 | 1 | 4 (cli,types,services,lib) | convention | lazy-load-cli-startup | personal |
| plan/empty-delta-spec-no-behavior-refactor | 純重構或測試基礎設施變更（無產品可觀察行為）在 standard scale 下可合法攜帶空 delta-spec：於 plan 站記 WARN 說明理由、verify 2/5 交由 fresh-context 判定空 spec 是否誠實而 vacuous PASS，切勿為滿足 architecture-verifier 的 bidirectional-mapping 而製造假 REQ（會違反 No-Orphaned-Scope）；架構事實改由 knowledge-update 更新模組 README。 | 1 | 2 (cli,tests) | playbook | e2e-in-process | personal |
| correctness/completion-gate-deadlocks-empty-code-task-set | 以 change-progress 的 allCodeDone（定義為 codeTasks.length > 0 && checked === total）當 `change status implemented` 閘的判準，會讓 tasks.md 只含 [M]/[V]（零 code task）的變更被判「0/0 未完成」而永遠無法 implemented→verified→archived（死結）。規則：completion／all-done 類謂詞必須把空集合視為真空成立——閘只在「有 code task 且未全勾」時拒絕（total > 0 && checked < total），不可直接用 count>0 的 allCodeDone。獨立 review 於本變更抓到（F-1 critical，已加 fail-then-pass 回歸 pin） | 1 | 1 (services) | convention | mechanize-lifecycle-entry-gates | personal |
| templates/decoupled-reference-single-source-traceability | 解耦跨站 Reference 模板時，必須在 AI Knowledge (skill-authoring.md) 與 .hbs 註解 ({{!-- Single Source of Truth: ... --}}) 記載單一事實來源與引用關係，避免後續維護遺忘溯源 | 1 | 2 (templates,tests) | convention | eliminate-dangling-reference-pointers | personal |
| subagent-receipt-verification | 委派 subagent 執行長任務（如 review 或 verify）時，主 agent 必須等待 subagent 進入 idle 狀態並對產物檔案執行實體存在與解析校驗（Receipt Verification），嚴禁在收到首輪訊息時提前判定完成或偽造產物。 | 2 | 4 (cli,templates,tests,lib) | playbook | give-prose-conventions-executors, enforce-subagent-receipt-verification | personal |
| tasks/atomic-source-of-truth-sizing-exception | 不可再拆的 source-of-truth constant 與其 contract comment 可低於一般 task sizing 建議，但必須保留明確理由與獨立驗證。 | 1 | 3 (types,templates,tests) | playbook | require-v2-cli-for-skills | personal |
| knowledge/markdown-canonical-over-yaml | Module README 的 Section template 規則寫在 .prospec.yaml 不直覺；應由 _module-readme-conventions.md 作為 canonical Markdown source，YAML 僅承載工具行為設定。 | 1 | 3 (templates,lib,services) | convention | standardize-module-readme-format | personal |
| spec-sync/graduation-skips-sibling-prose | archive spec-sync（Phase 3.5）只以 delta-spec 的 `**Spec:**` block 取代被 MODIFIED 的 REQ body；同一 feature spec 內的 acceptance scenario 與非 MODIFIED 的姊妹 prose 不會畢業。當變更修正一個 token 或語意、而該 token 也散落在 acceptance scenario 或非 MODIFIED prose 時，須 grep 整份 feature spec 並在 feature commit 直接修正，勿倚賴 graduation。 | 1 | 2 (templates,tests) | playbook | align-review-severity-with-cli-enum | personal |
| fix/unifying-equivalent-values-drops-the-canonical-spelling | 為消除兩份 generator 對同一等價類（case-insensitive 相等的語言名）各取一邊的拼字差，修復改為統一取其中一欄的原始拼字——結果把小寫 english 洩進另一欄本已正規化的預設值，破壞 byte-identity pin 未覆蓋的邊界（round 2 fix-induced critical）。統一等價值時，凡一側有 canonical 預設，必須以該 canonical 為準，並為預設值的所有拼法變體補 pin | 1 | 3 (lib,templates,tests) | convention | support-native-language-trust-zone | personal |
| knowledge/verify-every-source-touched-module | `prospec knowledge verify` 只戳了 cli/services/lib，漏掉 templates（`src/templates` 是 templates 模組宣告的 paths，不因 bundle 落到 lib 就免除）與 tests（測試檔異動亦算 source-touched）；`pnpm knowledge:check` 在 commit 前回 empty-range skip 假綠，feature commit 後必以 2 violations 轉紅，由 review round 2 以 `partitionMissingSync` 模擬抓到（F-4）。規則：戳 last_verified 前用 `git diff --name-only main` 對 module-map paths 歸屬，凡命中的模組全部 verify，別以「連帶」推理省略。 | 1 | 3 (templates,tests,lib) | playbook | prompt-trust-zone-language-at-init | personal |
| harness/subagent-rate-limit-fails-silently-respawn | 委派 subagent 撞到 API 429（session limit）會直接終止且不寫任何 receipt 檔，通知只在 task-notification 的 status: failed 出現；receipt 缺檔＋failed 狀態＝重新 spawn，絕不能判成 not-found、也不得退回 in-session 自評（normalize-executor-labels-and-stats verify 2/5 grader 首次即因此中斷，重 spawn 後 S）。規則：Physical Receipt Verification 的「檔案不存在」分支要先讀 lifecycle 狀態，rate limit 是「未執行」不是「無結論」。 | 1 | 1 (templates) | playbook | normalize-executor-labels-and-stats | personal |
| lib/never-fatal-pipeline-guards-every-schema-constraint | 「寬容、永不致命」的聚合／讀取管線若以嚴格 schema parse 收尾，每個 schema 約束到的欄位都要在入口逐一 type-guard，否則單一壞值（如 spend: -1 通過 Number.isFinite 卻違反 nonnegative）會讓整份報表在末端 ZodError 崩潰，寬容契約形同虛設（normalize-executor-labels-and-stats review R1-1 critical，pin 覆蓋 -1/NaN/Infinity）。規則：末端 parse 是最後防線不是過濾器——入口 guard 與 schema 約束一一對應，並以壞值 fixture 釘測試。 | 1 | 2 (lib,types) | convention | normalize-executor-labels-and-stats | personal |
| spec/story-pointer-is-trust-zone-story-not-slice-filename | delta-spec MODIFIED/REMOVED 的 **Story:** 要寫 REQ 實際所在的 trust-zone 故事號（讀 slice 檔內的 ## US-N 標題），不是 slice 檔名的數字：us-001.md 內是 US-008、us-23.md 內是 US-24、us-5.md 內是 US-6（normalize-executor-labels-and-stats plan verifier WARN 三處全錯）。規則：填 Story 前先 grep 該 REQ id 所在 slice 的 ## US- 標題。 | 1 | 1 (templates) | convention | normalize-executor-labels-and-stats | personal |
