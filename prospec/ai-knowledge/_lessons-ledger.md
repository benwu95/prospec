# Lessons Ledger

> Version-controlled, accumulating tier of the feedback-promotion pipeline. Keyed + incremental —
> `frequency` is a cross-change counter (never re-derived by re-scanning). `status`: personal |
> suggest-promote | promoted | declined.
> Auto-fed by `/prospec-archive` Phase 4.5 (Harvest) and refreshed by `/prospec-learn` Collect.
> Format + harvest rules: `.claude/skills/prospec-learn/references/promotion-format.md`.
> Durable across worktrees/clones (replaces the retired gitignored `.prospec/lessons.md`).
> Evidence pointer: each `source_changes` name resolves to its committed record at
> `prospec/specs/_archived-history/{date}-{name}.md` — cite that file's `## Review & Verify` section
> (grade, criticals/majors, quality_log digest) as the durable evidence, not the gitignored
> `.prospec/archive/` bundle.
>
> **Description language**: the `description` column — including its `｜ **Promotion**:` / `｜ **Retired**:`
> provenance suffix — is written in the original correction's language (often Traditional Chinese), a named
> exception the Constitution's Language Policy rule declares for this file, kept so the raw correction
> context is not lost in translation at this working-notes tier. Keys, `kind`, `status` and the structural
> fields stay English (`status` is a bare enum token); `_playbook.md` is English apart from the correction
> evidence the same rule exempts.

| key | description | frequency | impact_modules | kind | source_changes | status |
|---|---|---|---|---|---|---|
| test/structural-false-green | 斷言只驗內容存在性、不驗結構完整性與負向路徑 → 缺陷在場仍全綠；須 section-scoped + 結構斷言（集合/順序/contiguity）+ 負向斷言 + mutation-verify  ｜ **Promotion**: PB-001 強化版，Approved-by: benwu95, 2026-06-11；add-drift-checker 再證：無控制字元的 sanitize 斷言 trivially green + mutation 未生效即綠；add-mcp-server 再證 ×2：invalid-map 列表吞錯與 distinct-vs-summed 排序語意均無測試凍結，成為 review criticals；src-review-round2-remediation 第 7 度：round-2 review 找出 content-merger〔toContain-only〕、change-resolver〔無專屬測試〕、steering layer 表未斷言等假綠，修復以 exact-output + 負向 + mutation-verify〔A1–A5 revert 即紅〕補強；quick-scale-and-ceremony-cleanup 第 9 度：metadata-completeness 的 `hasVerifyGrade` S/A clause 無測試釘死——drop `(result==='S'\|\|result==='A')` 仍全綠（c3 skill-mismatch、c4 兩真，皆未隔離該 clause），D 級變更可過閘 → 補 verify+非S/A(c10) + review+A(c11) 兩測，mutation-verified 變紅；slim-skill-trigger-context 第 10 度：ff/plan/archive 的 format-ref on-demand 移轉只以 baseline item-set/MANDATORY-count 釘「refs 不在 Startup Loading」（negative 面），漏釘「refs 仍於 consuming phase 被引用」（positive 面）——刪掉某 phase 的引用仍全綠（deployed-but-unread）；review test-quality 揪出，補 ff/plan/archive 三個 section-scoped phase-citation 斷言〔mutation-red〕；converge-skill-triggers 第 11 度：新增的 ≥3 mutation guard 起初以字面 `['a','b']` filter 自證（near-tautological、未走 production 述詞，鬆綁 production 下限仍綠），review test-quality 揪出，重構為實斷言與 guard 共用 `skillsBelowMinTriggers` 述詞〔真 SKILL_DEFINITIONS + 合成 skill，鬆綁即紅〕；document-drift-report-contract 第 12 度：修 phantom `knowledge_health.stale[]` 卻無釘住其缺席的測試,r1 verify test-quality 揪出 → r2 補 section-scoped fidelity〔遍歷 DRIFT_CHECK_IDS〕+ 負向〔任何 report 消費者不得讀 phantom field〕+ positive,三類 mutation-verified 轉紅；align-language-policy-scope 第 13 度 ×2：守核心不變式的 guard 用 `find(...)!`，`not.toContain(undefined)` 在 vitest 恆過——把知識庫從英文區整段刪掉仍綠；同一 guard 只比對字面 glob，以 prose 重述同一矛盾亦溜過。修法：先斷言 glob 存在再比對＋加語意（prose）斷言，4 項 mutation 實證會紅；split-verify-adjudication 第 14 度：兩條自寫斷言為 false green——`expect(detail).toContain('1')` 撞上 fixture 的 change 名稱 "c1"（把退出碼從 detail 拿掉仍 81 綠）、RFC-2119 映射用全文件 `toContain('MUST')`（把 MUST→FAIL 整段換掉仍綠，而它是唯一釘該映射的測試）。修法：斷言整句渲染結果並 section-scope 到該維度區段；archive-cli-entry 第 15 度 ×2：零寫入快照只記檔案不記目錄——ensureDir 建空目錄的回歸不轉紅（F5）；replay 等價測試只斷言 dry ⊆ real、未斷言 real ⊆ dry——「real run 多寫」型缺陷（F1）由構造即通過（F6）。修法：快照納目錄項＋補反向 unpredicted-set=∅ 斷言，均 mutation-verified；add-harness-capability-flags 第 17 度 ×2：交集測試的 fixture 降級群組**最後**一個成員（antigravity），因此把 `intersectCapabilities(...)` 換成 `configs.at(-1)` 全套仍綠——而測試註解明寫「last member wins 會被抓到」；同一輪的 repo-wide 負向斷言是「過去措辭白名單」，10 種合理改寫有 8 種溜過（含 delta-spec 自己點名待改寫的原句）。修法：fixture 改降級**中間**成員使首尾兩種退化皆變紅；負向斷言改比對判斷的**形狀**（條件詞＋能力名詞＋否定，兩種語序）並豁免引用已解析值的句子；report-dropped-req-bullets 第 18 度 ×3（**全部發生在「修 false-green」的過程中**）：新寫的空白正規化測試斷言「結果為空」，而任何「完全抽不出 bullet」的破壞也產生空結果，與被破壞的實作無法區分；三個假回報探測同樣只斷言空結果；dry-run 測試只斷言無條件輸出的 header 動詞，跳過 bullet 迴圈仍全綠。**共同形狀：負向斷言必須同時證明機制有在運作**，否則它與「機制整個消失」等價。修法一律是讓 fixture 帶一條真正會被回報的項目，斷言精確的非空結果；pilot-mutation-testing 第 19 度 ×2：釘住「不進 CI」的斷言只點名 `prospec-check.yml`，而 `ci.yml` 才是真正的閘門——在 `ci.yml`／`release.yml` 插入 mutation 步驟仍全綠；另一條以負向 grep 擋住被禁止的表格列，僅把 `name the mutations` 改成 `list the mutations` 即全綠、缺陷完全復原。**兩者同一形狀：涵蓋面等於作者當下想得到的清單**。修法改為列舉／集合基準比對（列舉 workflow 目錄、準則列集合 vs 版控基準），並以具名守衛取代恰好釘在當日數量的 `>= 3`——後者是對合法整併的絆索而非空洞守衛 | 20 | 5 (tests,templates,cli,lib,services) | convention | add-output-contract, add-review-fix-loop, add-token-measurement-harness, reorder-stable-prefix-loading, add-drift-checker, add-mcp-server, src-review-round2-remediation, add-reverse-spec-extraction, quick-scale-and-ceremony-cleanup, slim-skill-trigger-context, converge-skill-triggers, document-drift-report-contract, align-language-policy-scope, split-verify-adjudication, archive-cli-entry, fix-cli-first-regressions, add-harness-capability-flags, report-dropped-req-bullets, pilot-mutation-testing, delegate-module-adjudication | promoted |
| docs/measurement-attribution-overclaim | 文件/註解宣稱的行為必須與實作實際可觀測者一致；量不到/沒做到的用 deliberate-exclusion 措辭明示（三案同根：harness warm 不對稱、BL-020 模板層歸因、drift checker 模板宣稱 shallow→skipped 但 code 未實作〔review C3〕）  ｜ **Promotion**: （PB-003，Approved-by: benwu95, 2026-06-12；fix-init-clobber-add-upgrade 第 8 度：version/CLI 重設計（iter-2）後 7 處 doc/comment 殘留舊宣稱（CLI「re-render canonical docs」/「記錄 prospec_version」）與新契約矛盾，iter-2 對抗式 review 揪出全數更正（README ×2、_index ×2、upgrade.ts/version.ts/canonical-docs.ts 註解）——再證 redesign 後須回掃所有描述舊行為的 doc/comment；add-mcp-server 再證 ×3：「首個 runtime dep」不實宣稱、tasks.md T10 宣稱未做的 dispatch、REQ 文字與實作漂移〔ConfigNotFound、列表過濾〕；harden-lib-correctness 第 5 度：delta-spec/proposal 的 scanner depth 需求（LIB-005）宣稱「MUST 發 logger warning」但實作為 doc-only 且 task 已標完成，verify 2/5 揪出 → 以 deliberate-exclusion 措辭修規格為文件化解法消解；src-review-round2-remediation 第 6 度：本輪 delta-spec 的 CONTRACT-001 第 4 項自寫「至少發 logger warning，不靜默丟」但實作只寫進無人讀的 result 欄位〔write-only〕→ review critical，修法為沿 result chain 傳到 ArchiveResult.knowledgeWarnings + 改寫 REQ 為 claim ⊆ impl）；converge-constitution-audit 第 9 度：verify Key Difference「Every other skill checks only its site-specific rule」對 `/prospec-explore` 的 advisory 多原則 Constitution Checkpoint 過度宣稱（claim ⊄ impl）→ review major，收斂措辭到 SDD-pipeline skills 並明列 explore 為決策輔助非 gate——再證措辭型 claim 亦須 ⊆ 實作；quick-scale-and-ceremony-cleanup 第 10 度：改寫 INVEST Verify 條款為 advisory 後，Constitution Constraints checklist:78 仍寫「User Stories pass INVEST validation before entering the Plan stage」（pre-plan 硬 gate）與新語意矛盾（claim⊄impl）→ review critical，同步改寫 checklist 為 advisory；再證 redesign 後須回掃同文件所有描述舊行為的斷言；add-metadata-format-reference 第 11 度：metadata-format reference 的 dimensions 範例用 flow-style，違反其自身「exactly what stringifyYaml emits」宣稱（實測 stringify 輸出 block）——一份治理格式的 reference 自己示範錯誤格式（claim⊄impl 的自我指涉版），review critical 揪出修為 block；並 Written-by 欄誤列 implement／漏 new-story·ff（factual claim⊄impl），review major 修正；archive-cli-entry 第 12 度：skill 模板 Error Handling 宣稱「The CLI rolls back…and reports the error」，但 execute() 外層 catch 吞掉錯誤、終端只見泛用 skipped 行（claim⊄impl，review major F4）——修法 ArchiveResult.skippedReasons 攜帶真實原因使宣稱回真 | 13 | 4 (cli,templates,lib,services) | playbook | add-token-measurement-harness, reorder-stable-prefix-loading, add-drift-checker, add-mcp-server, harden-lib-correctness, src-review-round2-remediation, backfill-promotion-path, fix-init-clobber-add-upgrade, converge-constitution-audit, quick-scale-and-ceremony-cleanup, add-metadata-format-reference, archive-cli-entry, delegate-module-adjudication | promoted |
| measure/spend-accounting-failure-paths | 金流/預算控制必須逐呼叫入帳、驗證輸入（NaN/空值）、失敗路徑不漏計——4 個 critical 同源於「成功路徑才入帳」假設 | 1 | 2 (lib,scripts) | playbook | add-token-measurement-harness | personal |
| docs/runbook-executable-as-written | 操作 runbook 留檔前須逐指令實際執行驗證（pnpm `--` 轉發、commit 前置缺漏皆為照抄必死） | 1 | 1 (docs) | playbook | reorder-stable-prefix-loading | personal |
| git/commit-message-english | commit 訊息（標題+正文）一律英文  ｜ **Promotion**: 已由使用者直接入憲：原則 2 + 0c1a4a2，無需再晉升 | 1 | 0 | constitution | session-correction (1d236fc amend) | declined |
| spec/fr-uniformity-needs-shared-type | 宣告「每個 X 都要 Y」的 FR，實作須以共用型別強制（如 `Source<T> = {available, reason?, items}`），逐項手寫必有遺漏——五檢項三有二無 availability 包裝成 critical（review C4） | 1 | 3 (lib,services,types) | convention | add-drift-checker | personal |
| ci/pipeline-semantics-not-unit-testable | CI workflow 管線語意層缺陷（pipefail、shell 預設）不被 CLI 層單元/e2e 覆蓋——workflow 模板需要自己的內容斷言（shell: bash、SHA pin、permissions、compose 形式）；`\| tee` 吃掉 exit code 由三個獨立 lens 同報（review C1） | 2 | 2 (templates,tests) | playbook | add-drift-checker, add-windows-smoke-ci | personal |
| scan/false-positive-kills-trust | 掃描器/檢查器類功能：誤報比漏報致命（摧毀對工具的信任）——排除規則（fenced block 含 CommonMark 關閉語意、block comment、placeholder/glob、repo 外路徑）須首版設計並以 dogfood 實證零誤報（首跑即中 fence 誤報 + 6 個誤報類 major） | 1 | 1 (lib) | playbook | add-drift-checker | personal |
| refactor/relocation-reference-sweep-completeness | 遷移/改名一個 artifact（如 ledger 路徑）須掃全部引用面（skills + types/skill.ts 描述 + knowledge files + tests），不只明顯的 file-path 字串——tier 標籤/描述等 wording 殘留最易漏，且 path-grep 契約斷言對 wording 殘留是盲的（須補語意殘留的 negative 斷言）。add-knowledge-flywheel review 4 majors 同源於此  ｜ **Promotion**: PB-008 · Approved-by: benwu95 · 2026-07-06；freq=3 ∧ modules≥2 fired；quick-scale-and-ceremony-cleanup 第 2 度：readme-counts→mcp-readme-counts 改名漏掉 review-lenses-content.hbs 的 prose 引用「the deterministic \`readme-counts\` check」——shipped src template 內 dangling id，path-grep 契約對散文引用是盲的；須補散文/描述引用的 sweep；inject-resolved-knowledge-budgets 第 3 度：`KnowledgeSizeBudget` interface 由 `lib/drift-sources` 移至 `types/config`、drift-sources 改為 `import type`（未 re-export），但 `drift-sources.test`/`drift-checker.test` 仍從 drift-sources import 該型別 → TS2459 latent；因 `tsconfig` exclude tests、esbuild runtime strip type-only import，`pnpm typecheck`/`test`/`lint` 三閘皆漏，僅對抗式 review 揪出。freq=3 ∧ impact_modules≥2 ⇒ suggest-promote | 3 | 5 (templates,types,tests,lib,services) | playbook | add-knowledge-flywheel, quick-scale-and-ceremony-cleanup, inject-resolved-knowledge-budgets | promoted |
| security/invariant-misses-parallel-consumers | 安全/一致性不變式只施加在最初發現的面，同一資料源的平行消費路徑漏掉——within-change ×3（symlink 圍堵漏 loadModuleMap parse 路徑、name guard 漏 health 的 collectGitTimestamps、list/read 過濾不對稱），其中 2 個成為 round-4 review criticals（真相面互相矛盾、存在性 oracle）。修一個面時必須 grep 同一資料源的全部消費者，把不變式套到每一面並各自測試凍結  ｜ **Promotion**: PB-007，Approved-by: benwu95, 2026-07-02；frequency=3 · impact_modules=3 · kind=playbook · rule=freq≥3 ∧ modules≥2 fired；fix-upgrade-doc-coverage 第 3 度：新 buildDocsInventory 只用 resolveBasePaths().baseDir 拼 `ai-knowledge/` 路徑，漏掉同一資料源的 knowledgePath——knowledge-init/agent-sync/knowledge-reader 全部尊重 `knowledge.base_path` 覆寫、唯獨新消費面沒有，成為 review confirmed critical〔覆寫專案五份文件誤報 MISSING、skill 將在錯誤位置建重複檔〕；修法為 registry 增 root 判別＋消費端經 knowledgePath 解析並以回歸測試凍結。src-review-round2-remediation 第 2 度——本輪核心主題：archive `**Feature:**` slug 未套 isSafeResourceName〔路徑逃逸〕、drift-sources markdown-link 未套 realpath containment〔symlink 存在性 oracle〕、measure/error-output 未套 check-output 既有的 sanitizeTerminal——同一不變式在最初發現面有、平行消費面無；連 review critical 本身也是「warning 在某條路徑浮現、archive 路徑沒有」的同型）；quick-scale-and-ceremony-cleanup 第 4 度：新 collectMetadataCompleteness 未 guard `parseYaml` 回 null（空/註解/null metadata 不 throw）→ deref null 崩潰整個 prospec check（review confirmed critical）；姊妹 collectReviewProvenance 有同型潛在 null-deref，僅因非-git 早 return 而在正常路徑被遮蔽——同一 parse 模式的平行消費面須同套 non-mapping guard；align-language-policy-scope 第 5 度：把語言範圍收斂成單一來源時只接了 `agent-sync` 一個 render site，漏掉 `init.service` —— 同一範本兩個消費面，Handlebars 非嚴格故缺 key 只渲染空路徑列表而不報錯；且 init 只寫 AGENTS.md、agent sync 只重寫「已設定 agent 的 configPath」，claude-only 專案那份空括號檔案永久留存。修法：共用 `entryLanguageContext(scope)` 由兩處 spread，並加 init-only contract 案例〔只從 init 移除即紅〕；enforce-metadata-schema 第 6 度（單一變更內 4 次）：改 index.md Module 欄的剝除邏輯只掃了 related_modules 的消費端、漏掉 knowledge-reader 的 parseIndexModules；修 critical 時只改被點名的三行、漏掉同一句陳舊敘述的其他 5 處；修 verify 模板 :210 漏掉同檔 :251 的 Self-Check 三態宣告；套 satisfies 後未驗證它在每個實際入口是否生效（spread 不受 excess-property 檢查）；split-verify-adjudication 第 7 度，兩個面：(a) `computeChangeDigest` 的排除清單手列，新增的 `escaped-defect-report.json` 未入列，於是 `--escaped-defects --json` 一跑就把 review/test 兩條 provenance 打成 stale 且因 `generated_at` churn 永不收斂——根因是守衛測試也手列 artifact，故改為由 filename 常數推導；(b) 新 formatter 分支都過了 `sanitizeTerminal`，緊鄰的既有 record-review 分支卻沒有；harden-verify-adjudication 第 8 度 ×3（修 parallel-site 缺陷的變更內再犯）：result-trim 修正漏掃同 quality_log 資料源的 `hasVerifyGrade`（review critical，`grade: "A "` 使已 verified 變更吃 FAIL-class 誤判）、registry 註解只修 test-provenance 漏同檔 20 行外 review-provenance 平行句（review critical）、root README `--record-tests` 表格同族句遲至 verify 3/5 稽核才被抓（即修＋雙語鏡像）；archive-cli-entry 第 9 度 ×2：dry-run 的 feature-map 預測用結果導向 guard（specFiles.length>0）重推 real run 的觸發條件（routes 存在 → ensureDir 在 unsafe-slug filter 之前）——all-unsafe-slug delta-spec 下預測漏報 bootstrap（review confirmed critical F1；修法抽 readFeatureRoutes 單一來源、預測鏡射觸發而非結果）；同輪 formatter 自述「failure-class 走 stderr」卻只套 refused/notFound、漏同類的 skipped（F3 major）——同一規則未掃完同類結果集 | 10 | 6 (lib,services,cli,types,tests,templates) | playbook | add-mcp-server, src-review-round2-remediation, fix-upgrade-doc-coverage, quick-scale-and-ceremony-cleanup, align-language-policy-scope, enforce-metadata-schema, split-verify-adjudication, harden-verify-adjudication, archive-cli-entry, delegate-module-adjudication | promoted |
| git/commit-body-bulleted | commit message 正文用條列式（- 開頭逐項），不用散文段落——使用者於 add-mcp-server commit 建議時糾正 | 1 | 0 | convention | session-correction (add-mcp-server) | personal |
| docs/duplicated-count-drift | 重複的事實型數字（測試 per-layer 細項、模板/檔案 inventory）跨 README badge/Testing + _index + module README 漂移；aggregate 總數正確仍可掩蓋互相抵銷的細項誤差（433/35 vs 實測 435/33），且 drift 引擎不檢查數字正確性——校正須從 `vitest run tests/<layer>` 重新導出，不可照抄 sibling doc。review 期由 PB-003 family lens 揪出（major，已修）；enhance-skill-instructions 再現（+19 contract 斷言後 README 仍 911，verify 補修 911→930）；fix-archive-sibling-reference 第三度——en/zh README 既存分歧（944 vs 955）校正至 957；vendor-engineering-heuristics 第四度——測試數 957→971 同步了，但模板 inventory（17→19 references、50→52 .hbs）漏更，review major 揪出補修；可見「數字事實」不限測試數，新增任何檔案類別都要回掃 README ×2 + _index + module README  ｜ **Retired**: 2026-07-04（PB-004，Approved-by: benwu95, 2026-06-14；fix-upgrade-doc-coverage 第 16 度：+2 test 檔使計數 1748→1817、review fix 再 +1 → 1818，README ×2 badge+Testing 逐層 vitest 重導兩次；並揪出既存 tests README 標頭（74 檔/1,779）與 _index（1,791）互相不一致，一併校正至 76 檔/1,818——再證 aggregate 與 per-layer 都要從 vitest 重導、勿信任何既有副本；remove-deprecated-steering-command 第 15 度：移除 1 命令/1 服務/1 .hbs/3 測試檔，implement 誤把 services 計數 −1 套在既有就錯的副本（README 14→13、module-map 13→12，皆應為 14），review 獨立 re-derive `execute()`-services=14 將三份校正至一致（_index 原即 14）；測試計數 1786→1748 逐層 vitest 重導跨 README ×2＋_index＋tests README——再證須從 source re-derive、勿對既有副本套 delta；preserve-agent-config-edits 第 14 度：+27 測試後揪出既存測試計數漂移（_index 73files/1726、tests README 1760、root README badge+Testing 1726/unit1127/contract542/int17/e2e40——彼此不一致），逐層 `vitest run` 重導為 76 files/1786（unit1169/contract555/int19/e2e43）並跨 README ×2＋_index＋tests README 全數校正；再證 aggregate 不一致掩蓋細項、須從 vitest 重導不可照抄。fix-init-clobber-add-upgrade 第 13 度：skill 16→17 散落 README ×2 + CLAUDE + _index + skill-format contract，全數重導；另揪出兩處既存漂移——verify-skills.sh status-lifecycle 計數 8（promote-backfill 殘留）→10、_index services/cli 列檔數——同 commit 校正；模組 README 檔數（services 16→17、cli 30→32、templates 56→57、tests 73→76）由 knowledge-update 重導；src-review-round2-remediation 第 5 度：+34 測試 / +1 src 檔 / +2 test 檔，README ×2 badge+Testing 段與 _index 全數重導；review fix 又加 1 測試，counts 二次重導 1035→1036/unit 517→518——再證新增任何檔案類別都要回掃並從 vitest 重導每一份；complete-capability-to-feature-migration 第 6 度：刪 1 reference 模板使 .hbs 52→51、references 19→18、skill-format.test 淨 -2 使 tests 1041→1039/contract 471→469——README×2（badge+tree+Testing）+ _index + templates/tests module README 全數重導，review PB-004 lens 預先攔下；extract-backfill-spec-skill 第 8 度：skill 14→15 + .hbs 52→53 + 測試 1145→1160/contract 494→509，README ×2（badge/Testing/tree）+ _index + 模組 README 全數重導，review 揪出「14 Skill templates」subline 漏更〔major 已修〕；feature-first-backfill 第 9 度：+1 reference template〔feature-boundary-criteria〕+5 contract test 觸發 README ×2 + _index + 4 模組 README 重導，並揪出 coverage-to-90〔PR #37〕遺留的既存漂移 1160→1627〔per-layer 文件記 594/509 實為 1071/499，aggregate 掩蓋互相抵銷誤差〕，per-layer 逐層 `vitest run` 重導；add-feature-map 第 10 度（BL-040）：+31 測試 1627→1658（unit 1071→1099 / contract 499→502）、.hbs 54→55，並揪出既存 services 檔數漂移 15→16，README ×2（badge/Testing/tree）+ _index + 模組 README 全數重導；review PB-004 lens 預先攔下、verify 0 WARN；converge-archive-summaries 第 11 度（BL-041）：+1 contract test 1658→1659／contract 502→503，README ×2 + _index + tests README 重導，verify 補上 implement 漏更的 count）；backfill-promotion-path 第 12 度：+1 skill（15→16）/.hbs 55→56/+37 tests（1659→1696，contract 503→539、unit 1099→1100），README ×2（badge/Testing/skill 目錄+計數）+ _index + types/templates/services/tests 模組 README 全數重導；migrate-skill-contract-to-vitest 第 18 度：新增 skill-contract.test.ts（+20 integration test）使測試數 1840→1860、integration 18→38、檔數 77→78，逐層 `vitest run` 重導跨 README ×2 badge+Testing + index.md + tests README；review 揪出 `skill-generation.test.ts` 殘留 1/2 ref 計數未 derive〔nit 已修〕——再證計數事實須從 vitest re-derive，且既有測試檔的寫死計數同屬漂移面（不僅 README/文件）；carry-review-verify-evidence 第 19 度：+5 skill-format 契約斷言（archive Review & Verify）使測試數 1860→1865、contract 575→580，逐層 `vitest run` 重導跨 README ×2 + index.md + tests README；本次於 knowledge-update 階段主動同步（非 review finding）；**RETIRED 2026-07-04（#66）——根因由 #65 消滅：`pnpm counts`（`scripts/sync-counts.ts`）從單一真相重導所有計數，verify S/A commit-prompt 折入 feature commit；不再手動同步**；skip-unspawnable-test-command 第 20 度：沿用估算而非實測的測試總數寫進 summary／review，還跟著進了版控的 `_archived-history` 副本（宣稱 2,406、真值 2,397 passed ＋ 1 skipped）。`pnpm counts` 只管它擁有的檔案，archive 文件裡的數字無人守門 —— archive 前應以實測輸出回填，而非引用記憶中的數字 | 22 | 6 (lib,types,tests,templates,docs,services) | playbook | readme-onboarding-restructure, enhance-skill-instructions, fix-archive-sibling-reference, vendor-engineering-heuristics, src-review-round2-remediation, complete-capability-to-feature-migration, add-reverse-spec-extraction, extract-backfill-spec-skill, feature-first-backfill, add-feature-map, converge-archive-summaries, backfill-promotion-path, fix-init-clobber-add-upgrade, preserve-agent-config-edits, remove-deprecated-steering-command, fix-upgrade-doc-coverage, add-plan-flow-diagram, migrate-skill-contract-to-vitest, carry-review-verify-evidence, skip-unspawnable-test-command, restore-cli-first, restrict-identity-fallback | retired |
| archive/knowledge-sync-touched-module-readme | archive 知識同步只更新「內容有變」的 module README，漏掉「source 被改但 README 內容剛好不需改」的 module → drift engine knowledge-health 以 git commit 時間戳判該 module stale (WARN)，並被下個變更 verify V4 繼承為 pre-existing WARN（centralize 留 templates stale → fix-archive-sibling-reference 繼承；後者又對 services 自造同類）。archive Entry Gate 須對 diff 觸及 source 的每個 module 都 bump 其 README commit（真實切題註記、勿為刷時間戳寫假內容），結束前重跑 `prospec check` 確認 0 warn（知識同步完整性家族，與 docs/duplicated-count-drift 相鄰）。vendor-engineering-heuristics 第三度：feat commit 0e20ba2 改 types/services/templates/tests 四模組 source 未同 commit READMEs → 提交後 drift 立即報 4 模組 stale，archive Entry Gate 重跑 `prospec check` 揪出並同步——確認「同 commit touch README」是預防、archive 重跑是補網  ｜ **Retired**: 2026-07-04（PB-005，Approved-by: benwu95, 2026-06-14；remove-deprecated-steering-command 第 15 度：refactor commit cc34fd9 改 cli/services/templates/tests/lib 五模組 source 並同 commit 各模組 README（PB-005 預防已套）；唯 `prospec check` 在 commit 前跑 → templates README 未 commit 的 git-timestamp 報 1 模組 stale，commit 後 0 stale、archive Entry Gate 複核——再證同 commit touch README 為預防、archive 重跑為補網；preserve-agent-config-edits 第 14 度：feat/refactor 改 lib/services/templates/tests source 未同 commit 模組 README → drift 報 4 模組 stale；archive Entry Gate 同步 lib/services/templates README 內容、修正 tests README／_index 既存測試計數〔1760/1726→1786、73→76 files〕、feature-map 補 lib；殘餘 stale 為 README 未 commit 之 git-timestamp，graduate commit 後消解。fix-init-clobber-add-upgrade 第 13 度：feat commit dd9dec2 改 types/services/cli/templates/tests 五模組 source 未同 commit 模組 README → archive Entry Gate 前 drift 報 stale，/prospec-knowledge-update 同步 5 模組 README + module-map keywords + _index，graduation commit 後 0 stale；src-review-remediation 第 4 度：6 個 change 的 squash commits 改 lib/services/cli/types source 未同 commit module READMEs → drift 立即報 5 模組 stale，archive 知識同步重跑 `prospec check` 揪出並同步至 0 warn；src-review-round2-remediation 第 5 度：feat commit 1e71bdb 改 lib/services/cli/types/tests source 未同 commit module READMEs → drift 立即報 5 模組 stale，archive Entry Gate 同步 READMEs + _index 並重跑 `prospec check` 至 0 warn；complete-capability-to-feature-migration 第 6 度：feat commit b094384 改 templates/services/tests source，提交後 drift 立即報 3 模組 stale，archive 知識同步重跑 `prospec check` 至 0 warn——services 無內容變動以「spec resource 服務 Feature Spec」切題真實註記 bump，不刷假時間戳；extract-backfill-spec-skill 第 8 度：feat commit 3165de5 改 templates/types/tests source 未同 commit 模組 README → verify V4 報 templates/types stale（繼承自前一 commit 7140258）；archive Entry Gate 同步 _index + 模組 README + feature spec 並重跑 `prospec check` 至 0 warn；feature-first-backfill 第 9 度：feat commit 5b995de 改 templates/types/services/tests 四模組 source 未同 commit 模組 README → 提交後 drift 立即報 4 模組 stale；archive Entry Gate 同步 4 模組 README + _index + sdd-workflow graduation〔知識同步 commit 後 0 stale〕；add-feature-map 第 10 度（BL-040）：feat commit 67ebb65 改 types/lib/services/templates/tests 五模組 source 未同 commit 模組 README → 提交後 drift 報 5 模組 stale，archive Entry Gate 同步 5 模組 README + _index + ai-knowledge/drift-detection graduation〔graduate commit 後 0 stale〕；converge-archive-summaries 第 11 度（BL-041）：feat commit bda5ca2 改 templates/tests source 未同 commit 模組 README → drift 報 2 模組 stale，archive 同步 tests README count + templates 註記 + _index〔graduate commit 後 0 stale〕）；carry-review-verify-evidence 第 17 度：feat commit 9b23efb 改 templates/tests source 並同 commit 兩模組 README（PB-005 預防已套），post-commit drift 0 stale、archive Entry Gate 複核通過；**RETIRED 2026-07-04（#66）——根因由 #65 消滅：verify S/A commit-prompt 將每個 source-touched module README 折入 feature commit（預防），archive Entry Gate 為 backstop；source-only commit 不再抵達 archive stale** | 17 | 5 (types,lib,services,cli,tests) | playbook | centralize-index-column-schema, fix-archive-sibling-reference, vendor-engineering-heuristics, src-review-remediation, src-review-round2-remediation, complete-capability-to-feature-migration, add-reverse-spec-extraction, extract-backfill-spec-skill, feature-first-backfill, add-feature-map, converge-archive-summaries, backfill-promotion-path, fix-init-clobber-add-upgrade, preserve-agent-config-edits, remove-deprecated-steering-command, add-plan-flow-diagram, carry-review-verify-evidence | retired |
| fix/rework-misses-parallel-site | rework 一條行為規則套到 N 個明顯站點（如 6 skill 的 Next-Step Handoff）時，漏掉邏輯相同的平行站點（entry-config session-detection 仍用舊 status-only lookup，重現了已修的 review-skip）——round-2 review 才揪出。Rework 一條規則時須 grep 同邏輯全部站點（含非 skill 的 entry config / lifecycle），不只最初列舉的集合。與 security/invariant-misses-parallel-consumers、refactor/relocation-reference-sweep-completeness 同族（第 3 次）。sync-knowledge-at-verify-commit 第 2 度：把「知識同步預防點前移」規則套到 verify/archive/lifecycle 多站點時，(a) 新 commit-prompt 步驟漏掉 `scale: backfill` 站點的 carve-out（feature-slug REQ 會 mint phantom 模組，review critical），(b) services 模組 README 仍留舊「mandatory checkpoint」敘述（review major）——再證 rework 一條規則須掃全部站點含 scale 變體與模組 README；enforce-metadata-schema 第 3 度：round-1 修 critical 只改 verifier 點名的三個檔案，round-2 複審再抓出 8 處同源陳舊敘述，其中 1 處還是修正本身新寫錯的算術 ｜ **Promotion**: 併入 PB-007（觸發條件擴及「套用一個修正」），Approved-by: benwu95, 2026-07-28；score detail: frequency=3 · impact_modules=2 · kind=playbook · rule=freq≥3 ∧ modules≥2 ⇒ suggest；split-verify-adjudication 第 4 度：修 flaky timeout 時只改了自己新增測試的 3 個檔案，漏掉同屬 git-bound 負載類、但**未被本變更編輯**的第 4 個檔案（round-2 窄審以 5 次全套件跑實測揪出）。教訓補強：判定「同一族」要按**負載/失效機制**分類，不是按「我改過哪些檔案」 | 4 | 4 (lib,services,templates,tests) | playbook | enhance-skill-instructions, sync-knowledge-at-verify-commit, enforce-metadata-schema, split-verify-adjudication | promoted |
| refactor/duplicated-helper-parallel-sites | 同一邏輯 helper 在平行模組各複製一份（comment/string masking 於 module-detector.stripComments 與 drift-sources.collectImportEdges——block-comment blanking 逐字重複、覆蓋面卻分歧〔前者另去行註解、後者另去 template literal〕）；未來改 masking heuristic 須兩處同步、易漏一處。應抽單一 blankCommentsAndStrings helper 至中性 leaf module 共用（drift-sources 已 import knowledge-reader；勿造 lib→lib cycle）。與 refactor/relocation-reference-sweep-completeness、security/invariant-misses-parallel-consumers 同族。harden-feature-prefixed-req-sync 第 2 度：新 collectReadmeCounts 的 readContainedFile 複製 knowledge-reader.readTextIfExists 的 realpath 圍堵讀檔、existsContained 為第三份同型 containment 核心、README_COUNT_RULES↔README_COUNT_CLAIM 詞彙三處編碼——3 個 review advisory majors 同源於「安全/格式邏輯複製於平行站點、缺單一真相」  ｜ **Promotion**: PB-006，Approved-by: benwu95, 2026-06-22；freq=3 ∧ impact_modules=2 ⇒ rule=freq≥3∧modules≥2 fired；dedupe-init-doc-registry 第 5 度：修 restatement 的變更自身引入 4 份相同投影 lambda（registry 兩處 spread + 測試兩處 toContainEqual）——review major，實作者採納抽 asKnowledgeInitDoc 具名 helper 共用；fix-upgrade-doc-coverage 第 4 度：2 個 review advisory major 同源——INIT_DOC_REGISTRY 以字面重述 USER_MANAGED_CONVENTION_DOCS 的三個文件名而未推導/綁定〔諷刺：本變更正是修平行清單漂移〕、index 範本 context 選擇以 `'knowledge/index.md.hbs'` 魔法字串於 init.service 與 contract test 雙處特判〔Handlebars 非 strict，漂移時靜默空渲染〕；proposed 待採納；preserve-agent-config-edits 第 3 度：auto-block 就地取代正則重複於 knowledge-update.service〔services〕與新 mergeManagedDoc〔lib〕、readFileIfExists 未被 knowledge.service/knowledge-update.service〔services〕既有 3 處 read-or-empty 採用——2 個 review advisory major，於 refactor 099eb18 反向修復〔抽 content-merger.hasAutoBlock/replaceAutoBlock 單一來源 + 採 fs-utils.readFileIfExists〕；mechanize-review-gate 第 6 度：metadata.yaml round-trip（parseYamlDocument→mutate→atomicWrite）於 check.service〔新 recordReviewProvenance〕/change-plan.service/change-tasks.service 三處重複——review M4 advisory major，建議抽 `changeMetadataPath`+`updateChangeMetadata` 共用 helper〔follow-up〕；enforce-metadata-schema 第 7 度：新寫的 stripMarkdownEmphasis 與 lib/knowledge-reader 既有的 `**` 剝除重複，且字元集更寬（services 手抄 lib），對 `` `lib` ``/`*lib*` 會給出不同模組身分；抽為 knowledge-reader.stripCellEmphasis 單一來源並補跨消費端一致性測試 | 8 | 3 (lib,services,types) | convention | src-review-round2-remediation, harden-feature-prefixed-req-sync, preserve-agent-config-edits, fix-upgrade-doc-coverage, dedupe-init-doc-registry, mechanize-review-gate, enforce-metadata-schema, restore-cli-first | promoted |
| knowledge/req-citation-precedes-graduation | 在 knowledge-update 階段於 module README 引用本變更的「新」REQ id（如 REQ-TEMPLATES-101/REQ-TESTS-027）→ 因 REQ 尚未 graduate 進 feature spec，feat commit 後 `prospec check` 立即報 req-references dangling FAIL；唯有 archive Phase 3.5 graduation 把 REQ 寫進 `specs/features/` 後才消解。預期行為（drift 引擎如設計般攔截），非缺陷——但須知：feat-commit→archive 之間存在 transient dangling 視窗；補網是 archive Phase 3.5（graduate）必跑且 graduation 後重跑 `prospec check` 確認 0 fail。與 docs/duplicated-count-drift、archive/knowledge-sync-touched-module-readme 同屬知識同步家族；add-plan-flow-diagram 第 2 度：implement 於 module README（templates/tests）引用未畢業之 REQ-TEMPLATES-125 → feat commit 697124e 後 `prospec check` req-references FAIL，移除引用消解、archive Phase 3.5 graduate 後 0 fail | 2 | 2 (templates,tests) | playbook | add-dependency-knowledge, add-plan-flow-diagram | personal |
| spec/reverse-extraction-fabricates-and-undercovers | AI 反向萃取/生成 spec 兩失效模式（dogfood 於真實 Python brownfield module 證）：(a) 捏造可數事實——草稿宣稱的 enum/mapping 數量與來源實際不符（多處精確數字錯誤）；(b) 靜默部分覆蓋——僅約半數行為入 US、其餘靜默漏失。守則：可數事實對來源核實否則 `~N`/`[NEEDS CLARIFICATION]`；先枚舉行為再聚類全部、明列 deferred（coverage 須可見）。已折入 prospec-design Phase 2b-code + REQ-TEMPLATES-104 AC5 + mutation-verified contract 斷言 | 1 | 2 (templates,tests) | playbook | add-reverse-spec-extraction | personal |
| verify/coverage-not-machine-measured | verify P4「coverage ≥80%」無法機器量測——repo 未裝 @vitest/coverage-v8，質性覆蓋充足但數值未證 → 每次 verify 記 WARN（grade A 不受阻）。跨變更重複；建議為 CI 加 coverage provider 一次解決。impact 偏 tooling（tests）非多模組，依規則（modules<2）維持 personal、不 suggest-promote  ｜ **Retired**: 2026-06-16 | 3 | 1 (tests) | playbook | add-knowledge-refresh-command, raw-scan-multi-language, raw-scan-c-cpp-swift | retired |
| raw-scan/techstack-deps-language-ordering | detectTechStack 與 collectDependencies 對每個語言的偵測順序/條件須對齊，否則同一 raw-scan 的 Tech Stack 與 Dependencies 兩段互相矛盾——review 兩度揪 major：(1) Ruby+PHP polyglot（Tech Stack=ruby 但 deps fall-through 到 composer）以 Gemfile→[] 短路修；(2) C-family 語言/deps 閘不對稱（detectCFamily 需 build 檔+源副檔名、collectDependencies 只看 manifest）以共用 hasCFamilySource gate 修。新增語言時兩處務必同序、同條件並各自測試。score：freq=2 ∧ modules=2 ⇒ 未達 freq≥3，維持 personal | 2 | 2 (lib,services) | convention | raw-scan-multi-language, raw-scan-c-cpp-swift | personal |
| design/hollow-artifact-to-pass-gate | 為了過一個為別種流程設計的 gate 而產生空殼 artifact（backfill promote 一度產 plan.md/tasks.md，內含預先打勾、實際無事可做的 task）是 make-work——正解是讓 gate **scale-aware**（verify/review Entry Gate 對 backfill 例外、verify 1/5 not-applicable、archive Phase 2 tasks-skip），不是用假檔蓋過。對稱於 quick 捨 plan+delta-spec、backfill 捨 plan+tasks。add-scale-adapter playbook 的本意是逐站顯式處理 false-block，非 fabricate artifact。使用者 review 揪出 | 1 | 2 (templates,tests) | playbook | backfill-promotion-path | personal |
| security/self-attested-marker-needs-provenance | 可手改的純文字 metadata marker（如 `scale: backfill`）若用來放寬品質 gate，等於無 gate——任何人可手設以 bypass（backfill 降級 [MUST] 測試 gate，新程式碼亦可冒充）。須把 marker 綁定到可檢查的 provenance（backfill 品質降級僅在 `backfill-draft.md` 存在時套用，否則退回 standard 契約 + WARN），把「promote 是唯一入口」從 social convention 變成硬條件。verify review C2 揪出（confirmed critical）；skip-unspawnable-test-command 第 2 度：同族的反向案例：新增的誠實 skip 在 Windows 上可由 `test_command: anything.cmd` 觸發，等於用設定關掉一個 FAIL-class 閘門。結論是**不加閘門而是寫明限制** —— 誠實 skip 家族本質上可由設定觸及，該 check 的用途是防無聲假 PASS 而非不可繞過；揭露靠報告的 `skipped` + reason 與 verify 的 `not-adjudicated`（絕不 PASS）保留，並在 proposal 的 Edge Cases 明載 | 2 | 2 (lib,templates) | playbook | backfill-promotion-path, skip-unspawnable-test-command | personal |
| workflow/user-confirm-timeout-proceed-with-record | 需要使用者即時確認的規劃決策（如 scale 確認）逾時無回應 → 以推薦預設續行是正確的自主行為，但必須三處留痕：quality_log WARN、proposal Open Questions、最終摘要明示可覆寫點——讓事後批准（使用者以 y 續行）有據可查。fix-upgrade-doc-coverage：AskUserQuestion 60s 逾時，scale: standard 依準則表+前例採用並全程留痕，verify 前使用者實質批准；add-plan-flow-diagram 第 2 度：new-story AskUserQuestion 逾時，四項設計選項（觸發/位置/內容/scale）依 issue 意圖採推薦預設並三處留痕（quality_log WARN、proposal Open Questions、摘要明示可覆寫），使用者事後以 y 續行並確認 any-of 判準 | 2 | 0 | playbook | fix-upgrade-doc-coverage, add-plan-flow-diagram | personal |
| types/type-name-contradicts-role-semantics | 型別名承載的語意與使用場景矛盾（CanonicalDoc 註解宣稱 never user-customized，卻被 user-managed 清單重用）是 load-bearing 誤導——共用「形狀」時型別名應角色中性（如 ConventionDocSource），ownership 語意放在各 const 註解；proposal 載明的結構名（UserManagedDoc）被實作靜默改用他型也屬同型偏差。dedupe-init-doc-registry review major，實作者採納改名修復 | 1 | 1 (types) | convention | dedupe-init-doc-registry | personal |
| test/consistency-guard-over-extraction | 同一份指引為 skill 自包含而刻意重述於兩範本（plan-format Section 5 與 prospec-plan Phase 4 的 any-of 門檻，比照 Context7 前例）時，抽共用 partial 會因兩處需要不同「高度」的敘述而變形、且與既有 Context7 不一致、降低範本可讀性——正解是加 section-scoped 跨檔一致性契約斷言釘住同一組 token（mutation-verified），保留兩形態又擋漂移。與 refactor/duplicated-helper-parallel-sites（PB-006，抽 helper）互補：程式邏輯重複抽 helper、指令散文重述加一致性守衛 | 1 | 2 (templates,tests) | convention | add-plan-flow-diagram | personal |
| test/e2e-help-timeout-flaky-under-load | `tests/e2e/cli.test.ts`「prospec --help」在 full-suite 併發滿載下 5s testTimeout 逾時（cold CLI spawn 延遲，非邏輯缺陷）；單獨跑該檔 43/43 全過、與變更無關、full-suite 連兩跑皆重現。修法：調高該 e2e 的 testTimeout 或降低併發壓力。首見於 generate-factual-counts verify 5/5 WARN（環境性，未阻 grade A）；sync-knowledge-at-verify-commit 第 2 度：part b verify 5/5 再現同一 flake（滿載下觸發、單獨 43/43 全過），確認為穩定的環境性 flake——建議調高該 e2e testTimeout；mechanize-review-gate 第 3 度：verify 5/5 full-suite 再現（單檔 43/43 綠、未阻 grade A）；converge-constitution-audit 第 4 度：同一 e2e `--help`/`init` cold-spawn timeout 於 full-suite 再現（單檔綠、template-only 變更無關）——freq=4 但 impact_modules=1（tests）< 2，維持 personal 不晉升；建議調高該 e2e testTimeout 一次解決；support-file-module-paths 第 5 度：`tests/contract/mcp-server.test.ts` 的 knowledge_health byte-for-byte 測試（gitFixture + checkExecute，git-subprocess 密集）於 full-suite 併發滿載下 5s 逾時（單檔 1.3s、與變更無關、連兩跑重現），本次直接採建議修法——為該測試加 20s testTimeout headroom，full-suite 3/3 穩定綠；再證此 flake 不限 e2e `--help`，凡 subprocess/spawn 密集測試在預設 5s 下皆有風險，宜統一調高或降併發；split-verify-adjudication 第 6 度：同根不同站：新增約 19 個 git/spawn-bound 測試使套件時長 +51s，把**未被本變更編輯**的 `tests/contract/mcp-server.test.ts` 5s 預設推爆（5 次全套件跑 2 次紅）。關鍵證據：其鄰居有 `}, 20000)` 覆寫故 5639ms 通過、它沒有故 5108ms 死。且本變更的 `--record-tests` 會把 flaky 的 exit code 蓋章進 metadata，使機械裁決非決定性。修法一般化為：每個 git/spawn-bound 檔案在檔頭宣告 `vi.setConfig({ testTimeout: 30_000 })`，而非逐測試補覆寫（後者會反被檔案預設倒掛）｜ **Promotion**: PB-010, Approved-by: benwu95, 2026-07-29 | 6 | 2 (services,tests) | playbook | generate-factual-counts, sync-knowledge-at-verify-commit, mechanize-review-gate, converge-constitution-audit, support-file-module-paths, split-verify-adjudication | promoted |
| docs/module-readme-manual-counts-uncovered | #65 的 counts 工具（`scripts/sync-counts.ts`）覆蓋 root README.md/README.zh-TW.md/index.md/tests README 的測試計數 + `.hbs`/skill/reference inventory，但**不涵蓋各模組 README 的「(N files, N lines)」標頭**——這些仍手動維護、隨 source 改動漂移（mechanize-review-gate archive 時 lib 等模組 README 行數已失準、`pnpm counts:check` 未攔、verify V4 亦不驗）。建議擴充 counts 工具覆蓋 per-module 檔/行計數（或改為 badge 生成），把最後一類手動計數也機器化。與 docs/duplicated-count-drift（PB-004，已 retired）同族、覆蓋面互補；emit-trigger-scaffold 第 2 度：knowledge-update 手動改 services `(17→20 files)`、cli `(30→35 files)` 標頭（sync-counts 不覆蓋），且 cli 標頭原即 stale（30 vs 實際 32）——再證此類手動計數無機器守門、易累積既存漂移；enforce-metadata-schema 第 3 度 ×2：lib README 的「(21 files)」變更前實為 23（模組原始檔數不在 pnpm counts 白名單）；sdd-workflow feature spec 的 req_count 宣告 119 而實際 active 為 117，兩者皆手維護、無機制可守 ｜ **Promotion**: 重啟並窄化 PB-004（範圍收斂為 pnpm counts 未涵蓋的計數：模組 README 檔數、feature spec frontmatter 計數器），Approved-by: benwu95, 2026-07-28；score detail: frequency=3 · impact_modules=2 · kind=convention · rule=freq≥3 ∧ modules≥2 ⇒ suggest | 3 | 2 (tests,lib) | convention | mechanize-review-gate, emit-trigger-scaffold, enforce-metadata-schema | promoted |
| spec/behavior-change-omits-modified-req | 變更改動了既有 named REQ（如 REQ-AGNT-020「entry config 列 per-skill Triggers」）所治理的行為，但 delta-spec 只 ADDED 新 REQ、未把該既有 REQ 列 MODIFIED → archive Phase 3.5 graduation 只寫新 REQ、既有矛盾條款留存 → 自我矛盾的 Feature Spec。plan/delta-spec 階段須 grep 受影響行為對應的既有 REQ（及其 User Story 驗收場景）並列 MODIFIED replace-in-place。slim-skill-trigger-context review 對抗式揪出（confirmed critical）：slim CLAUDE.md 牴觸 REQ-AGNT-020，補 delta-spec MODIFIED + US-412 場景，archive 已 replace-in-place。與 knowledge/req-citation-precedes-graduation 同屬 spec-graduation 家族 | 2 | 2 (templates,tests) | playbook | slim-skill-trigger-context, align-language-policy-scope | personal |
| docs/readme-check-enumeration-manual | 每新增一個 drift check，root README（`prospec check` 說明枚舉的檢查種類，README:639）就漂移——`pnpm counts` 只覆蓋數字，這段散文枚舉無單一來源、無機器守門：#66 review-provenance、#67 metadata-completeness、#63 knowledge-size 皆未同步入該清單（reviewer 對抗式揪出，verify [SHOULD] User-Facing-Doc 於 feature commit 補列 knowledge-size）。建議把該枚舉由 `DRIFT_CHECK_IDS`＋描述生成，或加一致性契約斷言釘住「README 枚舉 ⊇ 現有 check 種類」。與 docs/module-readme-manual-counts-uncovered 同族（counts 工具未覆蓋的手動 README 內容）  ｜ **Promotion**: PB-009 · Approved-by: benwu95 · 2026-07-06；split-verify-adjudication 第 4 度：新增 `test-provenance`／`constitution-severity` 兩個 check，root README 的散文枚舉（含 zh-TW 版）仍須手動補；本輪另補「哪些維度由誰裁決」段落與兩個新旗標說明。仍無單一來源 | 4 | 3 (lib,cli,docs) | playbook | mechanize-review-gate, quick-scale-and-ceremony-cleanup, enforce-knowledge-size-budget, split-verify-adjudication | promoted |
| test/typecheck-excludes-tests-hides-type-errors | `tsconfig.json` `exclude: ["tests"]` 使 `pnpm typecheck` 不檢查測試檔——測試檔的 type-only import 破損（如遷移型別後測試仍從舊模組 import → TS2459）不被 typecheck 攔、esbuild 於 runtime strip type-only import 故 vitest 亦綠、eslint 亦不報，三閘全漏，僅對抗式 review 揪出。建議 typecheck 納入 `tests/`（或加獨立 test-typecheck job），把 test 的型別破損變成機器可攔。與 refactor/relocation-reference-sweep-completeness 同案發現 | 1 | 1 (tests) | playbook | inject-resolved-knowledge-budgets | personal |
| review/proposed-fix-or-scenario-can-be-wrong | 對抗式 review 的 finding 情境或建議修法未必正確，套用前須先重現 finding、再驗證修法不引入回歸——support-file-module-paths M1：reviewer 稱非源碼檔的 fenced ```ts import 會成偽依賴邊，但實測該 fence 已被既有 template-literal 去除（backtick 觸發）抹掉、不成邊（真正向量是非源碼檔的**裸** import 行）；且 reviewer 建議修法（非源碼檔回退 `${prefix}/${EXT}`）會 scandir 一個檔案 → 重引本變更剛修掉的 ENOTDIR。改以「非源碼檔 file 條目回傳 null、caller skip」正確修復並 mutation-verify。守則：review→fix loop 中，尤其對 reviewer 附帶的具體修法，先重現再驗回歸 | 1 | 1 (lib) | playbook | support-file-module-paths | personal |
| knowledge/generated-file-trips-module-stale | 提交 module 內的 generated 檔（`src/lib/bundled-templates.ts`，每次 template 變更由 `pnpm bundle` 重生）會使 drift engine knowledge-health 以 git 時間戳判該 module（lib）stale——即使 lib 邏輯與 README 內容皆未變、且 lib 非語意上受影響（不在 related_modules）。為刷時間戳而寫假 README 內容違反 surgical-changes；archive Entry Gate 以 related_modules 判定不阻擋此 WARN，故它會作為 pre-existing WARN 被下個變更 verify V4 繼承（同 PB-005〔retired〕家族的 generated-file 邊界）。建議：knowledge-health source scan 排除 `bundled-templates.ts`（generated artifact），或明認此為已知 timestamp artifact。emit-trigger-scaffold verify V4 WARN；converge-skill-triggers 第 2 度：改 skill.ts + build/agent-sync 後，emit 遺留的 bundled-templates.ts 使 lib 於 verify V4 再現繼承 stale（lib 仍不在 related_modules，archive 以 types/tests 判定放行） | 2 | 2 (lib,templates) | playbook | emit-trigger-scaffold, converge-skill-triggers | personal |
| knowledge/l2-readme-at-budget-cap | L2 module README 長期壓在 1000-token 上緣時，任何新增的真實不變式都會觸發 knowledge-size WARN（本輪 lib/templates/services/types 四檔超出 7-46 tokens）。壓縮自己新增的文字只救回個位數 token，根因是檔案已達上限；解法是壓縮既有內容或做 issue #64 刻意延後的 sub-module 抽取，而非降低知識密度或調高預算；enforce-metadata-schema 第 2 度：types/lib/services 變更前即為 991/998/995（上限 1000），任何增補都會觸發 knowledge-size WARN；依慣例把 Key Files 收斂回 top ~10 並壓縮冗述才回到預算內。另注意 estimateTokens 量的是 JS 字串長度/4，不是位元組——用 wc -c 會高估含非 ASCII 的檔案；split-verify-adjudication 第 3 度且擴及 L1：`_status-lifecycle.md` 變更前已 1791/1800（僅剩 39 字元），閘門語意改變必須寫進正典狀態文件，繼續壓縮等於拿其他變更的知識換本變更空間。與前兩度不同的結論：由專案擁有者決定**上調預算**（l1 2000／l2 1500），但以 REQ-TYPES-069 明文登記——記下數字、聲明 shipped 預設不變、並自陳「PASS 來自預算放寬而非知識變小」。教訓演化為：預算可調，但不得靜默調｜ **Promotion**: PB-011, Approved-by: benwu95, 2026-07-29 | 4 | 3 (lib,types,docs) | convention | align-language-policy-scope, enforce-metadata-schema, split-verify-adjudication, restrict-identity-fallback | promoted |
| review/fix-introduces-new-defect | 修正本身需要與原始程式碼同等的審查強度——enforce-metadata-schema 的 1 critical + 9 major 中，**5 個是修正過程自身引入**：偏離記錄算術不成立（被 schema 救回的紀錄不可能同時算在失敗數內）、抽 helper 時把函式插進安全防護的 JSDoc 與宣告之間造成孤兒註解、`.loose()` 為推導型別加索引簽章反而關掉 tsc 的 excess-property 檢查（比修前更糟）、`.loose()` 只作用於頂層使 AC 過度宣稱、`satisfies` 未涵蓋 spread 成員故在該站點形同虛設。若 review 在 round 2 收手，這 5 個都會流進 verify。對策：每輪修正後重跑完整 review lens，勿只複查被修的那一點  ｜ **Promotion**: PB-007 (strengthened, not a new entry), freq=3, modules=5, Approved-by: benwu95, 2026-07-31 | 4 | 5 (types,lib,services,templates,tests) | playbook | enforce-metadata-schema, fix-cli-first-regressions, pin-windows-kill-semantics, delegate-module-adjudication | promoted |
| workflow/review-provenance-stale-after-verify-sync | `review-provenance` 的 digest 涵蓋整棵工作樹（僅排除 .prospec/.claude/dist/lockfiles），而 verify 的 S/A commit prompt 明文要求在 review 之後同步 module Knowledge —— 該同步必然使基線 stale，commit 改變 HEAD 後又 stale 一次。實務順序：commit → `prospec check --record-review` → archive；否則 archive 前的 `prospec check` 會 FAIL 而看似 review 沒跑過；split-verify-adjudication 第 2 度且擴為兩個 provenance：verify 新增 `test_provenance` 後，S/A commit prompt 的知識同步同時使 review 與 test 兩條基線 stale。實務順序確立為 code 定案 → 知識同步 → `--record-review` ＋ `--record-tests` → commit → archive；filter-nonsource-modules 第 3 度，並以一手來源（`src/lib/drift-sources.ts:979` `computeChangeDigest`）定出精確邊界：雜湊內容為 `HEAD` ＋ working-tree diff ＋ untracked 檔內容，`:(exclude).prospec`。推論三條：(a) review 後只改變更工件（delta-spec 措辭）不會 stale；(b) 改 source／tests／知識檔會 stale；(c) **commit 本身必然 stale，因為 HEAD 在雜湊內**——所以本列既有的順序（commit → `--record-review` → archive）是對的。本輪我做反了（record → commit → archive），基線在 commit 後即 stale，未被攔下只因 `collectReviewProvenance` 只列舉 `.prospec/changes/` 而 bundle 已被 archive 移走；若在 commit 與 archive 之間跑 `prospec check`，review-provenance 就會 FAIL。教訓因此不只是「順序」，而是「這個 gate 的作用域會讓錯誤順序無聲通過」  ｜ **Promotion**: PB-016，Approved-by: benwu95, 2026-08-01（freq=3 · modules=3 · kind=playbook · rule=freq≥3 ∧ modules≥2 ⇒ suggest） | 3 | 3 (lib,services,templates) | playbook | align-language-policy-scope, split-verify-adjudication, filter-nonsource-modules | promoted |
| gate/exemption-order-suppresses-known-failure | 機械閘門的分支順序決定豁免的實際範圍：`evaluateTestProvenance` 把 stale 分支排在 non-zero-exit 之前，於是「既 stale 又失敗」的 proven backfill 走進豁免分支，把**已記錄的失敗**吞掉——直接違反該 REQ 的粗體條款與模板的絕對句「never suppress a recorded non-zero exit」，而該維度現在逐字採用此 check 的狀態，agent 再也救不回來。規則：豁免必須逐分支而非迴圈頂端 `continue`；「已知的失敗」永遠排在「狀態未知」之前判定；並用一條在舊順序下會轉紅的測試釘住優先序；harden-verify-adjudication 第 2 度（高一層再犯）：collector 對不可解析 command 的 source-level early-return 在枚舉之前就吞掉已記錄的非零 exit——同一規則推上 collector/evaluator 邊界（不可用性降格為事實欄位、recorded-failure 判序最優先），review 另指出 digest-null 軸仍存同型（設計保留、待 follow-up） | 2 | 2 (lib,templates) | convention | split-verify-adjudication, harden-verify-adjudication | personal |
| security/swallowed-error-degrades-to-constant-fact | 被 `catch` 吞掉的錯誤再經 `?? ''` 塌成預設值時，會把「事實」變成常數而非誠實失敗：`gitCapture` 用 execFileSync 預設 1 MB maxBuffer，`git diff HEAD` 超限丟 ENOBUFS → null → `''`，digest 退化成僅 HEAD sha，staleness 偵測靜默關閉，於是 `test-provenance` 會對已改壞的程式回報 PASS 並被記成 `adjudicator: machine`（本 repo 當時 diff 已達門檻 47%）。規則：任何餵給閘門的指紋計算，擷取失敗必須 fail-closed 回 null（誠實 skip），絕不塌成可通過的預設值；harden-verify-adjudication 第 2 度 ×2：headline 修正本身無測試命中（revert 回 `?? ''` 全套件仍綠——「honest skip」測試打在 isGitWorkTree guard 而非 capture-failure 分支），且同函式 `ls-files` 擷取仍留一處 `?? ''`——補 unborn-HEAD 與 fault-injection 兩條 revert-red 測試並 fail-closed  ｜ **Promotion**: PB-013, freq=3, modules=2, Approved-by: benwu95, 2026-07-31 | 3 | 2 (lib,services) | convention | split-verify-adjudication, harden-verify-adjudication, restore-cli-first | promoted |
| perf/expensive-fact-recomputed-per-consumer | 同一輪內不可能不同的昂貴事實，被每個消費者各算一次：`collectReviewProvenance` 與新增的 `collectTestProvenance` 各自呼叫 `computeChangeDigest`，使每次 `prospec check` 跑 10 次 git（6–10 為 1–5 的逐位元重複，約佔 40% 執行時間；實測 whole-tree digest 408ms vs 純檢查 1080ms）。規則：新增第二個消費者時，把該事實提到編排層算一次再注入，而不是複製呼叫 | 1 | 2 (lib,services) | convention | split-verify-adjudication | personal |
| impl/model-of-external-tool-unverified | 對外部工具解析規則的「模型」未經一手來源查證就寫進實作 —— 本輪把 Windows 執行檔搜尋按 PATHEXT 排序，但 libuv 根本不讀 PATHEXT（`src/win/process.c`：只試 `""`／`"com"`／`"exe"`）。後果不是功能失效而是**閘門反向失效**：常見的 npm-global-shim ＋ standalone-binary 佈局會被判成不可執行，把 FAIL-class 閘門在可正常工作的機器上變成 skip。更值得記的是我在同一份 diff 的測試註解裡把規則寫**對**了、實作卻反著做 —— 註解與實作分屬兩次思考，寫對註解不代表實作跟上。規則：跨平台／外部工具的解析語意，動手前先取一手來源（原始碼或官方安全公告），並讓斷言引用該來源的措辭而非自己的推想；pilot-mutation-testing 第 5 度，**同一句話連錯三版**：Stryker 成本的因果宣稱 r1「驅動因子是測試觸及廣度」→ r2「是 mutant 密度、不是觸及廣度」→ r3 誤以 `timeoutMS` 為逾時上限。三次都是從結果表反推機制，而產生那些數字的同一次執行早就印了 `Detected 26 static mutants (46%) ... enable "ignoreStatic"` 與兩行 dry-run（57 tests/net 78ms vs 416 tests/net 54155ms）——合起來就是完整答案。第三版只有在去讀 `mutant-test-planner.js:124`（`timeoutFactor(1.5) × netTime + timeoutMS + overhead`）與 schema 預設值之後才收束。**一手來源包含工具自己印出的 WARN 與 dry-run 行，不只原始碼**  ｜ **Promotion**: PB-012, freq=5, modules=4, Approved-by: benwu95, 2026-07-31 | 6 | 5 (lib,tests,cli,templates,services) | convention | skip-unspawnable-test-command, restore-cli-first, add-windows-smoke-ci, pin-windows-kill-semantics, pilot-mutation-testing, delegate-module-adjudication | promoted |
| test/e2e-stale-dist-false-green | `pnpm test` 不自動 build（無 pretest hook）：改動 src 後 e2e 對舊 dist spawn 出假綠——collector guard 重排改變了可觀察的 skip reason，e2e 斷言本應轉紅，卻以 stale dist 宣稱全套件綠（SC-007 假陽性），review spec-architecture lens 重建 dist 實測 1 failed 才揭穿。規則：改動 src 後、宣稱全套件綠或跑 e2e 前先 `pnpm build`（tests README pitfall 已載但無 gate 防護——考慮 pretest hook 或 CI 順序即為 gate） | 2 | 2 (cli,tests) | playbook | harden-verify-adjudication, restore-cli-first | personal |
| test/assertion-encodes-implementation-not-rule | 斷言把「實作怎麼做」而非「規則是什麼」編碼進去，於是實作錯了斷言也跟著錯，缺陷被鎖進測試。本輪兩條：「較前目錄的 `.cmd` 勝出」與「自訂 PATHEXT 順序」—— 兩者都只是複述我那個錯模型的搜尋迴圈，mutation 交換迴圈嵌套只有一條轉紅，也就是整個排序故事都建立在錯的模型上。與 test/structural-false-green 的差別：那是斷言太弱，這是斷言指向錯的東西。規則：斷言的措辭要能對照外部規則來源，並補一條「該因素不影響結果」的負向斷言把模型釘死 | 1 | 2 (lib,tests) | convention | skip-unspawnable-test-command | personal |
| story/companion-doc-us-depends-on-capability-us | new-story advisory WARN：同一變更同時交付「能力」US（`prospec status` 命令）與「指向該能力的常駐文件」US（Session Start 指向）時，INVEST Independence 局部妥協——文件 US 須在能力 US 之後交付；以變更內分層實作順序（能力先落地、模板最後改指向）化解，advisory 不擋站（verify 3/5 判非違規）。evidence: `_archived-history/2026-07-29-add-status-router.md` ## Review & Verify | 1 | 2 (templates,cli) | convention | add-status-router | personal |
| test/fixture-shape-narrower-than-real-artifact | restore-cli-first：兩個 confirmed critical 同源——引擎只對照自造 fixture 而非真實產物。recountFeatureSpecCounters 只數 `### US-`（真實 spec 多為 `## US-`，finalize 會把 5 份 spec 的正確計數改錯）；findLedgerTable 在第一個空行截斷（真實 ledger 表中有空行，41 列只見 19，upsert 會重複建 key）。守則：解析真實 repo 產物的引擎，測試必須以那份真檔為 fixture（或加一條對真檔的對帳斷言），自造 fixture 只是補充 | 1 | 2 (lib,services) | convention | restore-cli-first | personal |
| refactor/wiring-orphan-exposes-latent-destruction | restore-cli-first：把長期無入口的 knowledge-update.service 接上 CLI，等於讓其未被檢驗的破壞性路徑立刻可觸及——updateModuleReadme 會用 skeleton 蓋掉 auto block 內的人工知識（正是 2026-07-05 解耦它的根因）、updateModuleMap 用 stringifyYaml 全量重寫吃掉 module-map 的手寫註解（dogfood 當場抓到）。守則：公開一個孤兒 service 前，先逐一稽核它的寫入點並縮限，不要假設「本來就在那裡」等於「安全」 | 1 | 2 (services,lib) | playbook | restore-cli-first | personal |
| docs/cli-generated-artifact-skips-language-policy | restore-cli-first：把 review.md 的產出交給 CLI 後，findings JSON 帶什麼語言就落什麼——結果 31 列 Summary 全為英文，違反 .prospec/changes/** 的繁中 Language Policy（verify 3/5 判 FAIL）。CLI 本身語言中立，責任回到提供 JSON 的一方。守則：把 artifact 產出委派給工具時，要同時把語言規則寫進該 artifact 的格式參考，否則政策會在委派邊界斷掉；add-harness-capability-flags 第 3 度：同一根因再現——`prospec review merge` 的 findings JSON 我以英文撰寫，12 列 Summary 全英文違反 `.prospec/changes/**` 繁中規則；且**同輪還擴散到 delta-spec.md**（Description／AC／Before／After／Reason 全英文）。兩者皆於 verify 3/5 的 Language Policy 稽核才被抓到。可機械化的防線：對 `.prospec/changes/**` 的 CJK 存在性做 lint；**已機械化**：`artifact-language` drift check（第 14 個 id）現在於每次 `prospec check` 就回報缺少 artifact language 字跡的變更工件，WARN 級。本條自此有機器防線，保留為 provenance——上線首跑即抓到本專案 9 個真實遺留違反  ｜ **Promotion**: PB-014, freq=3, modules=4, Approved-by: benwu95, 2026-07-31 | 3 | 4 (templates,cli,lib,types) | convention | restore-cli-first, fix-cli-first-regressions, add-harness-capability-flags, add-artifact-language-check | promoted |
| archive/mechanical-merge-drops-req-body | restore-cli-first：archive 的機械 Feature Spec sync 對 MODIFIED REQ 會刪掉原本文只留標題（ADDED 亦僅落標題），26 個 REQ 全數需從 delta-spec＋程式重寫；若畢業者未逐一補寫，信任區會永久掉字。已登記 backlog BUG-004。守則：Phase 3.5 畢業時必逐個 REQ 對照「合併後檔案」而非只看 CLI 回報的檔案清單；add-harness-capability-flags 第 3 度（**新變體，作者側**）：這次 delta-spec 有帶 `**Spec:**` 區塊，機械合併照契約整段取代——但我的 Spec 區塊只描述本次 delta，於是 REQ-TEMPLATES-066 的 mode B/A・spec-architecture lens・hard cap 與 REQ-TEMPLATES-155 的 `scale: quick` not-applicable 規則全被覆蓋掉，信任區永久少一段既有行為。Phase 3.5 逐 REQ 比對 `git show HEAD:` 才發現。規則：`**Spec:**` 必須寫「變更後的完整需求」而非差異；report-dropped-req-bullets：**已機械化**——archive spec-sync 現以集合差集（非數量，因為實測案例舊 3 新 3 數量相同而內容全異）回報 `**Spec:**` 區塊未重述的既有 bullet，Phase 3.5 gate 逐條要求確認，delta-spec-format 同步加上作者側指示。本條自此有機器防線，保留為 provenance  ｜ **Promotion**: PB-015, freq=3, modules=3, Approved-by: benwu95, 2026-07-31 | 3 | 3 (services,templates,tests) | playbook | restore-cli-first, fix-cli-first-regressions, add-harness-capability-flags, report-dropped-req-bullets | promoted |
| docs/generated-file-maintained-but-not-its-source | fix-cli-first-regressions：`pnpm counts` 只維護 `index.md`／模組 README（生成檔），不碰 `module-map.yaml`（來源）——`prospec knowledge update` 一次重生就把計數回退（實測 index 2,775 vs map 2,773）。修法＝registry 為每個生成檔 occurrence 補上 field-scoped 來源 twin（以 YAML node range 外科式改數字、不重新序列化），並以「生成檔 == render(來源)」guard test 雙向釘住。守則：維護派生事實時，先問「這個檔是不是別人的輸出」；只修輸出＝下一次重生就回退 | 1 | 3 (tests,services,lib) | convention | fix-cli-first-regressions | personal |
| test/platform-gated-assertion-never-executed | 以平台為條件跳過的測試（`describe.runIf(process.platform === 'win32')`）在該平台沒有 CI 時等於不存在：既無法證實作，又會讓相鄰的無條件斷言把另一平台的語意寫死而長期無人發現。本輪兩例：runIf 區塊從未執行過（新增 windows-smoke 後才首次跑）、`unspawnableReason('pnpm test') === null` 把 POSIX 前提寫成跨平台（Windows 上 pnpm 就是 .cmd shim，必紅）、`process.kill(pid,'SIGTERM')` 的訊號前提在 Windows 映射為 TerminateProcess | 1 | 2 (tests,lib) | playbook | add-windows-smoke-ci | personal |
| verify/ac-evidence-requires-external-run | 驗收條件的證據只存在於外部執行（CI job 的 log）時，verify 無法在評分時取得：該維度須誠實記 WARN 而非給分，且觀測任務必須標為 `[M]`（out-of-repo、無 diff），否則 code task 未勾 → task-completion FAIL → 不能 verify，而 CI 又只在 commit/PR 後才觸發，兩者互為前提造成死鎖 | 1 | 1 (tests) | playbook | add-windows-smoke-ci | personal |
| spec/landing-block-states-delta-not-result | `**Spec:**` 落地區塊會**整段取代**既有 REQ body，所以它必須陳述「變更後的完整需求」，不是本次差異。只寫 delta 會讓信任區永久遺失既有行為，且機械合併完全照契約執行、不會報錯。畢業前逐 REQ 以 `git show HEAD:{spec}` 對照舊 body，確認新 body ⊇ 仍成立的舊行為；filter-nonsource-modules 第 2 度換一個面向：審查指出 `**Spec:**` 裡一句不實宣稱後，我把訂正打進了 delta-spec 的 **Description**（繁中敘事，Language Policy 保證它不可能落地）而非會逐字取代 body 的 `**Spec:**` 區塊——結果不實陳述照樣進信任區，工件還自相矛盾。推論：只有 `**Spec:**` 會畢業，任何對「規格內容」的訂正都必須打在該區塊，改敘事欄位等於沒改 | 2 | 2 (services,templates) | convention | add-harness-capability-flags, filter-nonsource-modules | personal |
| template/conditional-branch-contradicts-unconditional-prose | 把能力判斷收斂進 `{{#if}}` 分支時，同一文件其他位置的**無條件**祈使句不會跟著變——false 分支渲染出「本 harness 沒有 X」與「請使用 X」並存的自相矛盾文件。本輪三處（The Loop 的 spawn 步驟、Reviewer Mode A、verify 2/5 的 spawn 指示），section-scoped 斷言全部看不到。修法：站點散文改為不指名機制、機制只由 partial 敘述，並加「兩個分支下 partial 之外皆不得出現該祈使句」的守衛 | 1 | 2 (templates,tests) | playbook | add-harness-capability-flags | personal |
| review/fix-introduces-same-class-defect | 修正 review findings 的過程本身會引入同類缺陷，且**修正者的自信最高、最不會回頭驗**。report-dropped-req-bullets 一輪修 8 項、round 2 揪出其中 3 項是修正自身造成：續行規則吸收 fence/表格造成假回報（牴觸同一段註解自己寫的優先序）、正規化讓四處「verbatim」變不實宣稱、新測試只斷言 header 而漏掉 payload。規則：每一輪修完必須把**修正本身**當成新 diff 送回對抗式重驗，不能只驗「原問題是否消失」；add-artifact-language-check 第 2 度，且**同一缺陷連續三輪迭代**：collector 的走訪 r1 拋 fs 錯 → r2 改用 canonical scanner 卻只是換成拋 `ScanError`（scanner 並不吞錯）→ r3 加 `catch { continue; }` 又把不可讀的 root 變成靜默空洞 PASS，直接牴觸我自己寫進 shipped reference 的「It never passes vacuously」。同輪另有兩例：Serbian 誤判以逐名特例修，於是 `Hindi (Romanized)`／`Persian (Latin)`／`Greeklish` 同族全存活；加 fence 剝除改變了述詞，卻讓六處描述（含會逐字畢業的 delta-spec Spec bullet）留在舊敘述。**規則升級**：修正必須針對缺陷的**類別**而非被回報的那個實例——修完先問「同族還有誰」與「我是否只是把失敗換了個形狀」，並把修正本身當成新 diff 送回對抗式重驗；pilot-mutation-testing 第 3 度，且**同一句話連錯三版**：成本的因果宣稱 r1「驅動因子是測試觸及廣度」→ r2「是 mutant 密度、不是觸及廣度」→ r3 逾時機制誤以 `timeoutMS` 為上限，三次都是同一個根因——沒讀產生那些數字的執行所印出的 `WARN MutantTestPlanner`（它直接指名 26 個 static mutant 與 `ignoreStatic`）。另外 r1 的修正只改了 finding 點名的檔案，`tasks.md` 兩處舊數字與五個規劃站點的舊設計全數存活  ｜ **Promotion**: PB-007 (strengthened, not a new entry), freq=3, modules=3, Approved-by: benwu95, 2026-07-31 | 4 | 4 (services,cli,tests,lib) | playbook | report-dropped-req-bullets, add-artifact-language-check, pilot-mutation-testing, restrict-identity-fallback | promoted |
| spec/req-scope-not-listed-modified | 改動落在某條既有 REQ 的宣告範圍內時，必須把它列為 MODIFIED，否則畢業後信任區會繼續描述舊行為而與出貨產物矛盾。report-dropped-req-bullets 改了 `delta-spec-format.hbs` 段落與 `prospec-archive.hbs` step 0——兩者都是 REQ-TEMPLATES-166 的宣告範圍——卻未列 MODIFIED，2/5 fresh-context 評分者揪出。判斷方式：對每個被改的產物問「哪條既有 REQ 宣稱它的行為」，而不是只看自己新增了什麼 | 1 | 2 (templates,services) | convention | report-dropped-req-bullets | personal |
| check/vacuous-pass-worse-than-crash | 掃描類 check 遇到不可讀來源時，靜默略過比直接崩潰更糟：崩潰會被看見，空洞 PASS 會被當成「已驗證」。add-artifact-language-check 三輪都踩在同一點上，最後一版才改為整個來源降為 `{available:false, reason}` 並指名該 root。規則：collector 對任何讀不到的輸入一律降級為帶原因的 skip，絕不縮小樣本後照常回報——報告必須能區分「掃過且乾淨」與「根本沒看」。**第五輪的收斂點是改寫宣稱的形狀，不是再補一條路徑**：「不會空洞通過」被連續四輪推翻（fs throw → ScanError throw → catch{continue} → containment／不可讀檔 → symlink 條目／DEFAULT_IGNORE 目錄名），因為 canonical scanner 有一組列舉不完也不該複製的過濾器。改成枚舉四個**記錄在案**的條件、並明說其餘與真正缺席無法區分後即穩定。規則：當一個保證連續被推翻，先檢查它是不是寫成了實作結構上無法兌現的**全稱**形式——修不完的往往不是缺陷，是宣稱 | 1 | 2 (lib,services) | convention | add-artifact-language-check | personal |
| scan/name-based-detection-leaks-by-orthography | 以「語言名稱字串」決定書寫系統，必然在正寫法變體上漏：`Serbian (Latin)`／`Hindi (Romanized)`／`日本語（ローマ字）` 都會被判成基礎語言的文字，對以拉丁字母書寫該語言的專案 100% 誤報。逐名特例只擋一個成員，需規則式（宣告拉丁正寫法即視為不可判定）並涵蓋非 ASCII 的宣告方式。根本解是 Unicode 屬性比對而非名稱比對 | 1 | 1 (lib) | playbook | add-artifact-language-check | personal |
| docs/broadcast-edit-needs-per-site-verification | 把同一個措辭修正推廣到多個檔案時，必須逐檔驗證結果，不能假設「同一個改寫」自動正確。add-artifact-language-check 第五輪把定義式改寫推到七份文件，其中兩份 README 反而被寫進了全稱宣稱與「never a vacuous pass／絕不空過」——正是那五輪花力氣撤回的句子，也正是 ledger 記載一再被證偽的措辭。推廣式編輯的失敗是靜默的：每個檔案單獨看都通順；pilot-mutation-testing 第 2 度 ×2：撤回的成本數字散落七處，兩輪修正各漏一批（r1 漏 tasks.md、r2 漏 proposal／plan／tasks 的五個設計站點）。**推廣式撤回比推廣式改寫更容易漏**，因為被撤回的措辭在每個站點的形態都不同 | 2 | 2 (docs,lib) | convention | add-artifact-language-check, pilot-mutation-testing | personal |
| test/assertion-pins-wording-not-invariant | 守衛釘的是某一種措辭而非不變量，於是同義改寫即可繞過而無任何失敗。pilot-mutation-testing：AC 要求「該規則不得以帶嚴重度的列存在於準則表」，我用負向 grep 釘關鍵字；reviewer 僅把 `name the mutations` 改成 `list the mutations` 就讓缺陷完整復原且全套綠。與 test/structural-false-green 的差別：那是斷言太弱，這是斷言把**規則的一種表述**當成規則本身。規則：凡「某物不得出現」的約束，釘集合或結構（item-set vs 版控基準、closed enum、目錄列舉），不要釘關鍵字——關鍵字的涵蓋面等於作者當下的詞彙量 | 1 | 2 (tests,templates) | convention | pilot-mutation-testing | personal |
| scan/grep-silently-misses-non-ascii | 本 shell 的 `grep -rn` 會靜默漏掉含 CJK 的行——回報「沒找到」而非「讀不了」，於是空結果被當成「已清乾淨」的證據。pilot-mutation-testing：r1 修正後用 `grep -rn` 掃描殘留舊數字並宣稱完成，r2 以 locale-safe 掃描找到 tasks.md 兩處與五個規劃站點全數存活。規則：對非 ASCII 工件做完整性掃描一律用 Python／ripgrep 逐行 UTF-8 讀取，`grep -rn` 的空結果不得作為完整性證據——與 check/vacuous-pass-worse-than-crash 同型：空結果與真正缺席無法區分時就不能當保證 | 1 | 0 | playbook | pilot-mutation-testing | personal |
| design/rule-placement-follows-its-subject | 規則放錯層級會得到一條沒有施力點的建議。pilot-mutation-testing：「宣稱做過 mutation 驗證者須列出變異」被放進 review 的 test-quality 準則表並標 major，但 critical/major 依 `review-format` 定義是對**變更**提出的 finding 的權重——而該規則的主體是 **reviewer 自己的產出**，那個 major 無處可填。移入 finding 內容規則後才有落點。同輪衍生：跨檔路標必須**具名**指涉，位置式的「the row above」會隨增列無聲漂移到錯的對象。規則：寫一條規則前先問「它的主體是誰」——管產出格式的歸格式契約，管變更的才歸帶嚴重度的準則表 | 1 | 2 (templates,tests) | convention | pilot-mutation-testing | personal |
| test/mutation-verify-must-hit-the-deployed-copy | 契約測試經 renderTemplate 讀 BUNDLED_TEMPLATES（先於檔案系統），所以只改磁碟上的 `.hbs` 再跑測試＝假綠：mutation 沒有到達受測物。本輪第一次 mutation-verify 因此誤判 guard 存活，補 `pnpm bundle` 後同一 mutation 立刻轉紅。凡受測物有「來源→部署／打包」兩層者（templates→bundle、src→dist、.hbs→已部署 SKILL.md），mutation 必須套在測試真正讀取的那一層，或在套用後先重建 | 1 | 3 (tests,templates,lib) | playbook | enforce-sub-module-budget | personal |
| test/skip-fixture-does-not-discriminate-its-guard | 一個「略過 X」的測試若 fixture 同時被更前面的條件排除，被測的那道 guard 從未被鑑別——本輪 subdirectory 案例取名 `diagrams`（不帶 `.md`），`.md` 過濾早已排除它，刪掉 `!e.isDirectory()` 全綠存活。多條件 filter 的每個 conjunct 都要有只違反它一個的 fixture（此處＝名為 `README.md` 的目錄），並逐一 mutation 確認各有專屬 killer | 1 | 2 (tests,lib) | convention | enforce-sub-module-budget | personal |
| fix/tightened-guard-drops-a-measurement-fail-open | 為了安全而收緊的過濾條件可能靜默移除一項量測，讓 gate 由 fail closed 變 fail open：`e.isFile()` 排除 symlink，使 symlink README 整檔退出 knowledge-size，而舊路徑（canonical reader 的 realpath containment）本來會量到並可能發 warn。containment 屬於讀取端（realpath 拒絕逃出樹外），走訪端不該重複判斷——在走訪層擋 symlink 買不到安全，只買到少一筆量測 | 1 | 1 (lib) | convention | enforce-sub-module-budget | personal |
| archive/us-level-spec-text-has-no-graduation-carrier | `**Spec:**` 區塊只替換 `#### REQ-` 的 body，因此變更若推翻了 User Story 層的敘述（`### US-` 的 I want、Acceptance Scenario、SC-N），archive 後 feature spec 會與它自己底下的 REQ 互相矛盾，且沒有任何自動載具或檢查會攔下。做法：在 delta-spec 建「Phase 3.5 手動收斂清單」逐行列出 `file:line` 與應收斂內容（本輪 8 列），並在 graduation 時逐條核對——獨立審查者兩度在此清單上找到漏列，說明清單本身也需要對抗式檢查 | 1 | 2 (templates,services) | playbook | enforce-sub-module-budget | personal |
| docs/frozen-contract-reference-omits-the-new-field | 凍結契約新增 additive 欄位時，除了 schema 與評估器，還要更新那份「agent 被明文要求據以重現、而非逆推 JSON」的出貨 reference（`drift-report-format`）與其部署副本；漏更新會讓可重現性承諾在使用者實際查的文件上不成立。durable fix：契約測試從 Zod `shape` 推導期望鍵集合並 scope 到 fenced jsonc block——比對散文提及不足以攔下「從 shape 刪鍵、留說明文字」這種形狀 | 1 | 3 (types,templates,tests) | convention | enforce-sub-module-budget | personal |
| fix/graceful-degradation-is-not-uniform | 把「讀不到＝視為缺席」套成一條全域規則會踩到一類例外：某些檔案的缺席本身就是一個決策開關。module-map.yaml 讀不到若退化成 null，check.service 會靜默改用 constitutionFallbackRules() 並對著存在的檔案回報 not found——dependency-direction 就以錯誤 ruleset 出裁決。做法：讓讀取結果帶 reason（absent／escaped／unreadable），content 讀取 graceful、治理檔案 unreadable 則 loud，並讓 loud 走既有的 typed error（帶 code 與可行動建議），而不是讓原始 EISDIR 逃出去 | 1 | 2 (lib,services) | convention | harden-contained-reads | personal |
| test/fixture-must-reach-the-code-under-test | 「略過讀不到的檔案」用目錄當 fixture 兩度空轉：一次是 .md 過濾早已排除它（enforce-sub-module-budget），一次是 glob scanner 的 onlyFiles 在讀取前就濾掉（本輪 collectMarkdownLinks／collectReqReferences）——兩次都是 mutation 存活才發現。寫略過類測試前先問「這個 fixture 會不會在抵達被測分支前就被前面的條件吃掉」，並用能真正觸發該分支的形狀（權限撤銷的真實檔案，POSIX 閘控） | 1 | 2 (tests,lib) | convention | harden-contained-reads | personal |
| fix/sweep-the-whole-failure-class-not-the-reported-line | 修一個「讀取失敗會中止整場」的缺陷時，被回報的那一行只是該類別的一個實例：本輪 knowledge-reader 修好後，同檔案還有六個 collector 以裸 readFileSync 讀 readdir 列出的檔案，一個名為 oops.md 的目錄同樣讓 prospec check 整場拋 EISDIR（collector 是 runChecks(...) 的參數，一throw 帶走其餘 13 個 verdict）。修完點名處要 grep 同型呼叫並逐一判定，且以凍結計數（readFileSync 在該檔出現次數）把新增的第二份實作擋在門外——per-function 的結構性 guard 擋不住 | 1 | 2 (lib,tests) | playbook | harden-contained-reads | personal |
| archive/change-history-row-labels-the-change-by-hand | archive.service 的 appendToChangeHistory 把 Change 欄寫死成字串 archive-sync（src/services/archive.service.ts:1206，該函式未收到變更名），而歷史上每一列都是真實變更名——那是 graduation 時人工收斂的結果。Phase 3.5 要把自己那幾列改成變更名＋一句影響摘要，否則 feature spec 的 Change History 會退化成一串 archive-sync 而失去追溯價值。根治是讓 CLI 收變更名 | 1 | 1 (services) | playbook | harden-contained-reads | personal |
| fix/thread-the-value-through-every-sibling-branch | 把缺失的參數接上去時，只接「被回報的那一條」路徑等於把同一個缺陷留在姊妹分支：本輪 syncToFeatureSpecs 的 if 有兩條寫入路徑，appendToChangeHistory 修好了，else 的 createNewFeatureSpec 仍硬寫另一個佔位字串（initial-sync），且該行零測試覆蓋——改成任意字面 566 個測試全綠。做法：修完一條路徑，grep 同一個輸出形狀（此處＝Change History 列）的所有產生點，逐一接上並各補一條測試 | 1 | 2 (services,tests) | convention | name-change-history-rows | personal |
| test/pin-the-branch-production-actually-takes | 唯一的正負向斷言長在 fallback 分支上，而所有真實資料都走另一條：本輪 appendToChangeHistory 的斷言掛在「Change History 沒有表頭分隔列」的 EOF fallback 測試上，而 7 份 feature spec 全部滿足 in-table 插入條件——只改 in-table 那條 push 的突變存活。寫斷言前先確認 fixture 走的是哪一條分支，並讓產線路徑本身有一條 section-scoped 測試（本輪同一個坑在轉義測試上又踩一次：fixture 沒有既存 spec，於是只走建檔路徑） | 1 | 2 (tests,services) | convention | name-change-history-rows | personal |
| data/attribute-history-by-two-independent-keys | 回填歷史紀錄的歸屬不能只按單一相似度排序：本輪 15 列 Change History 若只按 REQ 重疊數排名，project-setup.md:664 會被歸給五天前引入那些 REQ 的變更（它的摘要也提到同一批 REQ）。可靠規則是兩個獨立鍵的合取——日期硬過濾候選，再要求該摘要的 Requirements 表涵蓋該列全部 REQ；非唯一解就保留原值並說明，絕不猜。改前產生對應表供人確認、改後以同規則重新推導比對，兩次執行分離才不是用寫入結果驗證寫入 | 1 | 1 (services) | playbook | name-change-history-rows | personal |
| test/verify-the-mutation-actually-applied | mutation-verify 若沒確認突變真的落到檔案上，得到的綠燈毫無意義：本輪用 perl 套一個含 ${} 與反引號的替換沒有命中，測試對著未變異的程式跑出 1 passed，我一度把它記成 survived。做法：每個 mutation 套用後先 grep 目標字串或斷言 replace 前後不同（python 的 assert new!=s），再跑測試；工具鏈已有前例——契約測試讀 bundled templates，突變也必須先 pnpm bundle 才會抵達受測物 | 2 | 2 (tests,lib) | convention | name-change-history-rows, delegate-module-adjudication | personal |
| heuristic/allowlist-erases-the-unlisted-member | 分類啟發法用「白名單＋未列出者排除」時，遇到清單沒預料到的成員會抹除真實內容，而非只是少認一個：本輪 module 偵測以原始碼副檔名白名單窄化，加上「子集為空才退回」的全專案判準，使語言不在清單上的專案丟失全部程式碼目錄——Fortran 專案（docs/ 內剛好有兩支 .sh）偵測結果只剩 `['docs']`，比不過濾更糟。極性應反轉為拒絕清單（列出明確不算的），讓未知落在「保留」一側，失誤方向變成多一個而非全滅。另一實測教訓：放寬分類是**非單調**的，因為它可能讓專案跨過門檻而關掉救援（dotfiles repo 由 `[bin, zsh]` 掉成 `[bin]`）——追求分類精確度的報酬遞減甚至為負 | 1 | 1 (lib) | convention | filter-nonsource-modules | personal |
| fix/rescue-condition-keys-on-input-not-outcome | fallback／救援的觸發判準要對準**結果**而非**輸入**：本輪「子集為空才退回完整清單」被「子集非空但太薄」繞過——k8s manifest 專案只有一支 CI script，沒有任何目錄達到門檻，偵測出零個 module，而 `knowledge init` 只在檔案不存在時寫 module-map.yaml，`modules: []` 於是永久黏著（已以端到端實測確認）。判準改為 `dirModules.length === 0` 後同時涵蓋兩種情形。通則：救援條件寫在輸入側時，任何「輸入非空但不足」的形狀都會穿過；且下游只寫一次的檔案會把一次性錯誤變成永久狀態 | 1 | 2 (lib,services) | convention | filter-nonsource-modules | personal |
| test/fallback-restores-the-result-making-the-assertion-tautological | 被測邏輯帶 fallback 時，針對「某類輸入被排除」的測試若 fixture 全屬該類，排除整類會讓 fallback 還原原本結果，斷言恆真：本輪 template／樣式測試的 fixture 只有 `.hbs`/`.vue`/`.scss`/`.css`，把整組加入拒絕清單後子集清空、fallback 還原完整清單，三個目錄照樣被偵測，測試全綠——恰好放過 proposal 指名要防的那個回歸（移除單一副檔名才會轉紅，故有部分敏感度而更難察覺）。做法：fixture 混入不屬該類的資料讓 fallback 不觸發，並以負向斷言承接（本輪加兩個純文件檔後，變異令 `docs` 出現而轉紅）。與 test/skip-fixture-does-not-discriminate-its-guard 機制不同：那是前置條件先排除 fixture，這是後置 fallback 還原結果 | 1 | 1 (tests) | convention | filter-nonsource-modules | personal |
| verify/checked-v-task-certifies-a-wrong-figure | 已勾選的 `[V]` 驗證任務可以攜帶錯誤數字通過所有機器閘門：drift 的 task-completion 與 verify 1/5 只讀 checkbox 狀態，不讀斷言內容。本輪 T13 勾選並認證「olfparser 16 → 10」，實測是 16 → 9（移除 7 個）——proposal 的 SC-002 已據實訂正，plan 與 tasks 卻殘留舊數字，由獨立審查者揪出。做法：訂正任何實測數字後 grep 全部工件（proposal／plan／tasks／summary）逐處收斂，且 `[V]` 任務的描述本身要視為需驗證的斷言而非已完成的證據 | 1 | 2 (templates,services) | playbook | filter-nonsource-modules | personal |
| design/spec-already-delegates-the-decision | 使用者一句提問（「檔名和 module 資料夾的判定沒辦法用 LLM 即時決定嗎」）推翻了我整個問題框架。issue #114 列出四張硬編碼表與四個方向，我照著挑了「哪一種啟發法調整最好」——但規格早已把裁決權指派給另一層（US-301 標題就是 AI-Driven Module Detection、REQ-KNOW-003 寫 AI 自行決定 module 邊界）。查證後真正的缺口不是啟發法不夠好，而是那一層**看不見裁決、也沒被授權推翻裁決**：raw-scan.md 的 Directory Tree 本來就顯示所有目錄，缺的是「這個目錄是被人工排除還是被拒絕清單默默丟掉」的可分辨性，以及 skill 從未被授權增刪 module-map 條目。**可操作的推論**：面對一個啟發法的缺陷，先查規格有沒有把這個決定指派給別的層；若有，正確的軸線是「那一層有沒有證據、有沒有權限」，而不是「啟發法怎麼調更準」——後者會把應該上移的判斷永久固化在錯的層 | 1 | 3 (lib,templates,services) | playbook | delegate-module-adjudication | personal |
| learn/invented-source-name-inflates-frequency | 為 archive 後、merge 前的一筆修復 harvest 教訓時，我用了一個自己發明的 source_change 名（`guard-manifest-derived-values`）——那不是一個 change，是同一個 change 同一個 PR 的後續 commit。`learn upsert` 的 `frequency` 數的是 **distinct source changes** 且只做聯集，於是兩個既有 key 各被灌水 1（7 應為 6、11 應為 10）。兩者本已 promoted 所以未改變任何晉升決定，但這破壞了 promotion-format 明文保證的「同一 ledger ⇒ 同一決策」——計數是那個保證的唯一輸入。而 CLI 沒有撤回路徑（只聯集不移除），修正只能手改 ledger。**可操作的推論**：`source_change` 必須是真實存在的 change 名（`.prospec/archive/` 下有對應目錄），同一個 change 內的第 N 次復發寫進 description 而非新增一個來源；harvest 前先確認該名字存在 | 1 | 2 (lib,services) | playbook | delegate-module-adjudication | personal |
| tooling/installed-binary-lags-source-corrupts-artifact | 以安裝版執行檔跑自己正在修的那條程式路徑，會讓缺陷污染本次變更的工件本身：本輪用安裝版 `prospec review merge`（1.0.0，早於本修復）併入第二輪發現，`F-12` 被併進 `F-6` 那一列 —— issue #116 的缺陷在自己的修復流程中當場重演，稽核軌跡少一筆且無任何錯誤訊息。規則：當變更修的是 CLI 自身行為時，該變更流程內所有相關 `prospec` 指令一律改跑 source（`pnpm cli …`）；受污染的工件要以 source CLI 從輸入重建，而非手動補列。 | 1 | 2 (cli,lib) | convention | restrict-identity-fallback | personal |
| spec/absolute-consequent-contradicts-sibling-bullet | 同一個 REQ 內把驗收 bullet 的後件寫成絕對句，會與同 REQ 的另一條 bullet 互斥並一起畢業進信任區：本輪 REQ-CLI-028 的「id-less finding opens its own row」在同鍵還有未認領 pre-round 列時不成立（該由 bullet「取表序第一個未認領者」接手），REQ-TEMPLATES-066 的「never deduplicated by Location」則與 REQ-CLI-028 的 location+lens 退回比對正面衝突 —— 兩者都由 review／verify 的獨立評分者抓出。規則：寫 Spec bullet 時，後件要標明它的前提範圍；改寫既有 bullet 後必須把同一 REQ 的所有 bullet 兩兩掃過一次矛盾。 | 1 | 2 (templates,lib) | convention | restrict-identity-fallback | personal |
